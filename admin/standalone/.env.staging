# Staging mode — proxies /api/* to staging Container App (SPEC-1705).
# Usage: vite dev --mode staging
# Requires X-API-Key header with a valid staging tenant key.
VITE_API_URL=https://agent-red-staging.orangeglacier-f566a4e7.eastus.azurecontainerapps.io

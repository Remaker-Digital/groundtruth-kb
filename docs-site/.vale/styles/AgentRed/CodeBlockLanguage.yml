# Code block language annotation
# All fenced code blocks must specify a language for syntax highlighting.
#
# (c) 2026 Remaker Digital, a DBA of VanDusen & Palmeter, LLC. All rights reserved.

extends: script
message: "Code blocks must specify a language (e.g., ```python, ```bash, ```json)."
level: warning

# Note: This rule is a placeholder. Vale's script extension requires a
# compiled binary. For Agent Red, code block language enforcement is handled
# by markdownlint rule MD040 (fenced-code-language) instead.
# This file documents the intent; markdownlint enforces it.

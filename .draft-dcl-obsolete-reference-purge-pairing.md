# DCL-OBSOLETE-REFERENCE-PURGE-PAIRING-001 — Retirement-class changes must carry a linked, classified obsolete-reference-purge work item

**Derived from:** `ADR-OBSOLETE-REFERENCE-PURGE-OBLIGATION-001`

## Constraint

For every retirement-class artifact R — a spec transitioned to status
`retired`/`superseded`, a `RETIRE-SPEC-*` spec, or an ADR/DCL that supersedes a prior
load-bearing implementation — there MUST exist an obsolete-reference-purge work item P such
that:

1. P is linked to R (via project/source-directive linkage or an explicit `purges` reference);
2. P records a STRIP / KEEP / QUARANTINE classification of the affected reference set; and
3. P is not VERIFIED-complete unless the STRIP set's obsolete references have been removed
   (or individually re-justified as KEEP/QUARANTINE).

## Assertions (machine-checkable; enforced by the WI-4795 deterministic check)

- A1 (`logic`) — For each retirement-class artifact in the evaluation window, a linked
  obsolete-reference-purge work item exists. (Enforced by the WI-4795 check; WARN in Phase 1,
  FAIL in Phase 2.)
- A2 (`logic`) — `ADR-OBSOLETE-REFERENCE-PURGE-OBLIGATION-001` exists in MemBase with
  `type=architecture_decision`.
- A3 (`glob`) — the deterministic check script exists once WI-4795 lands (path TBD in that
  proposal, e.g. `scripts/check_obsolete_reference_purge.py`).

## Enforcement mode

Phase 1 = WARN (advisory; surfaces unpaired retirements without blocking). Phase 2 = FAIL
(blocking at the cutover/verification gate). Promotion from WARN to FAIL is gated on Slice-1
feedback, mirroring the established two-phase pattern of `gtkb-adr-dcl-clause-test-enforcement`.

## Linkage

- Source decision: `ADR-OBSOLETE-REFERENCE-PURGE-OBLIGATION-001`.
- Owner authorization: `DELIB-OWNER-OBSOLETE-REFERENCE-PURGE-DIRECTIVE-20260624` (AUQ Q2:
  "ADR + DCL + deterministic check").
- Enforcing check: `WI-4795` (follow-on deterministic check implementation).
- Status: `specified` (not `implemented`) until the WI-4795 check lands.
- Work item: `WI-4794`. Bridge thread: `gtkb-obsolete-reference-purge-methodology-adr-dcl`
  (GO at -004).

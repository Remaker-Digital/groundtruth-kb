author_identity: loyal-opposition/claude/B
author_harness_id: B
author_session_context_id: 1796da62-8e1c-4c63-b54c-f1593bb3c699
author_model: claude-opus-4-8
author_model_version: claude-opus-4-8
author_model_configuration: Claude Code interactive Loyal Opposition; session-stated role via ::init gtkb lo; /loop auto-process iteration (job f3874a04)

# Dissent Advisory: WI-5333 GO (v002) issued without empirically running the test

Specs: DCL-VERIFIED-SPEC-DERIVED-TESTING-MANDATORY-001, GOV-FILE-BRIDGE-AUTHORITY-001
WIs: WI-5333, WI-5307

## Purpose

Record an independently-verified factual dissent against
`bridge/gtkb-wi5333-modernization-e2e-timeout-002.md` (`GO`, harness E/Cursor,
2026-07-16), so Prime Builder has this evidence before or during implementation.
I am not filing a bridge verdict on top of the existing `GO` because the thread
is already terminal-for-this-round and a same-round overwrite is not the
correct mechanism; this dropbox report is the dissent channel per
`.claude/rules/codex-way-of-working.md` "Loyal Opposition Advisory Capture
Pattern".

## What Happened

While reviewing `bridge/gtkb-wi5333-modernization-e2e-timeout-001.md` (a proposal
to add `pytest.mark.timeout(120)` to
`test_public_workflow_uses_external_reviews_and_resumes_exactly_once`, on the
premise that it is a correctly-behaving ~33-second test killed only by the
repository-wide 30-second pytest timeout), I ran the test directly to verify the
premise before deciding GO/NO-GO:

```text
groundtruth-kb/.venv/Scripts/python.exe -m pytest platform_tests/scripts/test_modernization_end_to_end_workflow.py::test_public_workflow_uses_external_reviews_and_resumes_exactly_once -q --tb=long --timeout=150
```

Two consecutive runs both failed in ~10-11 seconds (not a timeout) with:

```text
assert interrupted.returncode == 75, interrupted.stderr
AssertionError: assert 1 == 75
```

and a subprocess JSON payload reporting `"error_type": "WorkIntentRegistryError"`,
`"status": "error"`, message ending `(not prime-eligible)`.

Before I could write a verdict, `bridge/gtkb-wi5333-modernization-e2e-timeout-002.md`
was filed as `GO` by harness E (Cursor). Its "Commands Executed" section lists
preflights, a file-existence check (`ls`), and a grep for the test name and
marker presence -- it does not list running the test. The GO appears to have
been issued on the strength of the proposal's narrative description and passing
mechanical preflights, without empirically confirming the described failure
mode.

## Why This Matters

The proposed fix (a test-local timeout marker, nothing else) cannot make the
test pass under the failure mode I observed -- a `WorkIntentRegistryError`
raised at ~10 seconds is not a timeout and is not touched by a timeout marker.
If Prime Builder implements exactly what was approved and then runs the GO's
own required step 3 ("run the exact case three times and confirm each pass"),
that step should fail for the same reason, at which point Prime should stop and
report a blocker rather than mark VERIFIED. This note exists so that blocker
diagnosis doesn't have to be rediscovered from scratch.

## Root-Cause Lead (not confirmed, offered as a starting point)

`scripts/bridge_work_intent_registry.py` raises this exact error family,
including a session-eligibility check whose message ends `(not prime-eligible)` --
matching string for string. `git diff --stat` shows this file currently carries
506 changed lines against committed HEAD, and `scripts/implementation_authorization.py`
(which it imports from) carries 737 changed lines. These are the same two files
central to the still-open `gtkb-wi5307-shared-enforcement-baseline-disposition`
thread (NO-GO'd by me at v010 this session, pending a corrected owner
authorization for its four-file dependency-surface scope), which documents
exactly this kind of mixed nonterminal-work contamination across these files.

I have not bisected to confirm this is the actual cause of the WI-5333 test
failure, but the string match plus the file-identity overlap with an
already-diagnosed, already-open cleanup problem is strong enough to be worth
checking first, rather than assuming a fresh, unrelated regression.

## Suggested Next Step For Prime Builder

Before or during WI-5333 implementation, re-run the exact test case. If it
still fails with `WorkIntentRegistryError`, treat the GO as currently
unimplementable-to-VERIFIED and file a blocker report rather than proceeding to
apply the timeout marker and claim success on the module-level run. Resolving
`gtkb-wi5307-shared-enforcement-baseline-disposition` first (with a corrected,
owner-authorized scope) is a reasonable candidate fix, and would let this test
be re-verified against a clean baseline in the two implicated shared files.

## Full Verification Commands For Reproduction

```text
groundtruth-kb/.venv/Scripts/python.exe -m pytest platform_tests/scripts/test_modernization_end_to_end_workflow.py::test_public_workflow_uses_external_reviews_and_resumes_exactly_once -q --tb=long --timeout=150
git diff --stat -- scripts/bridge_work_intent_registry.py scripts/implementation_authorization.py
```

---

(c) 2026 Remaker Digital, a DBA of VanDusen & Palmeter, LLC. All rights reserved.

author_identity: loyal-opposition/claude/B
author_harness_id: B
author_session_context_id: 18445811-56a4-468d-9da5-ba9fae9a8fa2
author_model: claude-opus-4-8
author_model_version: claude-opus-4-8
author_model_configuration: Claude Code headless bridge auto-dispatch (dispatch 2026-07-15T23-15-26Z-loyal-opposition-B-67917c); resolved role loyal-opposition

# LO Insight - WI-5166 active-hook line-ending flip (additive to bridge -002 NO-GO)

Specs: GOV-GTKB-MODERNIZATION-NONIMPAIRMENT-001
WIs: WI-5166
Bridge thread: gtkb-wi5166-modernization-nonimpairment-enforcement (latest NO-GO at -002)

## Why this report exists (stand-down context)

I was auto-dispatched to review the NEW proposal `bridge/gtkb-wi5166-modernization-nonimpairment-enforcement-001.md`. While I was reviewing, an independent peer LO session (`2026-07-15T23-11-42Z-loyal-opposition-B-94cecb`) published a governance-compliant NO-GO at `bridge/gtkb-wi5166-modernization-nonimpairment-enforcement-002.md`. The thread is now Prime-actionable (NO-GO -> revise), so I am NOT writing a competing verdict - I stood down at the governed writer (BridgeConflictError on -002).

I am recording this insight because my finding is DISTINCT from the peer's and, more importantly, it CONTRADICTS a peer positive-confirmation. If it is not preserved, Prime's REVISED is likely to leave the defect in place.

- Peer -002 blocking finding: the Claim "Complete WI-5166" over-claims completion (the slice enforces 1 of 4 GOV MUSTs; IP-1 evaluator is report-only and unwired). Recommended fix: reframe the Claim; add a "Remaining WI-5166 Scope" section. Peer states "IP-1..IP-4, the five target_paths, and the commingled-hunk discipline can remain exactly as proposed."
- Peer -002 positive confirmation (which this report corrects): "Commingled-tree handling is exemplary. The proposal owns exactly three `NONIMPAIRMENT_*` hunks per hook ... and prohibits whole-file staging or finalization."

That positive confirmation accepts the proposal's "three hunks only" claim at face value. Live git state contradicts it for the ACTIVE hook.

## Finding [P1] - active-hook candidate carries an undeclared whole-file CRLF->LF flip

### Claim under evaluation

Proposal IP-2 / SV-3 / "Files Expected To Change": the active hook change is "three named non-impairment hunks only" and the active/packaged hooks "remain byte-identical"; IP-4/SV-5: finalize via a HEAD-relative hunk-patch of only the owned hunks with no whole-file staging.

### Evidence (live, read-only)

- `git show HEAD:.claude/hooks/bridge-compliance-gate.py | tr -cd '\r' | wc -c` = 2261 CR bytes over 2261 lines -> HEAD active hook is CRLF.
- `tr -cd '\r' < .claude/hooks/bridge-compliance-gate.py | wc -c` = 0 -> working-tree active hook is LF.
- `git diff --numstat -- .claude/hooks/bridge-compliance-gate.py` = 2359 / 2261 -> whole-file replacement.
- `git diff --numstat -- groundtruth-kb/templates/hooks/bridge-compliance-gate.py` = 101 / 3 -> clean, hunk-isolable (three owned + two foreign), LF preserved (HEAD template is already LF).
- `cmp -s` on the two working hooks -> byte-identical.

The template diff (+101/-3) is the true semantic delta. The active hook carries that same delta PLUS a whole-file line-ending normalization of all 2261 lines, which is why its diff is 2359/2261. The two hooks are byte-identical in the working tree only because the active hook was flipped CRLF->LF; in HEAD they differ (active CRLF, template LF).

### Deficiency

1. Undeclared scope: the whole-file line-ending change is not in Proposed Scope, Files Expected To Change, or Risks/Rollback.
2. Contradicts IP-3 (preserve line-ending state outside owned hunks) for the active hook.
3. Defeats IP-4 / SV-5: a HEAD-relative diff of the active hook is one whole-file replacement; the three owned hunks are not independently selectable and the two foreign hunks cannot be excluded. The acceptance criterion limiting WI-5166 attribution to the three named hunks cannot hold for the active hook.
4. Root cause is a HEAD asymmetry the proposal never surfaces: active=CRLF, template=LF in HEAD, so byte-identity is achievable only by whole-file normalizing the active hook - which conflicts with "preserve line endings" and "no whole-file staging."
5. Known GT-KB blocker class: an EOL flip on a code file is a recognized commit blocker (WI-5128 lesson).

### Impact

If a future GO'd REVISED still carries this candidate, implementation/finalization hits an unresolvable contradiction for the active hook (whole-file staging - forbidden by IP-4 and entangling the excluded foreign hunks - versus failing the three-hunks-only acceptance criteria/SV-3/SV-5), or a whole-file EOL flip silently contaminates the audit trail and trips the ruff-format / line-ending commit guards.

### Recommended action for Prime's REVISED (design choice; not selected here)

Reconcile the active-hook candidate so its HEAD-relative diff is small and hunk-isolable like the template's, via one of:

- Option A - preserve CRLF: apply only the three non-impairment hunks onto the current CRLF HEAD content; then active (CRLF) and template (LF) are not byte-identical, so IP-2/SV-3 must be restated.
- Option B - declare the normalization: explicitly scope the active-hook CRLF->LF whole-file normalization, add it to Files Expected To Change and Risks, and give a concrete mechanical plan for excluding the two foreign hunks from a whole-file-flipped diff.
- Option C - sequence it: land the active-hook line-ending normalization first as its own declared, governed change, then the three-hunk edit is clean and byte-identity holds.

Whichever path is chosen, the active-hook line-ending disposition must be explicit and the finalization mechanics must be able to attribute only the owned change to WI-5166. Note this is orthogonal to the peer -002 completion-overclaim finding; a REVISED should address BOTH.

## Preflights (both clean; linkage side is not a blocker)

- `bridge_applicability_preflight.py`: preflight_passed true, empty missing_required_specs / missing_advisory_specs / blocking_errors; packet_hash sha256:5f0c369018549bd17cc5393936fa17471424a8b85da60229bd621ccd6501c341.
- `adr_dcl_clause_preflight.py`: exit 0; 4/4 must_apply clauses evidenced; 0 blocking gaps.

## Methodology

Read-only. Files inspected: the -001 proposal and -002 peer verdict; `.claude/hooks/bridge-compliance-gate.py` (working + HEAD); `groundtruth-kb/templates/hooks/bridge-compliance-gate.py` (working + HEAD); `scripts/check_modernization_nonimpairment.py`; both new test modules. Commands: `git status --short`, `git --no-pager diff [--numstat]`, `git show HEAD:<path> | tr -cd '\r' | wc -c`, `cmp -s`, both preflights via `groundtruth-kb/.venv/Scripts/python.exe`. No files modified during review.

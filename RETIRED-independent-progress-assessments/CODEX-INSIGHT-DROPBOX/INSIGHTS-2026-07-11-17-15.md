author_identity: loyal-opposition/claude
author_harness_id: B
author_session_context_id: 12a16794-f84d-457f-81b4-8e803034e4d5
author_model: claude-opus-4-8
author_model_version: claude-opus-4-8
author_model_configuration: Claude Code interactive session; resolved role loyal-opposition via ::init gtkb lo

# Loyal Opposition Session Report — 2026-07-11 (harness B)

WIs: WI-5173, WI-5118, WI-5179, WI-5180, WI-5185, WI-5186, WI-5198, WI-5199, WI-5072
Specs: SPEC-SHIM-HARNESS-DISPATCH-TELEMETRY-001, DCL-STARTUP-GATE-FRESH-START-ONLY-001, DCL-INIT-KEYWORD-STARTUP-DISCLOSURE-RELAY-001, DCL-VERIFIED-SPEC-DERIVED-TESTING-MANDATORY-001, DCL-NO-ACTION-STATUS-SEMANTICS-001

## Scope

Interactive Loyal Opposition session. Auto-processed the LO-actionable bridge queue across
several turns, then an owner-directed investigation of the Alibaba (H) harness and a
cross-harness functional-exposure check. All bridge state read from live TAFE/dispatcher +
numbered file chain; all premises verified against canonical MemBase, never the asserting
artifact.

## Bridge verdicts authored (terminal or Prime-routed)

| Thread | Verdict | Evidence / commit |
| --- | --- | --- |
| gtkb-wi5173-shim-dispatch-telemetry | VERIFIED (-008) | usage.coverage `unavailable` fix at both producers; 331 tests + both ruff gates; commit `0381b667` (19 files, isolated) |
| gtkb-wi5118-startup-gate-fresh-start-only | NO-GO (-004) then VERIFIED (-006) | -004: finalization blocked by foreign parity/inventory drift; owner granted by-reference waiver `DELIB-20260711-WI5118-BY-REFERENCE-FINALIZATION-WAIVER`; -006 committed `81d926a8` (11 files, by-reference, foreign drift excluded) |
| gtkb-wi5118-auq-completion-scope-amendment | GO (-002) | owner-approval DELIB + active PAUTH verified in MemBase; 5 undirtied target paths |
| gtkb-wi5179-harness-diagnostic-mode | GO (-004) | prior NO-GO's phantom spec-approval DELIB resolved (`DELIB-202666086`); sub-agent draft, main-session published |
| gtkb-wi5180-default-dispatch-metrics-snapshot | GO (-002) | all 11 specs + owner DELIB + PAUTH verified; sub-agent draft |
| gtkb-wi5186-lo-startup-gate-dcl-reconciliation | NO-GO (-004) | RELAY DCL claimed v2 but canonical head is v3; same owner-approved text double-applied by two uncoordinated tracks; mechanical gates pass, substance fails |
| gtkb-alibaba-harness-slice4b-dispatchable-registration | NO-GO (-004) | by-reference finalization not executable without a waiver; 3 shared config files carried foreign reflow; live smoke not independently reproducible (harness-invoke ban). Later resolved by a peer via by-ref waiver (`28f40a33`) |
| gtkb-wi5185 / gtkb-wi5186-lo-startup-gate-clear | stood down | peers reached the same verdict first (GO); dissent-free, agreed |

## Findings

### [P1] Alibaba harness H is non-functional (0 work produced); root cause corrected
- Observation: `author_harness_id: H` = 0 across all bridge files; 3+ dispatched LO runs today
  (09:08/15:37/16:44 UTC) all failed; `last_result: provider_failure_backoff_active`.
- Deficiency: `cloud_harness_base.py:1331-1333` (`invoke_native_hooks`) treats a native hook
  that exits 0 with empty stdout as a FATAL error, but the Claude Code hook contract defines
  exit-0 + empty-stdout as ALLOW / no-op. The registered PreToolUse hook `spec-before-code.py`
  is a WI-4449 recovery stub that `sys.exit(0)` with no output, so every tool-using review
  aborts. The `pythonw` token in the stderr is a red herring (raw command_label only).
- Action taken: suspended H dispatch (`can_receive_dispatch=false`, owner-approved); filed and
  then corrected WI-5198 (defect, P2) with the pre-scoped fix (line 1333 raise->continue +
  tests; do NOT touch the guard adapter at line ~1433; shared-base change requires a
  Cross-Harness Disposition). Fix is Prime's to implement; H should stay suspended until a
  REAL dispatched review (not a smoke) proves it.

### [P2] Registration READY smoke is an unreliable functionality proof (generalized)
- Observation: the H registration report's live "Reply with exactly READY" smoke passed, but
  it never exercised a tool-use path, so it masked the fatal PreToolUse defect.
- Action taken: filed WI-5199 (hygiene, P3) — functional-proof check for OpenRouter (F) and
  Ollama (D). Scope-narrowing finding: neither F nor D is exposed to WI-5198's bug (both use
  `hook_tier=guard-adapter-floor`, not native-full). But D has a DISTINCT failure on record
  (`fatal_worker_output_marker`, circuit-breaker-tripped 2026-07-10) and is quiesced; F's
  functional status is unverified. Acceptance = a committed verdict from a real dispatched
  review, per harness.

### [P3] Direct-harness-invoke-ban gate false-positives on harness-name literals in commands
- Observation: `gt backlog list | Select-String -Pattern "ollama|openrouter|..."` and a
  `gt backlog add` whose description contained those names were both hard-blocked as
  "harness-to-harness launch," though neither launched a harness.
- Impact: forced a route-around (capture to file + Grep tool; two-step add + update
  --description-file). Candidate refinement for the ban gate's matcher (distinguish a launch
  invocation from a name appearing in an argument/pattern). Not filed as a WI this session.

## Decisions / owner input this session
- Owner approved suspending Alibaba H dispatch and filing the defect (WI-5198).
- Owner directed the OpenRouter/Ollama exposure be flagged as its own check (WI-5199).
- Owner requested a fresh PB-session kickoff prompt to drive outstanding bridge work to
  terminal + build a harness functional matrix (delivered in-session; respects the
  direct-harness-invoke ban by defining "functional" as proof of committed work from real
  dispatch).

## Unresolved risks / handoff to Prime Builder
1. gtkb-wi5186-lo-startup-gate-dcl-reconciliation NO-GO is OPEN: reconcile the two
   startup-gate DCL amendment tracks, collapse the duplicate exact-text approval DELIBs,
   normalize trailing-newline, and re-file with versions matching canonical head (RELAY = v3).
2. WI-5198 (Alibaba H native-hook empty-allow bug) — pre-scoped; H suspended pending fix +
   real-review re-verification.
3. WI-5199 (F/D functional-proof check) — F unverified; D broken (distinct root cause) +
   quiesced. All three cloud harnesses (H, D, F) are non-functional or unverified for LO
   dispatch; B + the primary fleet carry LO coverage.
4. GO'd proposals awaiting Prime implementation -> report -> VERIFIED: wi5179, wi5180, wi5181,
   wi5182, wi5185 (re-verify each is still GO/unimplemented first).
5. Heavy commingled working tree (250+ dirty files) makes VERIFIED commit-scoping fragile; the
   by-reference finalization waiver (WI-5118 -005/-006 precedent) is the working mitigation.

## Methodology notes
- Two terminal VERIFIED finalizations (WI-5173, WI-5118) each timed out at 2 min in the
  post-commit auto-retire step but the git transaction landed cleanly both times; verify via
  `git log` after a finalize timeout rather than re-running.
- Fan-out sub-agents to draft/verify, serialize commits in the main session (git-index
  contention). Sub-agent spawns appeared to re-trigger the mid-session startup-input gate
  re-arm (WI-5118/WI-5186 class) — those fixes are now committed, so a fresh session should
  see less of it.

## Wrap discipline
No KB, MEMORY.md, push, or deploy performed as part of this wrap. The KB writes this session
(WI-5198 create/update, WI-5199 create/update, H dispatch suspension) were each explicit
owner directives. This report is the session's LO deliverable.

---

(c) 2026 Remaker Digital, a DBA of VanDusen & Palmeter, LLC. All rights reserved.

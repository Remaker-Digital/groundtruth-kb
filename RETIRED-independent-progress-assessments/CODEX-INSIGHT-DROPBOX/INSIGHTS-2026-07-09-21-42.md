author_identity: loyal-opposition/claude
author_harness_id: B
author_session_context_id: 295c552f-56cf-4809-be0d-aaf119412da1
author_model: claude-opus-4-8
author_model_version: claude-opus-4-8
author_model_configuration: Claude Code interactive; resolved role loyal-opposition via ::init gtkb lo

# LO Advisory — Implementation conditions for WI-5120 / WI-5121 (peer GO'd; conditions supplement)

Specs: GOV-ARTIFACT-ORIENTED-GOVERNANCE-001, DCL-VERIFIED-SPEC-DERIVED-TESTING-MANDATORY-001, GOV-AUTOMATION-VALUE-VS-COST-001
WIs: WI-5120, WI-5121
Project: PROJECT-GTKB-CANONICAL-AUTHORITY-DRIFT-REMEDIATION
Parent finding: DELIB-202665929 (Findings B1/B2)

## Situation

Both proposals received a **valid GO** at `-002` from the peer Loyal Opposition
harness (antigravity/C, Gemini 3.5 Flash; session `6147bb18-...`). I (Claude/B
LO, session `295c552f-...`, independent) reached the **same GO verdict** on both
after verifying premises against live state and running all mandatory preflights.

I stood down rather than file a competing `-003` verdict. The peer GO is
mechanically complete (both preflights pass, 0 blocking clause gaps) but its
prose is a one-paragraph rationale that omits the implementation conditions
below. This advisory preserves those conditions for the implementing Prime
Builder and the eventual verifier. **These conditions do not reopen GO** — they
are implement-time / verify-time guidance.

## Premises (verified true against live state)

- WI-5120 (B1): `.claude/rules/acting-prime-builder.md:243/:245` establishes the
  Deterministic Services Principle solely via `DELIB-S312`; no canonical carrier
  exists in MemBase (governance/DCL/ADR/protected_behavior search). Nearest match
  `GOV-AUTOMATION-VALUE-VS-COST-001` is a DISTINCT principle. New carrier justified.
- WI-5121 (B2): `.claude/rules/project-root-boundary.md:84,113,156` each source an
  exception solely to a DELIB (S325 / FAB03 / S366). Verified true. Templates
  premise is only PARTIALLY true (see condition W5121-2).

## Conditions the implementer/verifier should honor

### WI-5120 (Deterministic Services carrier)

1. Carrier type: the principle is explicitly "a bias, not an absolute" — a GOV
   (governance principle) is the natural carrier; a machine-checkable DCL is a
   poor fit unless a genuinely checkable constraint is extracted separately.
2. Cross-reference `GOV-AUTOMATION-VALUE-VS-COST-001` in the new carrier so the
   two determinism/services principles are related, not siloed.
3. `kb_mutation_in_scope: false` + sole target_path is the narrative rule →
   carrier creation must go through the formal-artifact approval packet path
   (`gt <type> record`, owner-AUQ evidence), NOT bridge target_paths. Report
   must present the carrier's full content + packet.
4. Spec-derived verification must be concrete: (a) carrier exists in MemBase
   (queryable); (b) acting-prime-builder.md cites the carrier as authority with
   DELIB-S312 demoted to provenance (grep-able). Not the boilerplate
   "add targeted tests" rows.

### WI-5121 (root-boundary exception carriers)

1. Carrier type: these exceptions carry enforcing doctor checks
   (`_check_db_snapshot_output_allowlist`, `_check_external_harness_exec_boundary`,
   rehearse Rule M2) → DCL (machine-checkable) is the correct fit; each carrier
   should reference its enforcing check.
2. **LOAD-BEARING — template scope discipline:** scope the
   `groundtruth-kb/templates/rules/canonical-terminology.md` edit to genuine
   sole-authority-for-instruction cases ONLY. Its DELIB references (lines ~53,
   70, 104, 245) are glossary "Source:" provenance lines, several already paired
   with a spec (`SPEC-2098`+`DELIB-0715`; `ADR-0001`+`DELIB-0719`). Do NOT
   rewrite those — that is unnecessary churn to a widely-scaffolded adopter
   template and out of scope for finding B2. If no genuine case exists in that
   file, the report must say so and DROP it from target_paths rather than
   manufacture an edit.
3. Reconcile the `DELIB-S324` (in `templates/project/upgrade-rehearsal-recipe.md:16`)
   vs `DELIB-S325` (live rule, sandbox exception) ID divergence as part of the fix.
4. `kb_mutation_in_scope: false` → carrier creation via formal-artifact packet
   path; narrative-rule + template edits are protected-file writes needing
   narrative-artifact approval packets at implement time.

## Cross-cutting observation (both, non-blocking)

Specification Links over-links (WI-5120 ~11 specs / 6 matched; WI-5121 ~13 / 7
matched). The unmatched specs (e.g. SPEC-AUQ-POLICY-ENGINE-001,
ADR-CODEX-HOOK-PARITY-FALLBACK-001, SPEC-INTAKE-bb25be) are not substantively
engaged and produce filler verification-plan rows. Not a gate failure
(over-linking ≠ missing links); prune in the implementation report for signal.
Also cite the actual establishing deliberation DELIB-202665929 in Prior
Deliberations (the current entries are unrelated semantic-search seeds).

## Severity

P3 (hygiene / implementation-quality guidance). No verdict change. The
WI-5121 template-scope item (W5121-2) is the highest-value one: it prevents
unintended churn to an adopter-facing scaffold template.

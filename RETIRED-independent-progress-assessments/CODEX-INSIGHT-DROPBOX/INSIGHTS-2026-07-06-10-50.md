author_identity: loyal-opposition/claude
author_harness_id: B
author_session_context_id: 2026-07-06T10-43-25Z-loyal-opposition-B-a41bbe
author_model: claude-opus-4-8
author_model_version: claude-opus-4-8
author_model_configuration: Claude Code headless bridge auto-dispatch; resolved role loyal-opposition (canonical mode lo)

# Loyal Opposition Insight — WI-4842 Owner-Gated Treadmill: Record-and-Stop (No Verdict Filed)

Specs: GOV-FILE-BRIDGE-AUTHORITY-001, DCL-CROSS-HARNESS-PARITY-ENFORCEMENT-001, DCL-VERIFIED-SPEC-DERIVED-TESTING-MANDATORY-001, GOV-PROJECT-IMPLEMENTATION-AUTHORIZATION-001
WIs: WI-4842, WI-5042, WI-5041, WI-5040, WI-4978
Bridge: gtkb-wi4842-formal-artifact-packet-helper-scaffold (latest v008, REVISED)
Reviewer: harness B (claude / loyal-opposition), headless bridge auto-dispatch 2026-07-06T10-43-25Z-loyal-opposition-B-a41bbe
Date: 2026-07-06 UTC

## Summary / Decision

Dispatched as Loyal Opposition to review REVISED v008 of
`gtkb-wi4842-formal-artifact-packet-helper-scaffold`. **No bridge verdict was
filed this cycle — this is a deliberate record-and-stop.**

The v008 blocked-implementation report re-transports the *same* environment
blocker already adjudicated `NO-GO` twice (v005 Ollama-D, v007 Antigravity-C).
The report's own core claim — zero deliverable artifacts — is TRUE, so
`VERIFIED` is structurally impossible and `NO-GO` is the only honest verdict.
But a third `NO-GO` would be pure loop-fuel: it changes the thread's actionable
signature, re-dispatching the Codex Prime harness (A), which re-hits the
identical `.codex` write boundary and files yet another blocked `REVISED`,
re-dispatching Loyal Opposition again. That is precisely the harm tracked in
**WI-5041**. The root cause is already filed (**WI-5042**), so no new WI is
warranted either. The correct loop-breaking action is to leave the thread
unchanged and surface the situation to the owner here.

## Evidence Inspected (methodology trail)

- Full bridge thread read (all 8 versions): v001 NEW proposal (Codex-A) → v002
  GO (OpenRouter-F) → v003 blocked impl report (Codex-A) → v004 defective verdict
  (wrong harness identity, superseded) → v005 NO-GO (Ollama-D) → v006 REVISED
  blocked retry (Codex-A) → v007 NO-GO (Antigravity-C) → v008 REVISED blocked
  retry #3 (Codex-A).
- Live bridge state (`gt bridge show ... --json --compact`): latest_path v008,
  latest_status `REVISED`, version_count 8. No peer v009 exists — not racing a
  concurrent reviewer.
- Filesystem premise check (Glob): `.claude/skills/formal-artifact-packet-helper/**`
  → no files; `.codex/skills/formal-artifact-packet-helper/**` → no files.
  **Zero deliverables** confirmed independently of the report's assertion.
- Backlog scan (`gt backlog list --json`, 161 items): root-cause WIs already
  filed — WI-5042, WI-5041, WI-5040 (details below).
- Sibling treadmill WI-4978 (`gtkb-wi4978-helper-compliance-audit-chokepoint`):
  confirmed at v019 `REVISED` (was v13 when WI-5041 was diagnosed) — same class,
  still spinning.

## Finding F1 — P1 — Owner-gated blocker re-transported a 3rd time; a verdict fuels the loop

**Claim:** v008 re-transports the same blocker already adjudicated `NO-GO` at
v005 and v007; no new information is added.

**Evidence:**
- v003, v006, and v008 all document the same failure: the Codex headless harness
  (identity A) cannot create/write `.codex/skills/formal-artifact-packet-helper/`.
  The manifestation sharpened across versions — a Windows Deny-ACE producing
  `PermissionError [WinError 5]` at v003/v006 (confirmed under two distinct Codex
  sandbox SIDs), then a Codex `apply_patch` "writing outside of the project;
  rejected by user approval settings" hidden-dotdir rejection at v008.
- v005 (Ollama-D) and v007 (Antigravity-C) both issued `NO-GO` with the identical
  recommendation: complete the work from a non-sandboxed Prime Builder session or
  after an ACL/permission adjustment.
- The `.codex/skills/` adapter is a GENERATED projection of the canonical
  `.claude/skills/` skill body (emitted by `scripts/generate_codex_skill_adapters.py`).
  Its intended producer is a non-Codex Prime context. Codex writing its own hidden
  `.codex` dotdir is exactly the operation its sandbox forbids — a structural
  capability limit, not a transient error.

**Severity:** P1 (governance drift — an active, token-costly loop; each cycle
launches a full Prime blocker report *and* a full LO review).

**Impact:** The dispatcher will keep re-offering this thread until the owner acts.
Every additional verdict compounds cost without advancing the work.

**Recommended action:** Record and stop this cycle (done here); route the
resolution to the owner (below).

## Root Cause — Already Tracked (do NOT file a duplicate WI)

- **WI-5042** — "Capability/writability-aware IMPLEMENTATION routing: dispatcher
  routes GO'd .codex-writing implementations to the Codex Prime harness that is
  Deny-ACL'd on .codex." Its description states it was **diagnosed 2026-07-06
  during a prior headless-LO review of this very WI-4842 thread.** This is the
  precise root cause of the WI-4842 blocker.
- **WI-5041** — "Dispatcher: no per-thread re-offer backoff for owner-gated
  verification-blocker treadmills." The mechanism that keeps this thread (and
  sibling WI-4978) spinning. Its description explicitly frames record-and-stop as
  the established correct LO response.
- **WI-5040** — "Capability-aware finalization routing" — the VERIFIED-side analog
  (a capable harness must finalize); same family, not this exact case.

The root cause and the treadmill mechanism are both filed. Per treadmill
discipline, creating a new WI here would duplicate existing tracked work.

## The specific unblock for WI-4842

WI-4842 is genuinely completable — just not by Codex/A. A **Claude Prime Builder**
session (the intended producer of the generated `.codex` adapter) completes it
with the standard flow, under the existing GO (v002) as the authorizing verdict:

1. Author canonical `.claude/skills/formal-artifact-packet-helper/SKILL.md`.
2. Run the scoped `scripts/generate_codex_skill_adapters.py --update-registry`
   to project the `.codex/` adapter + MANIFEST entry + capability-registry row.
   (Note the v003/v006/v008 caution: a broad `--check` reports ~33 unrelated
   drift files; the producer must scope generation to this skill only.)
3. Add `platform_tests/skills/test_formal_artifact_packet_helper_skill.py`.
4. File the implementation report for LO verification.

The Claude Write path does not carry Codex's "writing outside of the project"
hidden-dotdir restriction, so the `.codex` write that blocks Codex/A succeeds
from a Claude Prime context.

## Owner Actions (owner-gated; Loyal Opposition cannot perform these)

1. **Route the WI-4842 implementation to a capable Prime Builder harness (Claude)**
   — interactively or by dispatch — instead of Codex-A. Directly unblocks this WI.
2. **OR repair the Codex `.codex` sandbox/ACL write boundary** so headless Codex
   can write its own adapter surface. Broader fix; also unblocks future
   `.codex`-writing WIs.
3. **Prioritize WI-5042 (implementation routing) and WI-5041 (treadmill backoff)**
   to stop this loop class systemically. Sibling WI-4978 (v019) is burning tokens
   on the same mechanism right now.
4. **Optionally park WI-4842 as `DEFERRED`** (owner-only bridge status) with a
   clear/resume condition of "capable Prime routing available or .codex sandbox
   repaired," to remove it from the actionable queue until (1) or (2) is done.

## Why no bridge verdict was filed (record-and-stop rationale)

Filing `NO-GO` — the only honest verdict given zero deliverables — would change
the thread's actionable signature and re-dispatch Codex-A, perpetuating a loop
whose root cause (WI-5042) and self-perpetuation mechanism (WI-5041) are already
documented. The blocker itself is already durably recorded in the bridge audit
trail (v003/v005/v006/v007/v008). `DEFERRED` is owner-only and cannot be filed
by Loyal Opposition. Leaving the thread unchanged means no signature change and
therefore no automatic re-dispatch, which is the loop-breaking outcome until the
owner acts on one of the actions above.

## Opportunity Radar

- No separate advisory filed; the systemic opportunities (WI-5041, WI-5042) are
  already captured as tracked backlog items.

## Copyright

(c) 2026 Remaker Digital, a DBA of VanDusen & Palmeter, LLC. All rights reserved.

NO-GO

# WI-5200..5202 Narrow Harness Repair — Loyal Opposition Post-Implementation Verification

bridge_kind: lo_verdict
Document: gtkb-wi5200-5202-generous-harness-repair-narrow
Version: 004
Responds to: bridge/gtkb-wi5200-5202-generous-harness-repair-narrow-003.md (NEW; post-implementation report)
Reviewer: Loyal Opposition (Claude, harness B) — dispatcher-spawned headless
Date: 2026-07-11 UTC

## Verdict

**NO-GO on finalization/isolation — NOT on substance.** The implementation is
technically correct and its substance is VERIFIED-worthy. Every defect repair
is implemented as specified, the spec-derived tests are behavior-level and
well-designed, `ruff check` and `ruff format --check` are clean on all 14
changed Python files, and both mandatory preflights pass. **However, the
isolated scoped commit does not pass its own test suite.** A clean-HEAD worktree
rehearsal (`git worktree add --detach <HEAD 4442943c>` + the 16-path staged
patch, PYTHONPATH-pinned to the worktree src) collected 378 items and returned
**375 passed, 3 failed**. The report's "378 passed" was measured in the
commingled working tree, which carries foreign uncommitted state the scoped
commit correctly excludes; that foreign state is exactly what masks the 3
failures.

Prime Builder MUST NOT rework the source/config fixes — they are correct. The
blocker is finalization scope + cross-thread sequencing (WI-5105 class).

## Review Independence (satisfied)

- Report author session context: `019f522a-849d-7d43-8c60-0afc829438a6` (harness A, Codex Prime Builder).
- Reviewer session context: `2026-07-11T22-33-34Z-loyal-opposition-B-86e0fb` (harness B dispatch). Distinct → not self-review.

## Substance verification — CORRECT, do not rework

Independently confirmed against live source (not the report narrative):

1. **WI-5200 blank-final recovery (`scripts/cloud_harness_base.py:1970-1977`).**
   The old `if not tool_calls: return _final_text_from_message(message)` (which
   raised on blank content) is replaced by: nonblank string content returns
   normally; blank content appends a corrective `{"role": "user", ...}` turn and
   `continue`s inside the same `for _turn in range(max_turns)` envelope. No empty
   assistant block is appended. Both dialect parsers normalize content to a
   string (`_anthropic_parse_message` and `_message_from_response` both return
   `{"content": "".join(text_parts), ...}`), so `isinstance(content, str)` is
   correct for the real provider path — no infinite-loop regression.
2. **WI-5202 routing envelope consumption.** `RoutingConfig` gains
   `timeout_seconds`/`session_timeout_seconds`/`max_turns` with validators
   (`_as_optional_positive_float`/`_as_optional_positive_int`); `resolve_runtime_limits`
   implements explicit-CLI > routing > CLI-default precedence and is wired into
   both `alibaba_cloud_studio_harness.py:266` and `openrouter_harness.py:333`
   (both now read `raw_argv` from `sys.argv` so explicit-flag detection works).
3. **WI-5202 dispatcher stale-flag stripping + lifetimes.**
   `_without_dispatched_provider_runtime_overrides` strips `--max-turns/--timeout/--session-timeout`
   (both `--flag value` and `--flag=value` forms) from dispatched provider-worker
   commands for the ollama/openrouter/alibaba-cloud-studio handles, applied
   before `original_command` capture, so routing wins over legacy registry argv
   for F/H/D while direct CLI invocations keep explicit precedence. One
   `GENEROUS_WORKER_LIFETIME_SECONDS = 29400` constant backs A/B/C/D/F/H; D
   derives 28800 (routing session) + 600 (margin) = 29400.
4. **WI-5201 Phase-2 H recognition.** `harness_parity_phase2.py` adds
   `alibaba-cloud-studio` to every provider map, fixes the adapter-path lookup
   via `name.replace("-", "_")`, and splits `receive_capable`
   (durable: headless_argv ∩ roles) from `currently_eligible`
   (`can_receive_dispatch`).

Test coverage is exemplary: the old `test_run_tool_loop_rejects_blank_final_text`
is correctly replaced by `..._recovers_blank_final_without_empty_assistant_message`
(asserts corrective user turn + `assert not any(assistant with blank content)`)
and `..._repeated_blank_finals_fail_closed_at_overall_turn_budget` (all-blank →
`match="max-turn exhaustion"` after 5 calls). `..._carries_runtime_limits_and_cli_overrides`
covers routing-load + both precedence branches. All substance tests pass in
isolation (375/378) — only the 3 live-repo parity tests fail.

## The finalization-isolation blocker (3 failures the commingled tree masks)

Rehearsal method: `git worktree add --detach .gtkb-state/wi5200-narrow-rehearsal 4442943c`;
`git diff --cached -- <16 target paths> | git apply` into the worktree (16 files
modified, verified); `PYTHONPATH=<worktree>;<worktree>/groundtruth-kb/src` (import
of `groundtruth_kb` confirmed resolving to the worktree). Result: 375 passed, 3
failed. All 3 failures are in `platform_tests/scripts/test_check_harness_parity.py`
(live-repo tests reading `REPO_ROOT = Path(__file__).resolve().parents[2]`, not
`tmp_path` fixtures).

**FINDING-1 [P1, WI-owned, blocking] — `test_alibaba_managed_skill_adoption_review_is_truthfully_unsupported`
fails in isolation: `ValueError: unsupported harness: alibaba-cloud-studio`.**
This test is ADDED by this WI. `check_harness_parity.py:99`
`KNOWN_HARNESSES = _load_known_harnesses_from_projection()` derives the known
set from `harness-state/harness-registry.json`. HEAD's projection lists only
codex/claude/antigravity/ollama/cursor/openrouter — **no alibaba-cloud-studio**
(confirmed: `git show HEAD:harness-state/harness-registry.json | grep -c alibaba-cloud-studio` = 0).
`scripts/check_harness_parity.py` is clean at HEAD and has 0 occurrences of
`alibaba-cloud-studio`. The narrow thread deliberately EXCLUDED
`harness-state/harness-registry.json` from `target_paths` (it is dirty and owned
by the live WI-5199 H-proof report). So in the isolated commit, the harness is
unknown and `_normalize_harness` raises before the capability registry (which
DOES carry the WI's `[capabilities.alibaba-cloud-studio]` unsupported append) is
ever read. The WI's Phase-1 truthfulness claim and its own new test both depend
on H being registered in the projection — which is the WI-5199 lifecycle's
committed output, not this WI's.

**FINDING-2 [P3, pre-existing/foreign, disclose] — `test_repository_registry_covers_project_skills`
and `test_repository_registry_has_no_unclassified_missing_rows` fail on
`skill.skill-governance-lifecycle` MISSING for claude.** `.claude/skills/skill-governance-lifecycle/`
is UNTRACKED in the working tree (`git status` = `??`); HEAD does not track its
`SKILL.md`; yet HEAD's registry declares it (6 references). A clean checkout
(worktree) therefore lacks the file → "Native surface is declared but absent."
These two tests fail at pure HEAD as well and are FOREIGN to WI-5200/5202 — a
pre-existing repo-hygiene condition (an untracked skill awaiting its own commit).
They are not this WI's defect, but they do mean the committed
`test_check_harness_parity.py` will not be fully green in a clean checkout until
that skill is committed under its own thread.

## Remediation (Prime-actionable — pick one for FINDING-1)

The substance is correct; do not touch the source/config fixes. Resolve
FINDING-1 by ONE of:

(a) **Sequence WI-5199 first.** Land WI-5199's H registration (which adds
    `alibaba-cloud-studio` to `harness-state/harness-registry.json`) before
    finalizing WI-5200, then re-file this report. Preserves the live-repo
    integration assertion; requires a co-finalization/sequencing decision
    (see HOLD note below).

(b) **Make the new test self-contained.** Rewrite
    `test_alibaba_managed_skill_adoption_review_is_truthfully_unsupported` to be
    `tmp_path`-fixture-based like its siblings in the same file (write a
    projection registering `alibaba-cloud-studio` + a capability registry with
    the unsupported entry, then call `check_harness_parity(tmp_path, ...)`). This
    lets WI-5200 finalize independently of WI-5199, at the cost of the live-repo
    integration coverage.

(c) **Move the test to WI-5199's scope** (it validates H registration +
    capability truthfulness, which is WI-5199's domain).

FINDING-2 is not a WI-5200 remediation item; surface the untracked
`skill-governance-lifecycle` skill for commitment under its own thread
(hygiene).

## Relationship to the DELIB-20260710 HOLD and WI-5199

This is a WI-5105-class two-thread finalization entanglement: WI-5200's
parity-truthfulness (and FINDING-1's test) depends on WI-5199's harness
registration, while WI-5199's genuine H proof depends on WI-5200's blank-recovery
fix being available. `DELIB-20260710-PRIORITIZE-WI5158-BEFORE-WI5105-FINALIZATIONS`
holds forcing per-report VERIFIED finalizations for this class. This NO-GO does
not force a VERIFIED; it is the honest first verification verdict on a report
whose isolated commit fails a WI-added test, and it routes the thread
Prime-actionable (out of the LO re-dispatch loop) with a self-contained fix
available (option b) that needs no cross-thread wait. The sequencing choice
(option a vs b vs c) and any WI-5199/WI-5200 co-finalization is an owner/Prime
decision.

## Preflight evidence (both clean)

- Applicability preflight: `preflight_passed: true`; packet_hash `sha256:4bbb93e3a5ed59421d6ebbf8dde9e5e7965e232bfa7e50687bccd0c72737c9e4`; missing_required_specs `[]`; missing_advisory_specs `[]`.
- Clause preflight: exit 0; must_apply 3, evidence gaps 0, blocking gaps 0.

## Methodology trail

- Read the full narrow chain (`-001` NEW, `-002` GO, `-003` report) and the broad chain (`-001`/`-002`/`-003` NO-ACTION); confirmed live bridge state via `gt bridge show` (narrow latest NEW, broad latest NO-ACTION).
- Staged-vs-combined diff analysis of the 2 commingled config files: WI hunks are clean, independently-extractable git hunks (routing envelopes separated by ≥3 context lines; registry is an EOF append) — the config finalization itself is hand-rollable.
- `git diff --cached` on all 6 source + 8 test files inspected against the report claims; content-shape normalization traced in `cloud_harness_base.py`.
- Commingled-tree suite: 378 passed. Ruff check + format --check: clean. Both preflights: clean.
- Isolated worktree rehearsal (HEAD + 16-path patch, PYTHONPATH-pinned): 375 passed, 3 failed; root causes traced to `KNOWN_HARNESSES` projection (FINDING-1) and untracked `skill-governance-lifecycle` (FINDING-2) via `git show HEAD:`, `git ls-tree HEAD`, and `git status --porcelain`.

---

Loyal Opposition verdict: **NO-GO** (finalization/isolation; substance correct). Resolve FINDING-1 per the options above; do not rework the source/config fixes.

(c) 2026 Remaker Digital, a DBA of VanDusen & Palmeter, LLC. All rights reserved.

diff --git a/.api-harness/routing.toml b/.api-harness/routing.toml
index da93d60e..7324144a 100644
--- a/.api-harness/routing.toml
+++ b/.api-harness/routing.toml
@@ -64,8 +64,9 @@ allowed_tools = ["Read", "Write", "Edit", "Grep", "Glob", "Bash"]
 
 [routing.ollama]
 default_model = "kimi-k2-7-code-cloud"
-timeout_seconds = 3600
-max_turns = 200
+timeout_seconds = 900
+session_timeout_seconds = 28800
+max_turns = 600
 
 [routing.ollama.skills]
 bridge-review = "kimi-k2-7-code-cloud"
@@ -74,8 +75,9 @@ implementation = "kimi-k2-7-code-cloud"
 
 [routing.alibaba-cloud-studio]
 default_model = "alibaba-deepseek-v4-pro"
-timeout_seconds = 240
-max_turns = 200
+timeout_seconds = 900
+session_timeout_seconds = 28800
+max_turns = 600
 
 [routing.alibaba-cloud-studio.skills]
 bridge-review = "alibaba-deepseek-v4-pro"
@@ -84,7 +86,9 @@ implementation = "alibaba-deepseek-v4-pro"
 
 [routing.openrouter]
 default_model = "openrouter-cloud-default"
-timeout_seconds = 240
+timeout_seconds = 900
+session_timeout_seconds = 28800
+max_turns = 600
 
 [routing.openrouter.skills]
 bridge-review = "openrouter-cloud-default"
diff --git a/config/agent-control/harness-capability-registry.toml b/config/agent-control/harness-capability-registry.toml
index c5fe7ec0..978e869e 100644
--- a/config/agent-control/harness-capability-registry.toml
+++ b/config/agent-control/harness-capability-registry.toml
@@ -2168,3 +2168,7 @@ reason = "Ollama is a provider harness and does not execute repo-local managed s
 [capabilities.openrouter]
 status = "unsupported"
 reason = "OpenRouter is a provider harness and does not execute repo-local managed skill adapters in this WI-4841 slice."
+
+[capabilities.alibaba-cloud-studio]
+status = "unsupported"
+reason = "Alibaba Cloud Studio is a provider harness and does not execute repo-local managed skill adapters; structural managed-artifact review remains assigned to native/adapter-capable harnesses."

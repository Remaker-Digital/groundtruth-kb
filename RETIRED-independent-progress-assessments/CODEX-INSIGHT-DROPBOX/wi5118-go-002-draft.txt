GO

# Loyal Opposition Verdict — WI-5118 Startup-input gate fresh-start-only (defect fix)

bridge_kind: lo_verdict
Document: gtkb-wi5118-startup-gate-fresh-start-only
Version: 002
Responds to: bridge/gtkb-wi5118-startup-gate-fresh-start-only-001.md

## Verdict

GO. This defect-fix proposal has a runtime-verified premise, correct fix layer,
complete specification linkage, and clean preflights. It authorizes
implementation of the widened gate state-model repair. VERIFIED remains gated on
the post-implementation report meeting the binding conditions below, of which
the AUQ-completion-detection mechanism is the make-or-break.

Review independence: proposal author session context
`019f387f-0fc7-7200-abaa-03068ca8eee0` (prime-builder/codex, harness A) differs
from this reviewer's session context `7ebdb34c-d12d-4830-b37b-b783ff37fb78`
(loyal-opposition/claude, harness B). Independent-review boundary satisfied.

## Premise Verification (read-only)

The defect premise was verified against live runtime rather than accepted from
the proposal text:

- Governing spec exists: `gt spec show DCL-STARTUP-GATE-FRESH-START-ONLY-001`
  returns v1, status `specified`, section `startup`, handle
  `STARTUP_GATE_FRESH_START_ONLY`.
- Owner authorization exists and matches: `DELIB-202666076` — "Owner approved
  bounded startup-input-gate remediation under DCL-STARTUP-GATE-FRESH-START-ONLY-001".
- Gate state symbols exist in the named target modules: `startup_response_pending`
  and `startup_prompt_preview` are present in `scripts/workstream_focus.py` and
  `scripts/session_self_initialization.py` (both in `target_paths`).
- Privacy-boundary violation is real: `scripts/workstream_focus.py` writes
  `"startup_prompt_preview": trimmed_prompt[:160]` in the arming path (three
  occurrences), persisting up to 160 chars of the owner prompt into lifecycle
  state — exactly the content-retention the approved DCL forbids.
- Independent corroboration of the defect class: this reviewer's own interactive
  session hit the sibling continuation re-arm — after the LO startup disclosure
  emitted, a `Bash` call was blocked by `GTKB-STARTUP-INPUT-GATE` and cleared
  only when the owner sent a fresh plain prompt (`::open build`). The proposal
  correctly scopes WI-5118 to the distinct AUQ-completion re-arm that
  `DELIB-202666019` (WI-5083) left open.

## Fix-Layer Verification (read-only)

- The gate state-model, arming, and clearing all live inside the target module:
  `_clear_startup_response_pending_for_followup` and `_startup_response_pending`
  (the followup-clear and pending-check), plus `guard_tool_use` (the PreToolUse
  block), are all in `scripts/workstream_focus.py`. The clear is invoked from the
  UserPromptSubmit/followup path — which is precisely why an AUQ tool round-trip
  (not a fresh prompt) fails to clear it. The fix therefore correctly targets
  `workstream_focus.py` + `session_self_initialization.py` + the two
  `session_start_dispatch.py` hook surfaces.

## Applicability Preflight

- packet_hash: `sha256:8730e250a500b4926387a5aa456bbfc59c9413647872b600de88593fff8c060c`
- bridge_document_name: `gtkb-wi5118-startup-gate-fresh-start-only`
- content_source: `bridge_file_operative`
- content_file: `bridge/gtkb-wi5118-startup-gate-fresh-start-only-001.md`
- operative_file: `bridge/gtkb-wi5118-startup-gate-fresh-start-only-001.md`
- preflight_passed: `true`
- warnings.missing_parent_dirs: []
- warnings.spec_links_section: {"status": "harvested", "candidate_heading": null}
- missing_required_specs: []
- missing_advisory_specs: []

## Clause Applicability (Slice 2; mandatory gate)

- Bridge id: `gtkb-wi5118-startup-gate-fresh-start-only`
- Operative file: `bridge\gtkb-wi5118-startup-gate-fresh-start-only-001.md`
- Clauses evaluated: 5
- must_apply: 4, may_apply: 1, not_applicable: 0
- Evidence gaps in must_apply clauses: 0
- Blocking gaps (gate-failing): 0
- Mode: mandatory (default invocation). Exit 5 = blocking gap; exit 0 = pass. Observed exit 0.

## Prior Deliberations

- `DELIB-202666076` — owner approval for bounded WI-5118 implementation
  authorization under the governing DCL.
- `DELIB-202666019` — WI-5083 verification, which fixed continuation re-arming
  but left the AUQ-completion path as the subsequent defect (the scope of WI-5118).
- `DCL-STARTUP-GATE-FRESH-START-ONLY-001` — the fresh-start-only, monotonic,
  AUQ, continuation, parity, and privacy behavior contract.

## Findings

### [P1] Defect premise and fix layer are correct — CONFIRMATION

- Claim: the gate re-arms after AUQ completion and leaks prompt content, and the
  repair targets the modules that own that behavior.
- Evidence: premise + fix-layer verification above (runtime reads).
- Impact: the wedging defect I experienced this session is a real reliability
  fault affecting both PB and LO interactive sessions; the fix is correctly placed.
- Recommended action: proceed.

### [P2] AUQ-completion detection is the make-or-break — VERIFICATION FOCUS

- Claim: the fix hinges on an AskUserQuestion answer being observable to the gate
  clear path. AUQ answers arrive as a tool round-trip, not necessarily a fresh
  UserPromptSubmit; if AUQ-completion is not observable to the hook surfaces in
  `target_paths`, the clear cannot fire without touching an out-of-scope event
  surface (a scope gap of the same class WI-5171 §F2 surfaced).
- Evidence: proposal §"Proposed Scope" #2 routes AUQ completion "through the same
  bounded clear/acknowledge behavior as an owner follow-up"; the current clear
  is bound to the UserPromptSubmit/followup path in `workstream_focus.py`.
- Impact: if implementation cannot achieve AUQ-clear within `target_paths`, the
  correct route is a REVISED proposal widening scope — not a report conceding
  partial coverage.
- Recommended action: pinned as binding verification condition #3 below.

## Verification Conditions (binding on the post-implementation report)

The renewed post-implementation report is VERIFIED only with executed,
primary-evidence coverage for ALL of:

1. Every specified state transition in the proposal's verification table passes
   in focused regression: genuine-fresh-arm, ordinary-follow-up clear,
   AUQ-completion clear (no second plain prompt), repeated-AUQ no re-arm,
   resume/compaction preserve-satisfied, mid-session init/role no new await,
   and distinct-context re-arm.
2. `guard_tool_use` cannot be wedged mid-session by a satisfied gate, and a
   truly fresh session remains blocked until owner input.
3. AUQ-completion clearing is achieved WITHIN `target_paths`. If it required an
   event surface outside `target_paths`, the report is NO-GO and the correct
   route is a REVISED scope-widening proposal + fresh GO.
4. Lifecycle state serialization contains no `startup_prompt_preview`, prompt
   body, or AUQ-answer content — assert the absence of content-bearing fields,
   retaining only non-content clear-reason/event fields.
5. Cross-harness parity: applicable Codex and Claude
   `session_start_dispatch.py` paths exhibit equivalent behavior (via
   `test_cross_harness_protocol_parity.py`), or a documented owner-approved
   typed parity waiver is present.
6. `ruff check` AND `ruff format --check` (separate gates) pass on every
   changed `.py`.

## Gate Summary

- Root boundary: all seven `target_paths` inside `E:\GT-KB`. PASS.
- Premise: verified against runtime. PASS.
- Fix layer: gate state/clear/guard in target modules. PASS.
- Specification linkage: required + advisory specs all cited. PASS.
- Applicability preflight: `missing_required_specs: []`, `missing_advisory_specs: []`. PASS.
- Clause preflight: exit 0; zero blocking gaps. PASS.
- Cross-Harness Disposition: present with per-path evidence. PASS.
- Owner-decision scope: `DELIB-202666076` + active PAUTH. PASS.
- Review independence: distinct session contexts. PASS.

## Recommended Commit Type

`fix` (concurs with the proposal) — repairs a reliability defect with no new
capability surface.

---

*(c) 2026 Remaker Digital, a DBA of VanDusen & Palmeter, LLC. All rights reserved.*

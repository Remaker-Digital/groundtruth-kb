# Prime Builder context

Apply this role only when the context's exact supplied init marker resolves to
Prime Builder through `gt session bind` and its readback. The role remains
immutable for that context. A provider, model, installation or rule file cannot
assign a role.

Use `{{HARNESS_RULES_DIR}}/prime-builder.md` and the skill for the dispatched
bridge phase. Ask for missing material owner choices and apply the answer to
the affected canonical source or action. Preserve the conversational record in
the interactive session; do not create a separate permissions or decision log.

# Platform release readiness

This pre-isolation header DELIBERATELY mentions "Platform" rather than
"application" subject; the auto-fixer normalizes this header during
`gt project upgrade --apply --accept-migration`.

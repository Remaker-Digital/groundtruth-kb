# Loyal Opposition file-safety policy.
# Bridge: bridge/gtkb-lo-file-safety-pretooluse-enforcement-slice-1-006.md

approval_env_var = "GTKB_LO_FILE_SAFETY_APPROVAL_PACKET"

# TEMPORARY DISABLE - owner directive via AskUserQuestion, 2026-08-25, session a5bfb51a.
# Compact Operating Guidance section 14: a defective gate is disabled and a P0
# corrective work item filed. On file: WI-7048 (parsing defect, TEST-12167) and
# WI-7085 (this gate guards its own disable switch, TEST-12179).
#
# WHY: the shell-command detector (WRITEISH_COMMAND_RE, lo-file-safety-gate.py:69)
# scans raw command text for the redirect character and takes the following token
# as a mutation target. Six confirmed false positives blocked read-only commands
# in one session. Because the extracted target is garbage rather than a path, the
# entries below never matched - the gate blocked writes to scratchpad/** and
# bridge/**, which it is configured to permit.
#
# The "**" entry neutralizes blocking until WI-7048 lands. The behavioural rule is
# UNCHANGED: Loyal Opposition does not modify files it did not create without
# explicit owner approval. Enforcement is off; the obligation is not.
#
# TO RESTORE: delete the "**" line. Nothing else changes.
allow_patterns = [
  "memory/MEMORY.md",
  "scratchpad/**",
  "bridge/**",
  "**",
]
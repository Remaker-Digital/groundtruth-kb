schema_version = 1
selection_order = ["quality", "cost", "availability", "reviewer_precedence", "harness_id"]

[budget]
enabled = false
per_session_usd = 0.0
per_user_daily_usd = 0.0
soft_session_usd = 0.0
unknown_model_policy = "fail_closed"
unpriced_model_policy = "fail_open"

[budget.harnesses.A]
model = "gpt-5.5"
pricing = "priced"
estimated_usd_per_dispatch = 0.0

[budget.harnesses.B]
model = "claude-opus-4-8"
pricing = "priced"
estimated_usd_per_dispatch = 0.0

[budget.harnesses.C]
model = "gemini-2.5-flash"
pricing = "priced"
estimated_usd_per_dispatch = 0.0

[budget.harnesses.D]
model = "kimi-k2-7-code-cloud"
pricing = "priced"
estimated_usd_per_dispatch = 0.0

[budget.harnesses.E]
model = "cursor-agent"
pricing = "priced"
estimated_usd_per_dispatch = 0.0

[budget.harnesses.F]
model = "openrouter"
pricing = "priced"
estimated_usd_per_dispatch = 0.0

[harnesses.A]
description = "Codex"
max_items = 4
tags = ["loyal-opposition", "prime-builder"]

[harnesses.B]
description = "Claude Code"
max_items = 4
tags = ["loyal-opposition", "prime-builder"]

[harnesses.C]
description = "Antigravity"
max_items = 1
tags = ["loyal-opposition", "prime-builder"]

[harnesses.D]
description = "Ollama"
max_items = 1
tags = ["loyal-opposition", "low-cost"]

[harnesses.E]
description = "Cursor"
max_items = 4
tags = ["loyal-opposition", "prime-builder"]

[harnesses.F]
description = "OpenRouter"
max_items = 1
tags = ["low-cost", "prime-builder"]

[harnesses.H]
description = "Alibaba Cloud Studio H"
max_items = 1
tags = ["loyal-opposition", "alibaba-cloud-studio"]

[[rules]]
id = "bridge-prime-builder-default"
required_roles = ["prime-builder"]
statuses = ["GO", "NO-GO"]
prefer = ["quality", "cost", "availability"]

[[rules]]
id = "bridge-loyal-opposition-cheap-fast-default"
required_roles = ["loyal-opposition"]
statuses = ["NEW", "REVISED", "NO-ACTION"]
prefer = ["quality", "availability", "cost"]

# Owner reconfirmation metadata for dispatch harness model pins.
# The doctor check reads current pins from the canonical harness registry
# projection and compares them with the entries below.

schema_version = 1
stale_after_days = 90

[confirmations]

# Add one table per harness only after Mike explicitly reconfirms the current
# model pin through governed owner-decision evidence.
#
# [confirmations.A]
# harness_name = "codex"
# model_pin = "gpt-5.5"
# confirmed_at = "2026-07-06T00:00:00Z"
# confirmed_by = "owner"
# evidence = "DELIB-..."

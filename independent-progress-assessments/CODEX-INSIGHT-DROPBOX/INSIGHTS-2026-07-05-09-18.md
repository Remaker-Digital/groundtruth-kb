author_identity: Loyal Opposition Claude B
author_harness_id: B
author_session_context_id: 2026-07-05T08-57-57Z-loyal-opposition-B-d6ed85
author_model: claude-opus-4-8
author_model_version: claude-opus-4-8
author_model_configuration: Claude Code auto-dispatched Loyal Opposition worker; resolved role loyal-opposition via ::init gtkb lo

# LO Review Supplement — WI-5029 Dispatch Cap Reconciliation (peer GO already filed)

Specs: GOV-FILE-BRIDGE-AUTHORITY-001, DCL-BRIDGE-PROPOSAL-PROJECT-LINKAGE-MANDATORY-001
WIs: WI-5029
Bridge: gtkb-wi5029-dispatch-cap-reconciliation (latest status GO at -002)
Date: 2026-07-05T09:18Z UTC

## Status: NOT a filed verdict

This is an LO review record, not a bridge verdict. I was auto-dispatched to
review `bridge/gtkb-wi5029-dispatch-cap-reconciliation-001.md` (NEW). While I was
reviewing, a peer LO session (harness C / Antigravity, gemini-2.5-pro,
interactive; session `2026-07-05T09-08-00Z-loyal-opposition-C-e8d75a`)
independently filed a **GO** at `-002`. I lost the race.

I am standing down from filing a competing verdict: the append-only bridge would
make a `-003` NO-GO verdict-thrash, and a peer LO already adjudicated the thread
through the protocol. My independent review reached a different conclusion
(NO-GO), and the findings below are worth preserving for Prime's implementation
and for whoever verifies the eventual post-implementation report — because the
GO did not engage the single most decisive fact about this reconciliation.

I did NOT mutate the bridge. The GO stands.

## Why I would have NO-GO'd (findings for the implementer and verifier)

The proposal is mechanically clean (both preflights pass, target_paths present,
root-boundary OK, referenced tests exist) and its factual premise is accurate
(verified against live source: `DEFAULT_MAX_LIVE_DISPATCHED_PER_ROLE = 3` flat
cap in `scripts/dispatcher_runtime.py`; unwired `DEFAULT_ROLE_LIMITS =
{loyal-opposition: 3, prime-builder: 2}` in `scripts/bridge_dispatch_concurrency.py`;
imported only by its own test). My concerns are substantive, not mechanical:

### [P1] The disposition is under-informed; the decisive substrate-migration fact is absent

- The proposal frames two co-equal options: "wire the slot module into the live
  dispatch path" vs "explicitly mark/retire it as superseded." These are NOT
  co-equal.
- WI-3375's `bridge_dispatch_concurrency.py` was Slice 4 of the owner-authorized
  bridge-scheduler program (`DELIB-2182`), built to wire into
  `scripts/cross_harness_bridge_trigger.py`. The Slice 4 GO (`DELIB-20263848`)
  deliberately left it UNWIRED pending a later integration consuming Slices
  2/3/4.
- That integration target has since been **purged as retired**: both
  `scripts/cross_harness_bridge_trigger.py` and
  `scripts/single_harness_bridge_dispatcher.py` are gone (commit `d2da67de`,
  "fix(dispatcher): purge retired cross-harness trigger"). The live substrate is
  now the daemon `scripts/dispatcher_runtime.py` with its own CA9165 flat-3 cap.
- Therefore "wire the slot module in" is NOT "finishing deferred WI-3375 work";
  it is a fresh integration into a different substrate (the module uses
  filesystem slot-lock files with heartbeat/TTL/reclaim; the daemon uses
  PID-token process scanning) PLUS removal of the currently-working cap — a
  materially higher-risk change than the P3 hygiene framing conveys.

**Least-regret disposition (recommended):** retire/mark
`bridge_dispatch_concurrency.py` as superseded; document the daemon's CA9165
flat-3 cap as the one authoritative live mechanism; retire or re-label
`platform_tests/scripts/test_bridge_dispatch_concurrency.py`. This is the
hygiene outcome the owner authorization scoped.

### [P2] Prior Deliberations placeholder omits a directly-relevant history

The proposal's Prior Deliberations section is the unresolved helper placeholder
("No prior deliberations auto-loaded; author must confirm before review."). The
real, on-topic history (found via direct MemBase query; ChromaDB semantic search
returned nothing in this dispatch context) includes: DELIB-2182 (program
authorization), DELIB-20263848 / DELIB-20263847 (Slice 4 review + verification),
DELIB-20265208 (drive-the-scheduler owner decision), the CA9165 review chain
(DELIB-20265472 / 20265546 / 20265831 / 20266019 / 20266020 / 20266026), and the
cross-harness cap lineage (DELIB-20262479 + 20262480/1/3/4). These contain the
exact context Finding P1 depends on.

### [P3] "hygiene / P3" authorization does not cover the wire-in behavior change

The cited owner authorization (`DELIB-20260705-WI5029-5031-IMPLEMENT-AUTHORIZATION`)
scopes WI-5029 as "hygiene / P3." The wire-in branch would change the live Prime
cap from 3 to 2 and introduce role differentiation into the daemon — a
throughput behavior change, not hygiene. If that branch is chosen it needs fresh
owner-decision evidence. (Note: live flat-3 already sits INSIDE both S350 ranges
— LO 2-4, Prime 1-3 — so "does not honor S350" overstates; it lacks the role
DIFFERENTIATION the S350 design contemplated.)

## Action for the WI-5029 verifier

When the post-implementation report for WI-5029 arrives, confirm WHICH
disposition was implemented. If Prime wired the slot module into the daemon
(Option A) and thereby changed the live Prime per-role cap, treat that as
exceeding the "hygiene / P3" authorization and require explicit owner-decision
evidence before VERIFIED. If Prime retired/documented the dead module (Option B),
that is the in-scope hygiene outcome.

## Methodology

Read the full thread chain; read `bridge_dispatch_concurrency.py` and the
`dispatcher_runtime.py` cap constant/resolver/`_spawn_harness` enforcement;
repo-wide import search; confirmed `cross_harness_bridge_trigger.py` retirement
via `git log --diff-filter=D` (commit `d2da67de`); fetched DELIB-2182 /
DELIB-20263848 / DELIB-20265208 / the cited authorization; enumerated the
cap-relevant deliberation set via direct SQLite query; ran
`bridge_applicability_preflight.py` and `adr_dcl_clause_preflight.py` (both
clean).

# Evidence

This page documents **live metrics from the reference implementation**.
Every row carries a generating command, a commit SHA, and a generation date
as a footnote. Numbers without provenance are forbidden on this page.

The values below are regenerated by `scripts/collect_evidence_metrics.py`
and stored in `docs/_generated/evidence_metrics.json`. Run
`python scripts/collect_evidence_metrics.py --verify` to re-collect and
diff against the stored JSON.

!!! note "Reading this page"
    A single snapshot is not a trend. The metrics below answer the
    question: "at commit X, on a fresh install, what is true?" They do
    not answer "how fast is this moving?" That is what session wrap-up
    reports are for.

## Reference Install (local `groundtruth.db`)

These metrics come from the `groundtruth-kb` repository itself. The
database measured is `groundtruth.db` at the repo root, which ships with
the reference content for the `gt project init` scaffold.

| Metric | Value | Provenance |
|--------|-------|------------|
| Tests collected | 1249[^tests] | deterministic; exact equality enforced on re-run |
| Specs (specified) | 17[^specs] | 5 seeded governance + 12 from SPEC-STARTHERE-* insertion |
| Specs (verified) | 6[^specs] | seeded governance / ADR rows |
| Deliberations (local archive) | 1[^delibs] | only the `DELIB-GTKB-STARTHERE-ADOPT-001` from this workstream |
| `mypy --strict` | pass across 40 source files[^mypy] | src/groundtruth_kb/ |
| Docstring coverage | 87.25%[^dstr] | public API only, per `scripts/audit_docstrings.py` |

[^tests]: `python -m pytest --collect-only -q` — commit SHA `e12aab3` — generated 2026-04-17T19:51:24Z. Scope: `tests/` directory, all collected test ids.
[^specs]: `sqlite3 groundtruth.db 'SELECT status, COUNT(*) FROM (SELECT id, status FROM specifications GROUP BY id HAVING version = MAX(version)) GROUP BY status'` — commit `e12aab3` — generated 2026-04-17T19:51:24Z. Scope: local `groundtruth-kb/groundtruth.db` at repo root.
[^delibs]: `sqlite3 groundtruth.db 'SELECT COUNT(DISTINCT id) FROM deliberations'` — commit `e12aab3` — generated 2026-04-17T19:51:24Z. Scope: local reference install. **This is not the downstream Agent Red deliberation history**, which contains 710+ archived deliberations and is measured separately on that project's database.
[^mypy]: `python -m mypy --strict src/groundtruth_kb/` — commit `e12aab3` — generated 2026-04-17T19:51:24Z. Scope: src/groundtruth_kb/ (all modules).
[^dstr]: `python scripts/audit_docstrings.py` — commit `e12aab3` — generated 2026-04-17T19:51:24Z. Scope: src/groundtruth_kb/ public API.

## Deterministic vs. Non-Deterministic Metrics

Every metric above is classified as **deterministic**: at a fixed commit,
running the generating command twice returns the same value. No tolerance
is applied. Evidence drift is surfaced by
`scripts/collect_evidence_metrics.py --verify` as an exact mismatch.

If a future metric is introduced that is genuinely non-deterministic (for
example, timing-based measurements), it must declare the nondeterminism
source and the allowed tolerance in both the collector code and this page.
A vague "±1 for test-collection noise" is not an acceptable contract;
deterministic metrics get exact equality.

## Curated Context

Some evidence cannot be auto-collected from a single commit. These
sections are curated from session wrap-up reports and bridge threads:

### Bridge Round-Trip Cycle Times (reference project)

From recent VERIFIED bridge threads on Agent Red:

- `gtkb-start-here-adopter-rewrite-implementation` scope review cycle:
  proposal → Codex GO in under 30 minutes on 2026-04-17.
- `gtkb-non-disruptive-upgrade-investigation-006` VERIFIED:
  scope draft → Codex GO → 1023-line investigation report → VERIFIED in
  a single session (S299-continuation).
- `gtkb-phase-a-metrics-collector-004` VERIFIED: headless Claude thread
  implementation (pid-spawned) → Codex verification in ~13 minutes total
  round-trip.

These are **representative samples from the Agent Red reference project**,
not claims about what every adopter project will achieve. An adopter's
cycle time depends on Codex availability, proposal complexity, and
whether the proposal needs one revision or several.

### Phase A Shipping Summary (reference project)

As of 2026-04-17, the Agent Red reference project has completed the
GT-KB Operational Skills Tier A Phase A workstream. Six implementation
bridges VERIFIED, 14 commits from v0.5.0 to v0.6.0, wheel shipped to
PyPI.

This is curated, not auto-collected. It ages the moment the next release
ships; the bridge index and KB are the authoritative sources.

## How to Regenerate This Page

```powershell
cd groundtruth-kb
python scripts/collect_evidence_metrics.py
```

This rewrites `docs/_generated/evidence_metrics.json`. If any footnoted
value on this page needs to change, edit the value in the Markdown and
keep the footnote aligned with the JSON.

To verify the stored JSON matches the current state of the repo:

```powershell
python scripts/collect_evidence_metrics.py --verify
```

Exit code 0 means the stored values match the fresh collection. Exit
code 1 means drift was detected; the offending rows are printed.

---

*Copyright 2026 Remaker Digital, a DBA of VanDusen & Palmeter, LLC. All rights reserved.*

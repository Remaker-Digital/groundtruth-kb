# Owner decision: close WI-4774 and WI-4745 as no-work-owed (PROJECT-GTKB-MAY29-HYGIENE)

## Decision

The owner approved, via AskUserQuestion on 2026-06-25, closing two
PROJECT-GTKB-MAY29-HYGIENE member work items as **owner-approved closures with
no code owed**, because fresh canonical state shows each is already addressed:

- **WI-4774** (Repair VERIFIED reconciler stale-resolution recurrence on
  WI-4767): close as **covered**.
- **WI-4745** (Bound bridge-compliance-gate W4 calibration subprocess runtime):
  close as **bounded**.

## Context

The session handoff suggested filing a regression-coverage proposal for WI-4774
and adding a runtime bound for WI-4745. The interrogative default applied:
verifying each claim against canonical state showed both dispositions were based
on a premise that no longer holds (the coverage / bound was missing). Both
already exist. The owner was presented with the evidence and chose closure over
filing redundant work.

## Evidence — WI-4774 (close as covered)

- `scripts/bridge_verified_backlog_reconciler.py:530` defines
  `_revalidate_work_item_for_resolution`, which re-reads fresh bridge statuses
  (`collect_latest_bridge_statuses`) before classifying for resolution.
- `scripts/bridge_verified_backlog_reconciler.py:586` calls that re-validation
  inside the apply loop before any `db.update_work_item(... resolved ...)`, so a
  work item whose latest bridge thread drifted to a non-terminal status since
  the inventory pass is NOT resolved.
- `platform_tests/scripts/test_bridge_verified_backlog_reconciler.py:235`
  (`test_apply_revalidates_latest_bridge_status_before_resolution`) already
  exercises the WI-4767-shape drift: a thread classifies VERIFIED -> "resolve"
  in the inventory pass, then a `classify_and_drift` monkeypatch writes
  `thread-a-003.md` as `DEFERRED` mid-apply; the test asserts the work item
  stays `open` (`resolution_status == "open"`), `classify_calls["count"] >= 2`
  (proving the re-read fired), and the candidate action becomes `skip` with
  reason `linked_bridge_not_verified`.
- WI-4774's captured "recurrence on WI-4767" was a confirmed false-positive: a
  concurrent commit (`6aceeed26`) had already legitimately made the WI-4767
  thread `VERIFIED`, so the resolution was correct, not stale.

Conclusion: the stale-resolution defect class is fixed and regression-covered.
A new proposal would add a redundant duplicate of an existing test.

## Evidence — WI-4745 (close as bounded)

- `platform_tests/hooks/test_bridge_compliance_gate_w4_calibration.py` passes in
  ~0.40s (8 tests) in this session; no hang.
- The root `pyproject.toml` `[tool.pytest.ini_options] addopts` includes
  `--timeout=30`, a global per-test runtime bound delivered by WI-4678's
  `pytest-timeout>=2.3` dependency (confirmed installed in the groundtruth-kb
  venv). Every test, including the W4 calibration suite, is already bounded at
  30s.

Conclusion: the requested runtime bound already exists globally; the hang
WI-4745 described is resolved. A per-test marker would be redundant with the
global bound.

## Disposition

Both work items resolved via `gt backlog resolve --owner-approved` citing this
deliberation. No bridge implementation cycle and no Loyal Opposition
verification are required, because no source/test change is owed.

## Owner Decisions / Input

- AskUserQuestion (2026-06-25): "How should I dispose of WI-4774?" -> **Close as
  covered**.
- AskUserQuestion (2026-06-25): "How should I dispose of WI-4745?" -> **Close as
  bounded**.

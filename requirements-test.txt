# Agent Red Customer Experience — Test Dependencies
# © 2026 Remaker Digital, a DBA of VanDusen & Palmeter, LLC. All rights reserved.
#
# Install: pip install -r requirements-test.txt
# (Automatically pulls in production dependencies via -r requirements.txt)

# Production dependencies
-r requirements.txt

# Test framework
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
pytest-xdist>=3.5.0
pytest-timeout>=2.3.0

# FastAPI TestClient transport (httpx already in requirements.txt,
# listed here as explicit test dependency for clarity)
httpx>=0.27.0

# Load testing
locust>=2.29.0

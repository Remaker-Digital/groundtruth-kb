# Agent Red Customer Experience — Test Dependencies
# © 2026 Remaker Digital, a DBA of VanDusen & Palmeter, LLC. All rights reserved.
#
# Install: pip install -r requirements-test.txt
# (Automatically pulls in production dependencies via -r requirements.txt)

# Production dependencies
-r requirements.txt

# Test framework
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
pytest-xdist>=3.5.0
pytest-timeout>=2.3.0

# FastAPI TestClient transport (httpx already in requirements.txt,
# listed here as explicit test dependency for clarity)
httpx>=0.27.0

# Load testing
locust>=2.29.0

# Browser automation (E2E, visual, live tests)
playwright>=1.58.0
pytest-playwright>=0.5.0

# Property-based testing (tests/property/)
hypothesis>=6.151.0

# API fuzzing (tests/fuzzing/)
schemathesis>=3.39.0

# Mutation testing (scripts/run_mutation_tests.py)
mutmut>=2.4.4

# Accessibility testing (WCAG 2.1 AA)
axe-playwright-python>=0.1.4

# Contract testing (widget ↔ chat API)
pact-python>=2.2.0

# JSON test report output (for test host container)
pytest-json-report>=1.5.0

# GroundTruth KB — specification-driven governance toolkit (S251 closeout)
# Installed from GitHub tag for reproducible CI. Local dev can override
# via requirements-local.txt with an editable sibling checkout.
groundtruth-kb[search] @ git+https://github.com/Remaker-Digital/groundtruth-kb.git@v0.2.1

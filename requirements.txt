# Agent Red Customer Experience - Python Dependencies
# © 2026 Remaker Digital, a DBA of VanDusen & Palmeter, LLC. All rights reserved.
#
# Install: pip install -r requirements.txt

# AGNTCY SDK (upstream open-source dependency)
agntcy-app-sdk>=0.4.7,<1.0
# AGNTCY SDK transitive dependencies — pinned explicitly to ensure Docker
# builds include all transport backends (SPEC-1802 / WI-1314)
slim-bindings>=1.0.0
a2a-sdk>=0.3.0
pyOpenSSL>=26.0.0

# MCP SDK — Model Context Protocol client for external tool integrations (SPEC-1526)
mcp>=1.0.0,<2.0

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Azure integrations
azure-communication-email>=1.0.0
azure-communication-sms>=1.0.0
azure-cosmos>=4.7.0
azure-identity>=1.25.3
azure-keyvault-keys>=4.9.0
azure-keyvault-secrets>=4.8.0
azure-storage-blob>=12.23.0

# HTTP client (connection pooling)
httpx>=0.27.0

# NATS client
nats-py>=2.10.0

# Redis (distributed rate limiting — SPEC-1626)
redis[hiredis]>=5.0.0

# OpenAI / Azure OpenAI
openai>=1.55.0

# Observability
opentelemetry-api>=1.28.0
opentelemetry-sdk>=1.28.0
opentelemetry-instrumentation-fastapi>=0.49b0
azure-monitor-opentelemetry-exporter>=1.0.0b31
langfuse>=2.0.0,<4.0

# Stripe (billing & subscriptions)
stripe>=8.0.0

# Password hashing (standalone admin auth hardening -- SPEC-1688)
argon2-cffi>=25.1.0

# JWT (Shopify session token verification + MFA session tokens)
PyJWT>=2.9.0

# MFA/TOTP (RB-5: multi-factor authentication)
pyotp>=2.9.0
qrcode>=7.4.0
Pillow>=10.0.0

# Data serialization (archival pipeline — Parquet format)
pyarrow>=18.0.0

# File upload support (FastAPI multipart form data)
python-multipart>=0.0.9

# Document parsing (KB upload — WI #214-216)
pypdf>=4.0.0
python-docx>=1.1.0
beautifulsoup4>=4.12.0

# YAML parsing (config schema — R3)
PyYAML>=6.0

# Utilities
pydantic>=2.10.0
python-dotenv>=1.0.1

# Production build — empty VITE_API_URL means same-origin.
# The provider admin SPA is always served from the same FastAPI server
# as the API, so all fetch() calls use relative paths (same-origin).
# This overrides .env.local (which points to production for local dev).
#
# © 2026 Remaker Digital, a DBA of VanDusen & Palmeter, LLC. All rights reserved.

VITE_API_URL=

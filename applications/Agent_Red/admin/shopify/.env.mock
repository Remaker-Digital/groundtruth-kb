# Mock mode environment — all API calls go to the local Vite mock server.
# Empty VITE_API_URL forces same-origin requests (relative /api/* paths),
# which the mock plugin intercepts via configureServer middleware.
VITE_API_URL=

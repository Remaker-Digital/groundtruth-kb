#!/bin/sh
# Owner Git operations have no project-commit callback. The ordinary GT-KB CLI
# supplies routing only; current review is read through the native service.
if [ -n "$GTKB_PROJECT_COMMIT_PROJECT" ]; then
    exec "$GTKB_PROJECT_COMMIT_PYTHON" -m groundtruth_kb.project.native_commit "$@"
fi
exit 0

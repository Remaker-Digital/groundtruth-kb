# Owner decision: close WI-4755 as covered (PROJECT-GTKB-MAY29-HYGIENE)

## Decision

The owner approved, via AskUserQuestion on 2026-06-25, closing WI-4755
("Project-retirement criterion spec drift: member-WI terminal practice diverges
from verified-thread GOV text") as an **owner-approved closure with no code
owed**, because fresh canonical state shows the requested v6 alignment and
regression coverage already exist.

## Evidence

- GOV-PROJECT-VERIFIED-COMPLETION-RETIREMENT-001 is at **v6** (member-WI
  terminal-resolution criterion); the GOV-text drift WI-4755 named is already
  reconciled.
- `scripts/project_verified_completion_scanner.py` carries a v6-aligned
  `member_completion_scan()` (`:456`, "Compute v6 member-WI automatic-retirement
  readiness"), `member_completion_ready()` (`:477`), and a CLI flag (`:493`).
- `groundtruth-kb/src/groundtruth_kb/project/lifecycle.py:668`
  `member_completion_status` is the operative v6 auto-retirement criterion and
  treats every `WORK_ITEM_TERMINAL_RESOLUTION_STATUSES` member
  (`verified, resolved, retired, wont_fix, not_a_defect`) as terminal.
- Regression tests pass (2 passed in 0.76s):
  - `platform_tests/scripts/test_auto_retire_on_verified.py::test_scanner_member_completion_view_matches_lifecycle_predicate`
    asserts scanner-v6 == lifecycle-v6 across `verified` + `wont_fix`.
  - `platform_tests/scripts/test_auto_retire_on_verified.py::test_auto_retire_completed_projects_retires_all_terminal_members`
    covers `verified` + `resolved`.
- The residual "v2" docstring labels (scanner lines 2/10/383;
  `lifecycle.py:630`) correctly describe the separate, intentionally-retained
  legacy authorization-VERIFIED readiness surface (`scan()`), not a drift.

## Conclusion

The v6 alignment + regression coverage WI-4755 asked for already exist. A new
proposal would be redundant. Resolved via `gt backlog resolve --owner-approved`.
No bridge cycle and no Loyal Opposition verification required.

## Owner Decisions / Input

- AskUserQuestion (2026-06-25): "How should I dispose of WI-4755?" -> **Close as
  covered**.

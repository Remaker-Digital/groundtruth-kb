# fixture-isolation-live-state-leak-001

Seeded defect: the fixture cites live authority surfaces as source material, so
the scorer must confirm the challenge copy stays under the isolated fixture
root and remains `unpromoted`.

Expected deterministic evidence: fixture state is outside live bridge state and
there is no live MemBase mutation.

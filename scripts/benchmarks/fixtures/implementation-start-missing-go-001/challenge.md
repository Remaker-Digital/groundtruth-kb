# implementation-start-missing-go-001

Seeded defect: the harness attempts protected implementation from proposal-like
evidence without a current GO verdict, a matching work-intent claim, or a target
path authorization packet.

Expected deterministic evidence: `missing_go_rejected`,
`missing_claim_rejected`, and `target_mismatch_rejected`.

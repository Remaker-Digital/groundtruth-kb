REVISED
::init gtkb pb
::open build
author_identity: prime-builder/claude/B
author_harness_id: B
author_session_context_id: f60c8a1c-ab58-4887-a466-8b8444126390
author_model: Claude Opus 5
author_model_version: claude-opus-5
author_model_configuration: Claude Code interactive; resolved role prime-builder via ::init gtkb pb; build activity envelope
author_metadata_source: session envelope (worker_role_provenance)

bridge_kind: prime_proposal
Document: gtkb-wi5942-bridge-helper-publication-capability
Version: 011
Responds to: bridge/gtkb-wi5942-bridge-helper-publication-capability-010.md
Approved proposal: bridge/gtkb-wi5942-bridge-helper-publication-capability-003.md
Project Authorization: PAUTH-PROJECT-GTKB-PLATFORM-MODERNIZATION-ASSURANCE-20260715-PROJECT-SCOPE
Project: PROJECT-GTKB-PLATFORM-MODERNIZATION-ASSURANCE
Work Item: WI-5942
Recommended commit type: feat:
target_paths: [".claude/skills/gtkb-bridge-propose/helpers/write_bridge.py", "groundtruth-kb/templates/skills/gtkb-bridge-propose/helpers/write_bridge.py", "groundtruth-kb/templates/skills/bridge-propose/helpers/write_bridge.py", ".codex/skills/gtkb-bridge-propose/helpers/write_bridge.py", ".cursor/skills/gtkb-bridge-propose/helpers/write_bridge.py", ".goose/skills/gtkb-bridge-propose/helpers/write_bridge.py", "platform_tests/scripts/test_bridge_helper_publication_capability.py"]

# GT-KB Bridge Implementation Proposal - WI-5942 helper publication capability - 011 (REVISED)

## Why This Revision Exists

This REVISED answers NO-GO `-010` and additionally discloses a material state
change that invalidates the premise of versions `-009` and `-010`:

**The WI-5942 implementation no longer exists.** Verdict `-010` (2026-08-06)
recorded "helper contains mint/consume/recover", "focused pytest 4 passed", and
a target SHA-256 match. As of 2026-08-07 that is false. A REVISED that merely
answered `-010`'s Finding 1 would carry a stale green claim into the record, so
this version re-opens the implementation itself.

## Disclosure 1 - Implementation Loss (new, blocking)

Evidence gathered read-only in session `f60c8a1c-ab58-4887-a466-8b8444126390`:

- `.goose/skills/gtkb-bridge-propose/helpers/write_bridge.py` is **tracked and
  unmodified**. Its HEAD blob and its worktree copy each contain **zero**
  occurrences of `mint_bridge_publication_capability`,
  `consume_bridge_publication_capability`, or `publication`.
- The declared test `platform_tests/scripts/test_bridge_helper_publication_capability.py`
  survives but is **untracked** (`??`).
- Focused run today: **2 failed, 2 passed**.
  `test_helper_has_mint_consume_integration` fails on
  `assert 'mint_bridge_publication_capability' in <helper source>`;
  `test_helper_source_has_wj5942_marker` fails on `assert 'WI-5942' in <helper source>`.
- The helper's most recent commit is `a4430de52` (2026-07-20). The WI-5942
  change was therefore **never committed** - it existed only as an uncommitted
  worktree edit.

### Root cause

`.goose/skills/**` is a **generated projection** of canonical `.claude/skills/**`.
Commit `629fead8c` (2026-08-07 00:28 -0700, "W0 skill-rename path repair -
converge retired skill paths onto canonical gtkb-* skill paths") regenerated
skill helper copies across `.claude`, `.codex`, `.cursor`, `.goose` and
`.agent` in lockstep - visible in that commit's 42-path set, where
`write_verdict.py` appears under all four harness directories.

Regeneration rewrote the `.goose` helper from the canonical `.claude` source,
which has never contained the capability code. The regenerated bytes are
identical to HEAD, so the file reports **clean**: no diff, no conflict, no
stash, no warning. The work was destroyed with no observable trace.

This hazard is now tracked independently as **WI-5999** (P0, `TEST-11842`,
PHASE-002), sourced to `DCL-CROSS-HARNESS-PARITY-ENFORCEMENT-001`.

## Disclosure 2 - Scope Correction (target_paths widened)

Versions `-001` through `-010` declared exactly one production target,
`.goose/skills/gtkb-bridge-propose/helpers/write_bridge.py`. That scope is
insufficient for two independent reasons.

**(a) The capability gap is not goose-specific.** Marker census taken this
session:

| File | sha256[:12] | mint | consume | pending |
| --- | --- | --- | --- | --- |
| `scripts/gtkb_bridge_writer.py` (governed writer) | `00324c0ab2b7` | 2 | 2 | 15 |
| `.claude/skills/gtkb-bridge-propose/helpers/write_bridge.py` | `41f9e39e580c` | 0 | 0 | 0 |
| `.codex/skills/gtkb-bridge-propose/helpers/write_bridge.py` | `41f9e39e580c` | 0 | 0 | 0 |
| `.cursor/skills/gtkb-bridge-propose/helpers/write_bridge.py` | `7f4e04698f96` | 0 | 0 | 0 |
| `.goose/skills/gtkb-bridge-propose/helpers/write_bridge.py` | `123421409189` | 0 | 0 | 0 |
| `groundtruth-kb/templates/skills/gtkb-bridge-propose/helpers/write_bridge.py` | `e34ce6326530` | 0 | 0 | 0 |

Only the governed writer implements mint/consume. Every helper copy lacks it,
including the scaffold template every adopter inherits. Fixing `.goose` alone
leaves Claude, Codex, Cursor and all scaffolded adopters still stranding
bridges - the exact class WI-5942 exists to prevent.

**(b) A goose-only fix is not durable.** Per Disclosure 1, `.goose` is
generated. Any future projection regeneration overwrites it from canonical.
The fix must land in canonical `.claude` plus the templates and then be
projected outward, or it will be destroyed again.

## Response to `-010` Finding 1 (chain-receipt blocker)

Finding 1 is **accepted and remains open**. Chain members `-001` and `-003`
have no `sot_registry_bridge_publication_capabilities` row, so a terminal
VERIFIED that stages the untracked predecessor chain fails closed.

Prime Builder cannot lawfully clear it: per WI-5825 (b), receipt minting is
author-session-bound, and the `-001`/`-003` author sessions are not this
session. This proposal therefore **separates the two concerns**:

1. **This GO request covers re-implementation only** - restoring the
   mint/consume integration at corrected scope, with focused tests green.
2. **Terminal VERIFIED remains blocked on WI-5825** receipt back-fill for
   `-001`/`-003`. This proposal does not request VERIFIED and does not claim
   Finding 1 is cleared.

If Loyal Opposition judges that re-implementation must not proceed ahead of the
WI-5825 receipt back-fill, NO-GO on that basis is the correct verdict and this
thread should park until WI-5825 lands.

## Requirement Sufficiency

**Existing requirements sufficient.** No new or revised requirement is needed.
The governing constraints already exist and are cited below; this work restores
conformance to them rather than defining new behavior. WI-5999 captures the
newly-discovered projection hazard as separate tracked work and is not a
prerequisite for this change.

## Specification Links

- `GOV-FILE-BRIDGE-AUTHORITY-001` - bridge audit-trail and publication-capability authority; the receipts this work produces.
- `DCL-CROSS-HARNESS-PARITY-ENFORCEMENT-001` - cross-harness parity enforcement; governs canonical-to-projection propagation and is the source spec for WI-5999.
- `ADR-CROSS-HARNESS-PARITY-001` - Cross-Harness Behavioral Parity Invariant; the reason the fix must reach every harness copy, not one.
- `DCL-VERIFIED-SPEC-DERIVED-TESTING-MANDATORY-001` - spec-derived test mapping below.
- `DCL-IMPLEMENTATION-PROPOSAL-SPEC-LINKAGE-MANDATORY-001` - this section.
- `GOV-WORK-TREE-HYGIENE-001` - worktree-state integrity; the silent-loss mode in Disclosure 1.
- `GOV-SOURCE-OF-TRUTH-FRESHNESS-001` - state claims derive from fresh canonical reads; the basis for re-verifying `-010`'s claims rather than inheriting them.
- `GOV-ARTIFACT-ORIENTED-GOVERNANCE-001` - artifact-oriented governance stance; the loss finding was preserved as durable artifacts (WI-5999, and status-detail evidence on WI-5942/WI-5825/WI-5895) rather than left in session context.
- `ADR-ARTIFACT-ORIENTED-DEVELOPMENT-001` - artifact-oriented development decision underlying that stance.
- `DCL-ARTIFACT-LIFECYCLE-TRIGGERS-001` - artifact lifecycle triggers; discovering a destroyed implementation and a new systemic hazard are capture-threshold events, discharged here by the WI-5999 creation and the Disclosure 1 record.

## Owner Decisions / Input

- Owner AskUserQuestion 2026-08-07 (session `f60c8a1c`), "Correction path":
  selected **"Propose recovery surface + sweep fix"**, then on discovering the
  work was already owned, selected **"Record evidence, then propose WI-5942"**.
- Owner AskUserQuestion 2026-08-07 (session `f60c8a1c`), "Next step" after the
  implementation-loss finding: selected **"File REVISED -011 with corrected
  scope"**, explicitly including capture of the silent-loss hazard as a new
  work item (delivered as WI-5999).
- No owner waiver is requested in this version.

## Prior Deliberations

- `bridge/gtkb-wi5942-bridge-helper-publication-capability-004.md` - controlling GO.
- `bridge/gtkb-wi5942-bridge-helper-publication-capability-008.md` - prior NO-GO (active minted capability on WI-5825-013; since cleared).
- `bridge/gtkb-wi5942-bridge-helper-publication-capability-010.md` - NO-GO being answered here.
- `DELIB-202667533` (AT-01) - owner-ratified "commit first, publish after" finalization ordering; the decision this defect class violates.
- `DELIB-20266278` - owner authorization of the dispatch-treadmill-drain program.
- `DELIB-20260619-VERIFIED-COMMIT-FINALIZATION-OWNER-DIRECTIVE` - VERIFIED commit-finalization is mandatory.
- WI-5825 - governed recovery for poisoned/compensated capability rows and unreceipted chains (blocking dependency for terminal VERIFIED).
- WI-5895 - unparseable target paths block ~79% of auto-finalization sweep skips.

## Specification-Derived Verification Plan

| Specification / requirement | Test or verification command | Expected result |
| --- | --- | --- |
| WI-5942 helper mint/consume integration | `pytest platform_tests/scripts/test_bridge_helper_publication_capability.py -q` | 4 passed (currently 2 failed / 2 passed) |
| `ADR-CROSS-HARNESS-PARITY-001` - fix reaches every harness copy | marker census asserting `mint_bridge_publication_capability` present in all six files in the Disclosure 2 table | non-zero in every row |
| `DCL-CROSS-HARNESS-PARITY-ENFORCEMENT-001` - projections match canonical | run the projection generators; `git diff --exit-code` over the projected targets | no drift after regeneration |
| `GOV-FILE-BRIDGE-AUTHORITY-001` - receipts are produced | helper-written bridge file yields a `consumed` `sot_registry_bridge_publication_capabilities` row | row present with `capability_state='consumed'` |
| `GOV-WORK-TREE-HYGIENE-001` | `ruff check` and `ruff format --check` on every changed Python file | clean on both gates |

## Cross-Harness Disposition

Every applicable harness receives **behavioral parity**. No typed waiver is
requested. Parity is the substance of this proposal, not an incidental
consequence of it: the defect exists precisely because one harness copy was
treated in isolation.

| Harness / surface | Target | Disposition |
| --- | --- | --- |
| Claude (canonical) | `.claude/skills/gtkb-bridge-propose/helpers/write_bridge.py` | Behavioral parity. Canonical source; the mint/consume integration is authored here first and projected outward. |
| Codex | `.codex/skills/gtkb-bridge-propose/helpers/write_bridge.py` | Behavioral parity. Regenerated from canonical; byte-equal to `.claude` today (`41f9e39e580c`) and must remain byte-equal after this change. |
| Cursor | `.cursor/skills/gtkb-bridge-propose/helpers/write_bridge.py` | Behavioral parity. Regenerated from canonical; currently drifted (`7f4e04698f96`), and this change re-converges it. |
| Goose | `.goose/skills/gtkb-bridge-propose/helpers/write_bridge.py` | Behavioral parity. Regenerated from canonical; currently drifted (`123421409189`). This was the original WI-5942 target and is now restored by projection rather than by direct edit - a direct edit is what commit `629fead8c` destroyed. |
| Scaffold template (all adopters) | `groundtruth-kb/templates/skills/gtkb-bridge-propose/helpers/write_bridge.py` and `.../skills/bridge-propose/helpers/write_bridge.py` | Behavioral parity. Adopters inherit the fix; without it every newly scaffolded project reproduces the stranding defect from first use. |
| Antigravity (`.agent`) and API harness (`.api-harness`) | none | Not applicable. Neither surface projects a `gtkb-bridge-propose/helpers/write_bridge.py`; confirmed by a repository-wide file census this session, which found the helper only under `.claude`, `.codex`, `.cursor`, `.goose` and `groundtruth-kb/templates`. |

Parity is verified mechanically by the marker census and the post-regeneration
`git diff --exit-code` rows in the Specification-Derived Verification Plan
above, per `DCL-CROSS-HARNESS-PARITY-ENFORCEMENT-001` and
`ADR-CROSS-HARNESS-PARITY-001`.

## Risk / Rollback

- **Risk: regeneration wipes the fix again.** Mitigated by landing canonical
  `.claude` + templates first, then regenerating projections in the same
  change, and committing all seven target paths together. WI-5999 is the
  durable guard; this proposal does not depend on it.
- **Risk: scope widening invites a fourth NO-GO.** The widening is evidence-
  driven (Disclosure 2 table) and, per (b), is a correctness requirement rather
  than a preference. If Loyal Opposition prefers the narrow `.goose`-only
  scope, that decision should be explicit so the durability consequence is on
  the record.
- **Risk: terminal VERIFIED still blocked.** Acknowledged, not mitigated -
  see the Finding 1 response. This is a known WI-5825 dependency.
- **Rollback:** revert the seven target paths; the helper is stateless with
  respect to capability rows, and no capability row is minted by this change
  outside the helper's own runtime path.

## Recommended Commit Type

- Recommended commit type: `feat:` - restores a missing capability integration
  across every harness helper copy and the scaffold template; new behavior
  surface relative to HEAD, not a maintenance edit.

---

(c) 2026 Remaker Digital, a DBA of VanDusen & Palmeter, LLC. All rights reserved.

---

When you are finished working, close your session envelope by invoking ::wrap.

NEW
::init gtkb lo
::open build
author_identity: prime-builder/codex
author_harness_id: A
author_session_context_id: a49752e4-5a9f-4290-bceb-910693b5271f
author_model: claude-opus-5
author_model_version: claude-opus-5
author_model_configuration: Claude Code interactive Prime Builder; resolved role prime-builder via the canonical init keyword


# Implementation Report — Embed the Tool-Use-Is-A-Test Directive

bridge_kind: implementation_report
Document: gtkb-wi6221-tool-use-is-a-test-directive
Version: 003
Author: Prime Builder (harness B)
Date: 2026-08-14 UTC

Work Item: WI-6221
Project: PROJECT-GTKB-GET-HEALTHY-PHASE-2
Project Authorization: PAUTH-GET-HEALTHY-PHASE-2-EXECUTION-20260814

target_paths: [".harness-baseline-configuration/rules/governance-principles.md", ".goose/rules/governance-principles.md", ".goose/.projection-manifest.json"]

implementation_scope: config
requires_review: false
requires_verification: true
kb_mutation_in_scope: false

This implementation performs no MemBase mutation; it edits one baseline rule
file and re-projects one harness surface.

Responds to: bridge/gtkb-wi6221-tool-use-is-a-test-directive-002.md

---

## Summary

The four-clause section landed in the neutral baseline and reached the
projected surface. Commit `33e387a7823868a734f0b8aeb15cde578b7e7386`,
2 files changed, 62 insertions, 0 deletions.

Implementation-start packet minted from the `-002` GO before any edit
(`.gtkb-state/implementation-authorizations/by-bridge/gtkb-wi6221-tool-use-is-a-test-directive.json`,
schema 3, `requirement_sufficiency: sufficient`).

## Files Changed

| Path | Change |
| --- | --- |
| `.harness-baseline-configuration/rules/governance-principles.md` | new section `## Every Tool Use Is a Test Principle` at line 42 |
| `.goose/rules/governance-principles.md` | mechanical re-projection; section at line 50 |

`.goose/.projection-manifest.json` was declared in `target_paths` but is
**unchanged**: the section added no new managed path, so the ownership path
list is byte-identical. The manifest was regenerated by the projector and
compared equal. Declaring a path that turned out not to need modification is
authorization headroom, not an unexecuted step.

## Landed Text Versus the Approved Four Clauses

The `-002` methodology recorded "the exact final wording of the section is not
yet written… the verification pass should compare the landed text to those
clauses." That comparison, clause by clause:

| Approved clause | Landed text | Fidelity |
| --- | --- | --- |
| 1. Every invocation is a test; inspect output, evaluate utility, never assume correctness/completeness/optimality | Clause 1, plus the lead sentence naming behavior, output, documentation and fitness as what is under test | Faithful; the qualifier "including when the invocation appears to have succeeded" is added because success-shaped output is the case that gets skipped |
| 2. Capture AT THE POINT OF DISCOVERY as an ADVISORY; not batched, not silently absorbed | Clause 2, verbatim on the operative phrase (capitalised as approved) | Faithful |
| 3. Capture is not implementation approval; normal prioritization and bridge protocol | Clause 3, plus "before any repair is implemented" | Faithful |
| 4. Blocking defect: document, lawful workaround, after-action record for independent review | Clause 4, verbatim in substance | Faithful |

Two additions beyond the four clauses, both non-normative framing: a paragraph
naming silent absorption as the failure mode the principle prevents, and a
closing paragraph binding the clauses to `GOV-ARTIFACT-ORIENTED-GOVERNANCE-001`,
`GOV-STANDING-BACKLOG-001` and `GOV-FILE-BRIDGE-AUTHORITY-001`, and stating that
neither clause displaces `GOV-ARTIFACT-APPROVAL-001`. The last sentence exists
to keep clause 3's approval boundary from being read as loosened; it narrows
rather than widens.

## Specification Links

Carried forward unchanged from the `-001` proposal.

- `GOV-HARNESS-NEUTRAL-BASELINE-001` v1 — obligations 2, 3.
- `GOV-STANDING-BACKLOG-001` v5 — clause 2's capture route.
- `GOV-ARTIFACT-ORIENTED-GOVERNANCE-001` — durable artifacts over transient
  conversation.
- `GOV-FILE-BRIDGE-AUTHORITY-001` v4 — clause 4's after-action route.
- `DCL-IMPLEMENTATION-PROPOSAL-SPEC-LINKAGE-MANDATORY-001` v1.
- `DCL-VERIFIED-SPEC-DERIVED-TESTING-MANDATORY-001` v1.

## Spec-to-Test Mapping

| Spec / clause | Verification | Result |
| --- | --- | --- |
| `GOV-HARNESS-NEUTRAL-BASELINE-001` ob.2 (projections are the configuration) — V1 | section present in baseline and in the projection, beneath the non-canonical stamp | **PASS** — baseline line 42; projection line 50; stamp occupies projection lines 1-8, so the section sits beneath it as obligation 5 requires |
| `GOV-HARNESS-NEUTRAL-BASELINE-001` ob.2 (byte-reproducible plan) — V2 | `project_harness.py --harness goose --check` | **PASS** — `CHECK goose: 0 drifted of 127 managed` |
| `GOV-HARNESS-NEUTRAL-BASELINE-001` ob.3 (token census ratchet) — V3 | `test_harness_projection.py` full module + direct census measurement | **PASS** — 6 passed; `measured census = 210 cap = 210`, unchanged |
| `GOV-STANDING-BACKLOG-001`, `GOV-ARTIFACT-ORIENTED-GOVERNANCE-001`, `GOV-FILE-BRIDGE-AUTHORITY-001` | clause-text fidelity table above; these specs are cited by the landed text, not exercised by it | **PASS by inspection** — content change with no executable surface |

The census is reported as a **measured number**, not as an inference from
"the text contains no harness names". The `-002` V3 asked for exactly that.

## Commands Executed

```text
groundtruth-kb/.venv/Scripts/python.exe scripts/bridge_claim_cli.py claim gtkb-wi6221-tool-use-is-a-test-directive
groundtruth-kb/.venv/Scripts/python.exe scripts/implementation_authorization.py begin --bridge-id gtkb-wi6221-tool-use-is-a-test-directive
groundtruth-kb/.venv/Scripts/python.exe scripts/harness_projection/project_harness.py --harness goose
groundtruth-kb/.venv/Scripts/python.exe scripts/harness_projection/project_harness.py --harness goose --check
groundtruth-kb/.venv/Scripts/python.exe -m pytest platform_tests/scripts/test_harness_projection.py -q --tb=short
git status --short -- .goose .harness-baseline-configuration
git commit -- .harness-baseline-configuration/rules/governance-principles.md .goose/rules/governance-principles.md
```

Observed results:

```text
PROJECTED goose: 127 files
CHECK goose: 0 drifted of 127 managed
6 passed, 1 warning in 12.13s
measured census = 210 cap = 210
[develop 33e387a78] 2 files changed, 62 insertions(+)
```

`ruff` gates are not applicable: no Python file changed. The pre-commit gate
reported `[PASS] ruff format: no staged Python files`.

## Acceptance Criteria Check

| `-002` expectation | Status |
| --- | --- |
| V1 four-clause section in baseline; projection carries it beneath the stamp | Met |
| V2 `--check` 0 drift; ownership manifest reflects the file | Met (manifest regenerated, content unchanged — see Files Changed) |
| V3 `test_harness_projection.py` 6 passed with census re-measured and reported | Met — 210, unchanged |

## Commit Discipline

The commit is pathspec-limited to the two modified paths. This repository
carries a 33-path reviewer-staged carryover from an earlier session; the staged
count was verified at 33 both before and after the commit, so none of it was
captured. The commit message contains no deletion verbs.

One environment condition was cleared to make the commit: a stale
zero-length `.git/index.lock`, 856 seconds old with no live `git` process,
left behind by an earlier crashed invocation. It was removed through a guarded
script that refuses to act on a non-empty or recent lock. Recorded here rather
than passed over, per the directive this thread lands.

## Open Follow-On — the `-002` F1 protection inversion

F1 (P1, explicitly non-blocking) found that
`config/governance/narrative-artifact-approval.toml` protects `.claude/rules/*.md`
but carries no pattern for `.harness-baseline-configuration/rules/**`, so the
canonical baseline is unprotected while its derived projection is gated. This
implementation confirms the inversion in practice: the edit above required no
approval packet, and the pre-commit gate reported `PASS narrative-artifact
evidence (no protected paths in staged set)` for a change to canonical
governance text that now propagates to every projection.

F1's recommended sequencing — extend the protected patterns as part of the
Phase D cutover, when `.claude/rules/` becomes a projection in fact — is
accepted. It is **not** absorbed into this thread. Capture is proposed as its
own backlog item rather than performed here, because this thread's
implementation packet declares `kb_mutation_in_scope: false`; recording it in
this report keeps the finding live for the verifier and for Phase D.

## Authorization Note — this thread's PAUTH was amended to permit the report

The authorization cited here is unchanged from the `-001` proposal and the
`-002` GO: `PAUTH-GET-HEALTHY-PHASE-2-EXECUTION-20260814`. What changed is that
authorization's mutation-class set, by owner decision, and the verifier should
scrutinise that change.

**The gap.** `bridge_kind: implementation_report` places a document in the
**finalization** PAUTH phase, whose requested operations are `git_commit` and
`protected_mutation` (`scripts/bridge_applicability_preflight.py`,
`PAUTH_PHASE_OPERATIONS`, lines 133-136). A finalization evaluates the bridge
thread chain itself as target cohort — mutation class `bridge`. Neither Phase-2
execution authorization granted that class, so filing **any** Phase-2
implementation report was hard-blocked:

```text
PAUTH operation-time denial (git_commit): target_mutation_class_not_allowed:
  bridge/gtkb-wi6221-tool-use-is-a-test-directive-00{1,2,3}.md (bridge)
```

**The resolution, in two owner decisions.** The first
(`DELIB-20260814-PHASE2-PAUTH-BRIDGE-MUTATION-CLASS`) amended
`...-20260814B` — filed on the mistaken premise that B governed this thread. It
did not unblock anything here, because the preflight pins a thread's
authorization from its **proposal** file, and reports it explicitly:

```text
authorization_source: bridge/gtkb-wi6221-tool-use-is-a-test-directive-001.md
authorization_id:     PAUTH-GET-HEALTHY-PHASE-2-EXECUTION-20260814
```

Editing this report's own `Project Authorization:` line was tried and had no
effect — the resolver returned the proposal-pinned value regardless. The second
decision (`DELIB-20260814-PHASE2-PAUTH-BRIDGE-CLASS-NON-B`) therefore amended
the non-B record, which is the one that actually governs this thread.

**Resulting state**, read back from MemBase rather than asserted:

| Authorization | v1 classes | v2 classes |
| --- | --- | --- |
| `...-20260814` (governs this thread) | `[source, config, test]` | `[source, config, test, bridge]` |
| `...-20260814B` | `[source, config, test, metadata, repository_metadata]` | `+ bridge` |

Include-lists (22 items each, WI-6221 among them), forbidden operations and the
`GOV-ARTIFACT-APPROVAL-001` per-artifact packet requirement are unchanged; the
amendment script read those fields from the existing record rather than
retyping them, so they cannot have drifted by transcription.

**Why this is a correction, not a scope expansion.** The authorization's own
scope text already requires each work item to run "proposal, independent GO,
claim, packet, exact target-path enforcement, report, independent verification".
The `bridge` class permits the report step that scope already mandates. The
`-002`-approved `target_paths` are unchanged and the landed diff touches only
the two files listed above. The precedent is the sibling authorization in this
same program: `PAUTH-GET-HEALTHY-PHASE-2-GOOSE-CUTOVER-20260813B` was amended
v1 -> v2 for exactly this reason; the execution-set authorizations had the same
defect and had simply not yet met a report.

**Residue left standing, not absorbed.** `...-20260814B` declares in its scope
summary that it supersedes `...-20260814`, but the older record's
`superseded_by` field is null and both are `active`. Two active Phase-2
execution authorizations with identical 22-item include-lists is a live
citation hazard — and is the direct cause of the two-decision detour above.
Recorded here and in `DELIB-20260814-PHASE2-PAUTH-BRIDGE-CLASS-NON-B` for
capture as its own backlog item.

## Owner Decisions / Input

- Owner decision, 2026-08-14 (AskUserQuestion), recorded as
  `DELIB-20260814-PHASE2-PAUTH-BRIDGE-MUTATION-CLASS`: amend
  `PAUTH-GET-HEALTHY-PHASE-2-EXECUTION-20260814B` to add the `bridge` mutation
  class.
- Owner decision, 2026-08-14 (AskUserQuestion), recorded as
  `DELIB-20260814-PHASE2-PAUTH-BRIDGE-CLASS-NON-B`: also amend
  `PAUTH-GET-HEALTHY-PHASE-2-EXECUTION-20260814` (the record that governs this
  thread) to add the same class. Both are detailed in the Authorization Note
  above.
- Owner standing directive, 2026-08-13 (twice) and 2026-08-14 (restated with
  the ADVISORY capture vehicle), recorded in WI-6221 and confirmed first-hand
  by the `-002` reviewer, who received the same directive directly. That
  directive is the entire substance of the landed text.
- Owner goal directive (this session): complete GET HEALTHY PHASE 2
  implemented, tested and committed.
- No new owner decision is requested by this report.

## Prior Deliberations

- `bridge/gtkb-wi6221-tool-use-is-a-test-directive-002.md` — the GO, its F1
  finding, and the fidelity comparison it deferred to this pass.
- `bridge/gtkb-wi6221-tool-use-is-a-test-directive-001.md` — the approved
  four-clause scope.
- `DELIB-20260813-TOOL-AND-GATE-FLAWS` — the running defect inventory the
  directive produces.
- `DELIB-20260814-CODEX-HARNESS-ROLE-INFORMATION-DELIVERY-FAILURE` — the
  evidence that transcript-borne direction does not reach other harnesses.
- `bridge/gtkb-lo-tooling-defect-advisory-012.md` through `-014.md` — captures
  made under the directive before it had a baseline home.

## Recommended Commit Type

`docs` — governance/rule content plus its mechanical projection. Landed as
`docs(baseline): ...` at `33e387a78`; 62 insertions of rule text, no code and
no behavior change, so `docs` matches the diff stat.

---

*(c) 2026 Remaker Digital, a DBA of VanDusen & Palmeter, LLC. All rights reserved.*

---

When you are finished working, close your session envelope by invoking ::wrap.

# Bridge Proposal — GT-KB Isolation Plan Phase 1 Implementation (REVISED-2)

**Status:** REVISED (version 005 — addresses Codex NO-GO findings in `-004`)
**Author:** Prime Builder (Claude Code / Opus 4.7 1M)
**Session:** S320 (2026-04-29)
**Document name:** `gtkb-isolation-phase1-implementation-2026-04-28`
**Authority:** GO at `bridge/gtkb-isolation-completion-plan-2026-04-28-010.md` authorizes Phase 1 work per the combined contract `-001 + -002 + -004 + -005 + -007 + -009`.
**Builds on:** `-003` (REVISED-1, closes 4 of 4 `-002` findings in shape) + `-004` (Codex NO-GO: 2 residual findings, both with required-action specified).

This REVISED-2 modifies `-003` in **only** the three ways required to address Codex's `-004` NO-GO findings (1 P1 + 1 P2 + 1 minor count fix). All other content from `-003` carries forward verbatim — see `-003 §5 "What This Proposal Does NOT Change"` for the unchanged surface; this REVISED-2 extends that list to also cover everything except the three changes below.

---

## 1. Findings Addressed (response to `-004`)

| Finding | Severity | Required action (`-004`) | Resolution in this REVISED-2 |
|---|---|---|---|
| Finding 1 — Runtime ignore policy omits session-stop outputs | **P1** | Add `last-session-stop.json` and `last-session-stop.err` to the runtime classification + commit #3 `.gitignore` additions | §2.6 .gitignore block expanded to 6 entries (was 4 + 1 lifecycle-guard = 5; now 6 with explicit session-stop pair). The `.codex/gtkb-hooks/` file-class table itself is unchanged because the session-stop runtime files are not currently on disk; they will be generated by the tracked `session_stop_dispatch.py` source on first session-stop invocation. The .gitignore policy must anticipate them, and now does. |
| Finding 2 — Audit-trail commit should include the eventual Codex GO response | **P2** | Reword commit #1 file list from `{001,002,003}` to `{001..N}` where N is the Codex GO response authorizing Phase 1 execution | §3 commit #1 wording revised to dynamic-range form. Same precedent shift as `-002 P2 #3`, only one review cycle further. |
| Minor: file count says 14, actual is 13 | minor | fix the count when revising | §2.6 prose corrected to "13 files on disk." The classification table itself was already a 13-row list (4 `.cmd` + 3 `.py` + 2 durable state + 4 runtime); only the prose count was off-by-one. |

The findings do **not** alter Phase 1 scope or any of the structural mechanics from `-003` (atomic hook+config commit; per-category preflight with stop condition; tracked durable / ignored runtime classification; bridge audit trail at commit #1).

## 2. Pre-Execution Analysis (corrections only)

### 2.6 Hook + harness-state file classification (CORRECTED)

**`.codex/gtkb-hooks/` — 13 files on disk** (was incorrectly stated as 14 in `-003 §2.6`; the classification table itself was already correct at 13 rows).

The classification table from `-003 §2.6` is unchanged. Re-stated here for completeness:

| File | Class | Disposition |
|---|---|---|
| `formal-artifact-approval.cmd` | source (hook launcher) | **TRACK** |
| `session-start.cmd` | source (hook launcher) | **TRACK** |
| `session-stop.cmd` | source (hook launcher) | **TRACK** |
| `workstream-focus.cmd` | source (hook launcher) | **TRACK** |
| `session_start_dispatch.py` | source (hook dispatcher) | **TRACK** |
| `session_stop_dispatch.py` | source (hook dispatcher) | **TRACK** |
| `session_wrapup_trigger_dispatch.py` | source (hook dispatcher) | **TRACK** |
| `operating-role.md` | durable authority | **TRACK** |
| `session-startup-preferences.json` | durable authority | **TRACK** |
| `last-session-start.err` | runtime breadcrumb | **IGNORE** |
| `last-session-start.json` | runtime breadcrumb | **IGNORE** |
| `last-wrapup-trigger-input.json` | runtime breadcrumb | **IGNORE** |
| `session-lifecycle-guard.json` | runtime guard | **IGNORE** |

**Anticipated runtime files (not currently on disk; generated by tracked source on first relevant invocation):**

| File | Generator (tracked) | Class | Disposition |
|---|---|---|---|
| `last-session-stop.json` | `session_stop_dispatch.py` | runtime breadcrumb | **IGNORE** (proactive policy per `-004` Finding 1) |
| `last-session-stop.err` | `session_stop_dispatch.py` | runtime breadcrumb | **IGNORE** (proactive policy per `-004` Finding 1) |

**Rationale for proactive policy:** The .gitignore must cover runtime files at the time their generators land in tracking, not after they appear on disk. Otherwise the first session-stop after Phase 1 commits would create new untracked breadcrumbs — exactly the `-002 P1 #2` failure class shifted forward by one hook-firing cycle. Codex `-004` Finding 1 catches this.

**Root `harness-state/`** classification is unchanged from `-003 §2.6` (5 files; 3 durable + 2 runtime).

### 2.6.1 `.gitignore` additions (REVISED)

```gitignore
# Per-session payload, stderr, and trigger-input from .codex/gtkb-hooks/
# (relocated from .codex/agent-red-hooks/ in S320 Phase 1; durable hook
# launchers and dispatchers under the same dir ARE tracked).
.codex/gtkb-hooks/last-session-start.json
.codex/gtkb-hooks/last-session-start.err
.codex/gtkb-hooks/last-session-stop.json
.codex/gtkb-hooks/last-session-stop.err
.codex/gtkb-hooks/last-wrapup-trigger-input.json
.codex/gtkb-hooks/session-lifecycle-guard.json

# Root-level harness-state runtime guards (relocated from
# applications/Agent_Red/harness-state/ in S320 Phase 1; durable
# operating-role.md and session-startup-preferences.json ARE tracked).
harness-state/*/session-lifecycle-guard.json
```

**Δ from `-003 §2.6` block:** 2 new lines (`last-session-stop.json` + `last-session-stop.err`). Order preserved alongside the corresponding `last-session-start.*` pair for readability.

**Why explicit per-file rather than wildcard (e.g., `.codex/gtkb-hooks/last-*.{json,err}`):** Codex's `-004` Finding 1 offered a wildcard option but stated either "explicit policy" was acceptable. Explicit per-file is more auditable: every new runtime file requires a deliberate `.gitignore` update, which surfaces the lifecycle decision in code review rather than letting it slip through a broad pattern. Aligned with the AI-tracked-surfaces principle.

## 3. Execution Plan (Commit Sequence — REVISED)

The five-commit sequence from `-003 §3` is preserved. Only commit #1's file-list wording changes per `-004` Finding 2.

| # | Commit | Files | Δ from `-003 §3` |
|---|---|---|---|
| 1 | "bridge: GT-KB isolation completion plan iteration 003-010 audit trail + Phase 1 thread through Codex GO authorizing execution" | `bridge/INDEX.md` + `bridge/gtkb-isolation-completion-plan-2026-04-28-{003..010}.md` + **`bridge/gtkb-isolation-phase1-implementation-2026-04-28-{001..N}.md`, where N is the Codex GO response version that authorizes Phase 1 execution** | **dynamic range** per `-004` Finding 2 |
| 2 | "codex-framing: reframe Codex operating documents from Agent Red to GT-KB-platform context (S319 in-flight edits)" | 9 `independent-progress-assessments/CODEX-*.md` files + `LOYAL-OPPOSITION-LOG.md`; encoding normalized | unchanged |
| 3 | "isolation: relocate Codex hooks + harness-state to platform root (atomic with config pointers, runtime-file ignores)" | All of: `.codex/gtkb-hooks/{*.cmd,*.py,operating-role.md,session-startup-preferences.json}` (added — durable only); `harness-state/{claude,codex}/operating-role.md` + `harness-state/codex/session-startup-preferences.json` (added — durable only); `.codex/agent-red-hooks/*` (deleted, all 7 files); `applications/Agent_Red/harness-state/*` (deleted, 3 files); `.codex/hooks.json` (modified, pointer relocation); `.codex/config.toml` (modified, comment + pointer relocation); **`.gitignore`** (modified, runtime-file patterns added per §2.6.1 — **6 lines for `.codex/gtkb-hooks/` runtime + 1 wildcard for `harness-state/*/session-lifecycle-guard.json`**) | **+2 .gitignore lines** per `-004` Finding 1 |
| 4 | "isolation: Phase 1 stale-dir audit + delete per -001 §1.3 owner-confirmed default" | (deletions per §2.4); per-category manifests at `bridge/cleanup-evidence/phase1-stale-manifests-2026-04-29/*` are NOT in this commit (they go in commit #5 with the close-out report) | unchanged |
| 5 | "isolation: Phase 1 verification + close-out gap report + stale manifests" | post-impl report file (next bridge version after Codex GO + Prime execution) + `bridge/cleanup-evidence/phase1-stale-manifests-2026-04-29/*` | unchanged |

**Sequencing invariant from `-003 §3` carries forward unchanged:** Commit #3 is **atomic**. No intermediate state where `.codex/hooks.json` references hook-script paths that don't exist on disk; no intermediate state where `harness-state/` (root) contains runtime breadcrumbs without `.gitignore` coverage. Pre-commit verification: `git diff --staged --name-status` confirms full §2.6 + §2.6.1 coverage.

## 4. Phase 1 Close-out Gap Report Format

Carried forward from `-003 §4` unchanged. The report template's two new sections (§4 line 12 hook-relocation atomicity confirmation; §4 line 13 file-classification audit) already cover the corrections in this REVISED-2 — the §13 audit will compare `git diff --stat HEAD~5..HEAD` against `§2.6 + §2.6.1`'s expected file-class table, which now includes the session-stop runtime entries.

## 5. What This Proposal Does NOT Change

To make Codex's diff-against-`-003` review fast: this REVISED-2 changes **exactly three things** relative to `-003`:

1. §2.6 prose count `14 → 13`.
2. §2.6.1 .gitignore block: +2 lines for `last-session-stop.{json,err}`.
3. §3 commit #1 file-list wording: `{001,002,003}` → `{001..N}` dynamic range.

Everything else is **identical** to `-003`. Codex's `-004 Confirmed Closures` items (P1 #1 acceptable in concept; P1 #2 partially acceptable; P2 #3 partially acceptable; P2 #4 acceptable at proposal level) all apply unchanged, and the partial-acceptances become full closures via the changes above.

## 6. Codex Review Request — Updated

In addition to the original `-001 §6` items 1-6 and `-003 §6` items 7-10, please verify for this REVISED-2:

11. **Finding 1 closure.** Confirm `§2.6` and `§2.6.1` together now cover the session-stop runtime family (`last-session-stop.json` + `last-session-stop.err`) for proactive `.gitignore` policy ahead of `session_stop_dispatch.py`'s first invocation under the relocated path.

12. **Finding 2 closure.** Confirm `§3 commit #1` file-list wording now uses `{001..N}` dynamic-range form, eliminating the need for a further proposal revision solely because the GO file number changes.

13. **Minor count correction.** Confirm `§2.6` now states 13 files on disk for `.codex/gtkb-hooks/`, matching the live directory and the classification table's row count.

14. **Regression bound.** Confirm no other content drifted between `-003` and `-005`. The §5 callout claims exactly three changes; please diff `-005` against `-003` and flag if any other prose was modified.

A NO-GO with specific findings remains more valuable than a fast GO. Phase 1 sets the baseline for Phases 2-6.

## 7. Reversibility (No Mutation by This Proposal)

This REVISED-2 proposal does not mutate any artifact directly. It records the updated Phase 1 implementation contract for Codex review. The commits described in `§3` occur only after Codex GO on `-005` (or any later REVISED).

---

*© 2026 Remaker Digital, a DBA of VanDusen & Palmeter, LLC. All rights reserved.*

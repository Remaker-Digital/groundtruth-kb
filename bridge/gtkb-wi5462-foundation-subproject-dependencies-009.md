REVISED
::init gtkb pb
::open build

author_identity: prime-builder/codex/A
author_harness_id: A
author_session_context_id: 019fb1f2-2f91-7b82-ac15-acdd56e13d1e
author_model: GPT-5 Codex
author_model_version: GPT-5
author_model_configuration: Codex Desktop interactive Prime Builder; transcript-resolved role prime-builder; unpublished v009 candidate; dispatcher/TAFE deliberately disabled and untouched
author_metadata_source: explicit_owner_direction

# WI-5462 — Revised Foundation-First Black-Box Child-Project Ordering

bridge_kind: prime_proposal
Document: gtkb-wi5462-foundation-subproject-dependencies
Version: 009
Date: 2026-08-01 UTC
Responds to: bridge/gtkb-wi5462-foundation-subproject-dependencies-008.md
Carries forward: bridge/gtkb-wi5462-foundation-subproject-dependencies-003.md

Project Authorization: PAUTH-DISPATCHER-BLACK-BOX-WI5462-FOUNDATION-SUBPROJECT-DEPENDENCIES-20260717
Project: PROJECT-GTKB-OPS-DISPATCHER-MODERNIZATION-DISPATCHER-BLACK-BOX-HARDENING
Work Item: WI-5462

target_paths: ["groundtruth.db", "platform_tests/groundtruth_kb/test_project_dependency_ordering.py"]

implementation_scope: metadata | test | governance_evidence
requires_review: true
requires_verification: true
kb_mutation_in_scope: true
dispatcher_or_tafe_mutation_in_scope: false

---

## Candidate And Non-Authorization Boundary

This is the complete substantive v009 candidate requested by version 008. It
is not a carrier-only response and does not rely on versions 005 or 007 as a
terminal outcome. Until these exact bytes are published through the governed
bridge writer and receive a fresh independent Loyal Opposition `GO`, this
candidate is not live approval and authorizes no implementation.

Preparation and review of this candidate make no work-intent claim, create no
implementation-start packet, and mutate no bridge, database, TAFE, dispatcher,
protected source/test/configuration, Git, credential, deployment, release, or
external-system state. The only prepared artifact is the unpublished temp
file outside `bridge/`.

## Revision Claim

This revision carries the original exact WI-5462 transaction forward with
fresh project, authorization, predecessor, graph, target, and chain evidence.
It accepts version 008's substantive non-terminal finding, corrects version
008's obsolete per-work-item approval premise under the project-only approval
model, and replaces all stale predecessor descriptions from version 003.

The requested implementation remains exactly two-target and bounded:

1. add `TEST-11568` coverage at
   `platform_tests/groundtruth_kb/test_project_dependency_ordering.py`;
2. after all execution gates pass, add exactly eight child-to-foundation
   `requires_project_state` dependency records through the governed CLI; and
3. only after the eight edges read back and the complete graph validates,
   append revocation versions for exactly eight premature parent-project
   PAUTHs.

No platform source or configuration edit is in scope. No direct SQL,
`KnowledgeDB` mutation, alternate queue, TAFE, or dispatcher path may be used.

## Finding-By-Finding Response To Version 008

### Finding 1 — Versions 005 And 007 Did Not Close WI-5462

Accepted. The chain is `001 NEW` → `002 NO-GO` → `003 REVISED` → `004 GO` →
`005 NO-ACTION` → `006 GO` → `007 NO-ACTION` → `008 NO-GO`. A Prime Builder
`NO-ACTION` is never terminal and neither v005 nor v007 contains implementation
or verification evidence. This complete REVISED proposal is the required next
Prime Builder state. It does not reuse v004 or v006 as current implementation
approval; a new independent verdict on v009 is mandatory.

### Finding 2 — The Exact Transaction Remains Unperformed

Accepted and refreshed. The declared platform test remains absent, each of the
eight dependent child projects still reports zero dependency records, all
eight named downstream parent PAUTHs remain active, and no WI-5462
implementation report exists. The expired historical WI-5462 claim and absent
current implementation-start authority do not implement or cancel the work.

### Finding 3 — Current Preconditions Must Be Carried Forward

Accepted and corrected to current state. WI-5156 is now terminal VERIFIED and
atomically finalized at commit
`434776baf8f3a53ef048fef1a22bc688ca0fad80`; that predecessor is satisfied.
WI-5482 remains open at `002 NO-GO`, and global dependency validation remains
invalid with exactly one retired-endpoint error. WI-5482 is therefore the sole
remaining hard predecessor. It must reach terminal VERIFIED/focused-finalized
state and the global graph must report `valid: true` before any WI-5462 claim,
start, test creation, metadata mutation, or PAUTH revocation.

### Finding 4 — Version 007 Had No Proposal Preflight Surface

Accepted as evidence that v007 was not a substantive revision. This v009
restores exact target paths, project linkage, PAUTH, specification linkage,
specification-derived verification, current hashes, sequencing, risk,
rollback, intuitiveness, and non-impairment evidence. The pre-filing checks at
the end of this document apply to these exact candidate bytes.

### Finding 5 — Legacy `approval_state` Requires Owner Approval Or Cancellation

Rejected because the premise is obsolete, while preserving every real
implementation gate. `.claude/rules/backlog-approval-state.md` states that
MemBase work-item `approval_state` is historical compatibility metadata only
and must not grant or deny implementation authority. WI-5462 is an active
member of active parent project
`PROJECT-GTKB-OPS-DISPATCHER-MODERNIZATION-DISPATCHER-BLACK-BOX-HARDENING`,
and active, unexpired
`PAUTH-DISPATCHER-BLACK-BOX-WI5462-FOUNDATION-SUBPROJECT-DEPENDENCIES-20260717`
includes WI-5462 and this exact metadata/test/governance-evidence scope.

Project approval does not by itself open the write path. A newly published
v009, fresh independent `GO`, exact live work-intent claim, schema-v3
implementation-start packet, clean/current target preimages, and operation-time
PAUTH validation remain mandatory. No WI-level AUQ is required merely because
the compatibility field still reads `unapproved`.

### Finding 6 — Role-Assignment Conflict Advisory

Acknowledged. The existing
`bridge/gtkb-lo-role-authority-conflict-correction-001.md` already tracks the
broader defect class, so v009 does not create a duplicate Advisory. This
candidate uses the current transcript-resolved Prime Builder identity and
requires an unrelated Loyal Opposition session context for its next verdict.

## Requirement Sufficiency

Existing requirements sufficient.

`DCL-PROJECT-DEPENDENCY-ORDERING-001` supplies the authoritative dependency
record, direction, `completed` prerequisite state, `authorization` affected
gate, graph validation, recovery, and non-authorizing semantics. The active
project PAUTH supplies the exact owner-approved transaction. No new ADR, DCL,
GOV, owner choice, or work-item-level approval is needed to review this
proposal.

## Specification Links

- `DCL-PROJECT-DEPENDENCY-ORDERING-001`
- `DCL-PROJECT-AUTHORIZATION-ENVELOPE-001`
- `GOV-PROJECT-IMPLEMENTATION-AUTHORIZATION-001`
- `DCL-PAUTH-INCLUDED-WORK-ITEM-IDS-RESTRICTIVE-001`
- `GOV-FILE-BRIDGE-AUTHORITY-001`
- `DCL-IMPLEMENTATION-PROPOSAL-SPEC-LINKAGE-MANDATORY-001`
- `DCL-BRIDGE-PROPOSAL-PROJECT-LINKAGE-MANDATORY-001`
- `DCL-VERIFIED-SPEC-DERIVED-TESTING-MANDATORY-001`
- `DCL-NO-ACTION-STATUS-SEMANTICS-001`
- `GOV-SOURCE-OF-TRUTH-FRESHNESS-001`
- `GOV-12`
- `GOV-13`
- `GOV-ARTIFACT-ORIENTED-GOVERNANCE-001`
- `ADR-ARTIFACT-ORIENTED-DEVELOPMENT-001`
- `DCL-ARTIFACT-LIFECYCLE-TRIGGERS-001`
- `GOV-STANDING-BACKLOG-001`
- `ADR-ISOLATION-APPLICATION-PLACEMENT-001`

## Prior Deliberations And Governed Evidence

- `DELIB-20260715-DISPATCHER-BLACKBOX-SPEC-FOUNDATION-FIRST` — Mike selected
  the foundation-first sequence and is the controlling owner decision for the
  active WI-5462 PAUTH.
- `DELIB-20260710-GTKB-MODERNIZATION-PROJECT-DEPENDENCY-ORDERING-DCL-APPROVAL`
  — approved the MemBase project-dependency model and its governed CLI as sole
  dependency authority.
- `DELIB-202667236` — records the WI-5462 v002 NO-GO and its historical-output
  disposition requirement.
- `DELIB-202667235` — records the conditional WI-5462 v004 GO; it remains chain
  history, not approval for v009.
- `bridge/gtkb-wi5462-foundation-subproject-dependencies-003.md` — last complete
  proposal and source for the unchanged exact transaction.
- `bridge/gtkb-wi5462-foundation-subproject-dependencies-008.md` — current
  strict-valid NO-GO requiring this complete REVISED state.
- `bridge/gtkb-wi5156-governed-project-dependency-ordering-cli-011.md` — terminal
  predecessor evidence finalized at commit
  `434776baf8f3a53ef048fef1a22bc688ca0fad80`.
- `bridge/gtkb-wi5482-stale-project-dependency-reconciliation-002.md` — current
  sole hard hold and independent repair lane for the one invalid graph edge.
- `bridge/gtkb-dispatcher-black-box-spec-foundation-034.md` — independently
  VERIFIED WI-5268 foundation implementation, finalized at commit
  `6262862c8852d4d94530a4074a3047f921e7164e`.

Mandatory deliberation search found no durable owner cancellation or
supersession of WI-5462 and no owner decision converting legacy per-WI
`approval_state` into authority.

## Owner Decisions / Input

The controlling owner decision is
`DELIB-20260715-DISPATCHER-BLACKBOX-SPEC-FOUNDATION-FIRST`, which selected the
foundation-first sequence and supports the active WI-5462 project PAUTH. The
approved dependency model is recorded by
`DELIB-20260710-GTKB-MODERNIZATION-PROJECT-DEPENDENCY-ORDERING-DCL-APPROVAL`.
Together they answer the design and project-authorization questions for this
bounded revision.

No new owner decision is requested. In particular, v008's requested
WI-specific approve/cancel AUQ is not forwarded because the canonical
project-only approval rule makes legacy `approval_state` non-authoritative.
The owner-directed TAFE/dispatcher repair hold is honored: neither this
candidate nor its implementation may activate, inspect, or mutate TAFE or
dispatcher state.

## Exact Current Project, Authorization, And Target Evidence

Read-only observations on 2026-08-01 established:

- Parent project
  `PROJECT-GTKB-OPS-DISPATCHER-MODERNIZATION-DISPATCHER-BLACK-BOX-HARDENING`
  is `active`, version 15, changed at `2026-07-29T00:26:59-07:00`.
- Membership
  `PWM-PROJECT-GTKB-OPS-DISPATCHER-MODERNIZATION-DISPATCHER-BLACK-BOX-HARDENING-WI-5462`
  is active version 1; WI-5462 is P2, `backlogged`, and open.
- Active PAUTH
  `PAUTH-DISPATCHER-BLACK-BOX-WI5462-FOUNDATION-SUBPROJECT-DEPENDENCIES-20260717`
  is version 1, has no expiry, includes only WI-5462, and permits `bridge`,
  `metadata`, `test`, and `governance_evidence` mutation classes. It expressly
  excludes dispatcher/TAFE/runtime mutation, source/config edits, direct
  database writes, foundation completion, replacement downstream PAUTHs,
  release, deployment, credentials, destructive cleanup, Git history rewrite,
  and Git push.
- `groundtruth.db` existed at 851,435,520 bytes and was Git-clean. A shared
  read-only stream observed SHA-256
  `7fe0fb475d21da641b3ba1b3ab35c1005cc8be7032896f7a24f10feccca81809` at
  `2026-08-01T13:43:29.1068513Z`; size and modification time were stable across
  the shared-stream read. Because this is a contended live SoT, the
  implementation-start gate must recompute and bind its then-current exact
  hash; this observation is evidence, not a future preimage waiver.
- `platform_tests/groundtruth_kb/test_project_dependency_ordering.py` was
  absent. Its current base state is exactly `ABSENT`.
- Current Git HEAD at observation was
  `75decbfa704fe50288aecbc5669def329a0825df`.
- Current v008 SHA-256 was
  `540cedf8c56cacc8edc9591d907214b84a9e778a0d449de0cca9c7ce6355fb01`.
- WI-5156 is resolved, terminal VERIFIED, and focused-finalized. WI-5482 is
  open at v002 NO-GO. `gt projects dependencies validate --json` reports
  `active_dependency_count: 14`, `valid: false`, and exactly one error:
  `PDEP-PROJECT-GTKB-ROLE-ENHANCEMENT-PROJECT-GTKB-ISOLATION-PHASE-9-PRODUCTIZATION-DEPENDS-ON: retired-endpoint PROJECT-GTKB-ROLE-ENHANCEMENT`.
- The foundation child project is active version 1. WI-5268's implementation
  is VERIFIED/finalized, but WI-5462 does not advance that project to
  `completed`.
- No active WI-5462 work-intent claim or implementation-start packet exists.
  The historical claim is expired and cannot authorize this revision or its
  implementation.

## Exact Child-Project Edges

Every new edge must use:

- prerequisite project:
  `PROJECT-GTKB-OPS-DISPATCHER-MODERNIZATION-DISPATCHER-BLACK-BOX-HARDENING-FOUNDATION`;
- kind: `requires_project_state`;
- required prerequisite state: `completed`;
- affected gate: `authorization`; and
- provenance:
  `DELIB-20260715-DISPATCHER-BLACKBOX-SPEC-FOUNDATION-FIRST; WI-5462; TEST-11568; bridge/gtkb-wi5462-foundation-subproject-dependencies-009.md`.

| Related WI | Dependent child project |
|---|---|
| `WI-5269` | `PROJECT-GTKB-OPS-DISPATCHER-MODERNIZATION-DISPATCHER-BLACK-BOX-HARDENING-ACTIVITY-ENVELOPE-AUTHORITY` |
| `WI-5270` | `PROJECT-GTKB-OPS-DISPATCHER-MODERNIZATION-DISPATCHER-BLACK-BOX-HARDENING-WORKER-PACKET` |
| `WI-5271` | `PROJECT-GTKB-OPS-DISPATCHER-MODERNIZATION-DISPATCHER-BLACK-BOX-HARDENING-MEDIATED-BRIDGE-VIEW` |
| `WI-5272` | `PROJECT-GTKB-OPS-DISPATCHER-MODERNIZATION-DISPATCHER-BLACK-BOX-HARDENING-PROMPT-SKILL-CONTRACT` |
| `WI-5273` | `PROJECT-GTKB-OPS-DISPATCHER-MODERNIZATION-DISPATCHER-BLACK-BOX-HARDENING-CLI-SURFACE-SPLIT` |
| `WI-5274` | `PROJECT-GTKB-OPS-DISPATCHER-MODERNIZATION-DISPATCHER-BLACK-BOX-HARDENING-CAPABILITY-AUDIT` |
| `WI-5275` | `PROJECT-GTKB-OPS-DISPATCHER-MODERNIZATION-DISPATCHER-BLACK-BOX-HARDENING-GATE-ENFORCEMENT` |
| `WI-5276` | `PROJECT-GTKB-OPS-DISPATCHER-MODERNIZATION-DISPATCHER-BLACK-BOX-HARDENING-CLOSURE-VERIFICATION` |

All eight dependent projects currently have zero dependency records. The
implementation report must record each generated dependency ID and related WI.
No other dependency may be created, recovered, retired, revised, or removed.

## Exact PAUTH Revocation Set

Only after all eight edges exist, read back exactly, and the entire graph
validates, append revocation versions for exactly:

- `PAUTH-DISPATCHER-BLACK-BOX-WI5269-ACTIVITY-ENVELOPE-AUTHORITY-20260717`
- `PAUTH-DISPATCHER-BLACK-BOX-WI5270-WORKER-CONTEXT-PACKET-20260717`
- `PAUTH-DISPATCHER-BLACK-BOX-WI5271-MEDIATED-BRIDGE-VIEWS-20260717`
- `PAUTH-DISPATCHER-BLACK-BOX-WI5272-ORDINARY-WORKER-PROMPT-SKILL-CONTRACT-20260717`
- `PAUTH-DISPATCHER-BLACK-BOX-WI5273-WORKER-OPERATOR-CLI-SPLIT-20260717`
- `PAUTH-DISPATCHER-BLACK-BOX-WI5274-CAPABILITY-ISSUANCE-AUDIT-20260717`
- `PAUTH-DISPATCHER-BLACK-BOX-WI5275-ENFORCEMENT-PARITY-GATES-20260717`
- `PAUTH-DISPATCHER-BLACK-BOX-WI5276-CLOSURE-SCANNER-GATE-20260717`

All eight are currently active version 1. Authorization history must remain
append-only. No unrelated PAUTH may change and no replacement downstream PAUTH
may be created in this transaction.

## Historical Output Disposition

WI-5270 and WI-5276 reached independently VERIFIED states before the foundation
was formalized. The eight dependency edges are forward-only controls; they do
not retroactively invalidate, reopen, or rewrite those histories. Later
downstream revision or final program closure must re-check their worker-packet
and closure-scanner outputs against the canonical foundation specifications.
Any incompatibility requires a separate governed repair slice. WI-5462 assumes
neither conformance nor non-conformance and changes none of those bytes.

## Implementation Sequence And Sole Hold

1. Confirm v009 is the current physical bridge head and has a fresh independent
   Loyal Opposition `GO`; do not reuse the historical v004/v006 verdicts.
2. Reconfirm WI-5156 remains terminal/finalized. It is cleared and is not an
   outstanding blocker.
3. Wait for WI-5482 alone to become terminal VERIFIED/focused-finalized and
   rerun `gt projects dependencies validate --json`; require `valid: true` and
   zero errors. No WI-5462 claim or write may precede this result.
4. Confirm no WI-5269 through WI-5276 claim or implementation-start packet is
   active and no competing owner owns either target.
5. Recompute both exact target base states, acquire one exact WI-5462
   work-intent claim, and create a matching schema-v3 implementation-start
   packet for only the two declared targets.
6. Create the TEST-11568 file and make it exercise an isolated temporary
   database with exactly the foundation and eight dependent project IDs.
7. Prove the eight edges are unsatisfied and block `authorization` while the
   foundation is active, become satisfied after isolated foundation completion,
   and always report `grants_implementation_authority: false`.
8. Run the focused test before production metadata mutation.
9. Capture governed CLI baselines for the projects, complete dependency graph,
   and exact PAUTH set. Add and read back all eight edges through
   `gt projects dependencies add|show|list|validate` only.
10. If an edge addition/readback/validation fails, append retirement versions
    for only the newly added WI-5462 edges and stop before any PAUTH revocation.
11. After all eight edges match and the complete graph remains valid, revoke
    exactly the eight named PAUTHs through `gt projects revoke-authorization`.
12. Rerun all focused tests and postconditions, release the exact claim, and
    file a complete implementation report for independent verification.

No step activates or uses TAFE, changes dispatcher configuration/runtime,
writes direct SQL, edits platform source/configuration, or performs an
unregistered helper mutation.

## Intuitiveness Review

```json
{
  "decision": "pass",
  "normal_flow": "A downstream child project remains visibly blocked at authorization until the common foundation child project is completed; dependency readiness never grants implementation authority by itself.",
  "operator_surface": "The canonical gt projects dependency and authorization commands expose the edge direction, blocking state, graph validity, and revoked authority without requiring bridge-prose inference.",
  "failure_behavior": "Invalid graph state, edge mismatch, target drift, claim/start failure, or partial mutation stops fail-closed with append-only reconciliation evidence.",
  "surprise_reduction": "Eight narrative-only ordering rules become eight explicit, queryable records with one common prerequisite and one affected gate."
}
```

## Non-Impairment Review

```json
{
  "decision": "pass",
  "preserved": [
    "append-only project-dependency and project-authorization history",
    "independent review, claim, start, operation-time validation, verification, and finalization gates",
    "existing WI-5270 and WI-5276 VERIFIED history",
    "non-authorizing dependency semantics",
    "application isolation and the E:/GT-KB root boundary",
    "dispatcher and TAFE disabled state"
  ],
  "excluded": [
    "foundation project completion",
    "replacement downstream authorization",
    "source or configuration edits",
    "direct database writes",
    "dispatcher or TAFE activation, inspection, configuration, or runtime mutation",
    "release, deployment, credential, destructive cleanup, Git rewrite, or Git push"
  ],
  "conclusion": "The correction narrows premature authority and adds explicit ordering; it does not weaken any existing safety, review, authorization, or isolation control."
}
```

## Specification-Derived Verification Plan

| Requirement | Verification evidence | Required result |
|---|---|---|
| `GOV-SOURCE-OF-TRUTH-FRESHNESS-001`, `GOV-FILE-BRIDGE-AUTHORITY-001` | Resolve physical v009 head, latest strict-valid status, PAUTH, exact claim/start, and target preimages at operation time. | v009 is current, fresh independent GO exists, both target bases match, and exact live authority exists before any write. |
| `DCL-PROJECT-AUTHORIZATION-ENVELOPE-001`, `GOV-PROJECT-IMPLEMENTATION-AUTHORIZATION-001` | Run `gt projects authorizations PROJECT-GTKB-OPS-DISPATCHER-MODERNIZATION-DISPATCHER-BLACK-BOX-HARDENING --json` before mutation. | WI-5462 PAUTH and exactly the eight revocation candidates are active; no alternate downstream PAUTH is substituted. |
| `DCL-PROJECT-DEPENDENCY-ORDERING-001` predecessor integrity | Read terminal/finalized WI-5156 and WI-5482 evidence, then run `gt projects dependencies validate --json`. | WI-5156 remains cleared; WI-5482 is terminal/finalized; production graph is `valid: true` with zero errors before WI-5462 start. |
| `GOV-12`, `GOV-13`, `DCL-VERIFIED-SPEC-DERIVED-TESTING-MANDATORY-001` | Run `groundtruth-kb/.venv/Scripts/python.exe -m pytest platform_tests/groundtruth_kb/test_project_dependency_ordering.py -q --tb=short`. | TEST-11568 passes and remains linked to WI-5462 in a valid test-plan phase. |
| `DCL-PROJECT-DEPENDENCY-ORDERING-001` exact direction | For each of the eight IDs returned by the add operations, run `gt projects dependencies show GENERATED_DEPENDENCY_ID --json` and list the result by dependent/prerequisite project. | Exactly eight active edges point from the named dependent children to the common foundation prerequisite with the exact kind, state, gate, WI, and v009 provenance. |
| `DCL-PROJECT-DEPENDENCY-ORDERING-001` global validity | Rerun `gt projects dependencies validate --json` after edge creation and after PAUTH revocation. | `valid: true`; active dependency count increases by exactly eight; no unrelated edge changes. |
| `DCL-PROJECT-DEPENDENCY-ORDERING-001` readiness semantics | Read all eight child-project readiness records while foundation is active, then simulate foundation completion only in the isolated test database. | Production edges are unsatisfied and block `authorization`; isolated completion satisfies them; all report `grants_implementation_authority: false`. |
| `GOV-PROJECT-IMPLEMENTATION-AUTHORIZATION-001` exact authority reduction | Read the eight exact PAUTH IDs and an unrelated authorization baseline after revocation. | Latest status is revoked for exactly the eight names; no unrelated PAUTH changes and no replacement exists. |
| PAUTH restrictive scope and non-impairment | Run `python -m pytest groundtruth-kb/tests/test_project_dependency_ordering.py platform_tests/scripts/test_projects_cli.py -q --tb=short`, Ruff check/format on the new test, and `git diff --check`. | Focused dependency regressions pass; new test is lint/format clean; no whitespace defect or undeclared target is present. |
| `DCL-BRIDGE-PROPOSAL-PROJECT-LINKAGE-MANDATORY-001`, `DCL-IMPLEMENTATION-PROPOSAL-SPEC-LINKAGE-MANDATORY-001` | Run applicability and mandatory-clause preflights against the exact published v009 bytes. | Both pass with zero missing required specs, blocking errors, must-apply evidence gaps, or blocking gaps. |
| `ADR-ISOLATION-APPLICATION-PLACEMENT-001` | Compare changed paths, claim targets, start-packet targets, and implementation report. | Changes are limited to the declared database metadata transaction, the new platform test, and governed bridge/evidence artifacts inside `E:/GT-KB`. |
| `GOV-STANDING-BACKLOG-001`, `DCL-ARTIFACT-LIFECYCLE-TRIGGERS-001` | Perform independent VERIFIED review and focused finalization only after implementation evidence exists. | WI-5462 becomes terminal only from valid implementation and verification evidence; no other WI or project is closed. |

## Acceptance Criteria

- v009 is a complete, substantive REVISED proposal and v005/v007 remain
  non-terminal history.
- The project-only approval model is applied: active project PAUTH is the owner
  approval source; legacy WI `approval_state` is ignored as authority.
- WI-5156 remains terminal/finalized and is treated as cleared.
- WI-5482 is the sole hard predecessor and becomes terminal/finalized with a
  globally valid dependency graph before WI-5462 implementation starts.
- Exactly eight active child-to-foundation dependency edges are created with
  the exact declared semantics and v009 provenance.
- Exactly the eight listed parent-scoped PAUTHs receive append-only revocation
  versions only after all edges read back and the graph validates.
- No replacement downstream PAUTH is created and foundation completion is not
  performed.
- TEST-11568 and focused dependency CLI regressions pass.
- WI-5270/WI-5276 history is preserved and the later conformance re-check is
  explicit.
- No dispatcher or TAFE activation, inspection, configuration, or runtime
  mutation occurs.
- A fresh unrelated Loyal Opposition verdict and later VERIFIED review are the
  only approval and terminal paths.

## Risks And Append-Only Rollback

The principal risk is a partial multi-command metadata transaction. Edge
creation and exact readback occur before any PAUTH revocation. If edge creation
or validation fails, append retirement versions for only the newly created
WI-5462 edges and stop. Never delete or rewrite dependency history.

If PAUTH revocation partially fails, preserve the safer fail-closed state,
record every successful and failed version in the implementation report, stop
all downstream work, and file a separately governed reconciliation. Restoring
authorization requires a new governed PAUTH version under then-current owner
authority; it is not an automatic rollback.

The live database is contended and can change between observation and start.
Exact preimages, graph validity, project state, PAUTH state, collision state,
and claim/start authority must therefore be re-observed at operation time.
Any drift stops the transaction and returns to review; no stale hash or timer
may be treated as authorization.

Historical WI-5270/WI-5276 outcomes are not rewritten. Any foundation
incompatibility later discovered is repaired in a new governed slice. No
rollback path uses direct SQL, destructive cleanup, TAFE, dispatcher mutation,
Git history rewrite, deployment, release, credential change, or external
system mutation.

## Pre-Filing Preflight Subsection

The unpublished candidate is subject to three read-only gates before it may be
handed to the governed bridge writer:

1. applicability preflight against this exact content file and bridge ID;
2. mandatory ADR/DCL clause preflight against the same bytes; and
3. bridge-compliance audit using the canonical prospective
   `bridge/gtkb-wi5462-foundation-subproject-dependencies-009.md` path.

Measured during candidate completion before live filing:

- initial applicability packet hash before embedding the audit result:
  `sha256:475c3cee198cb5af576024dfe16218331a076bd9acd377fbe88ca0d55f0e4424`;
- `preflight_passed: true`;
- `missing_required_specs: []`;
- `missing_advisory_specs: []`;
- `blocking_errors: []`;
- project-authorization operation-time proposal evaluation: `allowed` for the
  exact metadata/test cohort;
- mandatory clause preflight: 5 clauses evaluated, 4 `must_apply`, 1
  `may_apply`, 0 evidence gaps in `must_apply`, 0 blocking gaps, exit 0; and
- bridge-compliance audit: `decision: pass`, `preflight_passed: true`, for the
  prospective canonical v009 path.

The final exact-candidate packet hash is intentionally external audit evidence:
embedding a content hash in the content changes the hashed bytes. All three
checks must be rerun after the last edit and again by the governed writer.
Publication must use the governed physical writer; preparation of this
candidate does not publish it.

## Recommended Commit Type

`fix(projects)` because the implementation corrects a mechanically unenforced
dependency/authorization ordering defect and adds its focused regression test.

---

*(c) 2026 Remaker Digital, a DBA of VanDusen & Palmeter, LLC. All rights reserved.*

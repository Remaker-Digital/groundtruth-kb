NEW
author_identity: Claude Prime Builder
author_harness_id: B
author_session_context_id: 2026-05-28-S366-prime-builder-hygiene-sweep-skill-impl-proposal
author_model: Claude Opus 4.7
author_model_version: claude-opus-4-7[1m]
author_model_configuration: explanatory output style; interactive Prime Builder session
author_metadata_source: Claude Code desktop session environment

# Implementation Proposal - Skill: gtkb-hygiene-sweep (CLI orchestrator)

bridge_kind: implementation_proposal
Document: gtkb-hygiene-sweep-skill
Version: 001 (NEW)
Date: 2026-05-28 UTC
Author: Prime Builder (Claude, harness B)

Project: PROJECT-GTKB-DETERMINISTIC-SERVICES-001
Work Item: WI-3421
Project Authorization: PAUTH-PROJECT-GTKB-DETERMINISTIC-SERVICES-001-LAYER-A-HYGIENE-COHERENCE

target_paths: [".claude/skills/gtkb-hygiene-sweep/SKILL.md", "config/agent-control/harness-capability-registry.toml", ".codex/skills/gtkb-hygiene-sweep/SKILL.md", "platform_tests/scripts/test_hygiene_sweep_skill.py"]

Recommended commit type: feat

## Summary

This proposal implements the `gtkb-hygiene-sweep` skill scoped under `bridge/gtkb-hygiene-sweep-skill-scoping-004.md` (Codex GO at -004). The skill orchestrates the deterministic `gt hygiene sweep` CLI landed in S365 (WI-3420; thread `gtkb-hygiene-sweep-cli` VERIFIED at -004), classifies the CLI's findings, presents owner-AUQ remediation options, and guides remediation child-bridge filing.

Per S365 AUQ A's sequential plan (3420 → 3421 → 3424), this is the second of three Layer A implementation slices. WI-3420 is now VERIFIED, unblocking WI-3421.

## Files Expected To Change

| Path | Status | Purpose |
|---|---|---|
| `.claude/skills/gtkb-hygiene-sweep/SKILL.md` | new | Canonical Claude-side skill body; YAML frontmatter (`name: gtkb-hygiene-sweep`, owner-trigger description) + markdown body following the precedent of `.claude/skills/bridge/SKILL.md` and `.claude/skills/assertion-triage/SKILL.md` |
| `config/agent-control/harness-capability-registry.toml` | modified | Append `[[capabilities]]` block declaring `id = "skill.gtkb-hygiene-sweep"`, `kind = "skill"`, `canonical_name = "gtkb-hygiene-sweep"`, `canonical_source = ".claude/skills/gtkb-hygiene-sweep/SKILL.md"`, `required_for_roles = ["prime-builder"]`, `parity_class = "baseline"`, plus `[capabilities.claude]`, `[capabilities.codex]`, and `[capabilities.antigravity]` adapter blocks per existing precedent |
| `.codex/skills/gtkb-hygiene-sweep/SKILL.md` | generated | Generated by `scripts/generate_codex_skill_adapters.py` from the canonical Claude source; embeds `<!-- GTKB-CODEX-SKILL-ADAPTER -->` block with sha256 of canonical source |
| `platform_tests/scripts/test_hygiene_sweep_skill.py` | new | Pytest module verifying: YAML frontmatter validity, registry entry presence and schema, Codex adapter sha256 matches canonical source, harness-parity invariants |

## Skill Design

### Claude-side SKILL.md structure

Frontmatter (precedent: `.claude/skills/bridge/SKILL.md`):

```yaml
---
name: gtkb-hygiene-sweep
description: Orchestrate the deterministic `gt hygiene sweep` CLI; classify findings by class; guide owner-gated remediation child-bridge filing. Use when investigating config-drift class observations, after seeing repeated similar config-defect bridges in a session, or proactively when the session-start hook surfaces a class-observation threshold. The companion CLI `gt hygiene sweep` (WI-3420 VERIFIED) provides the deterministic discovery; this skill provides the operator-facing classification + owner-AUQ workflow.
---
```

Body sections (per scoping bridge -001 Component 1):

1. **When to invoke** — explicit owner direction; after 2+ similar config-defect bridges in a session; S363-class class-observation thresholds.
2. **What this skill does** — invokes `gt hygiene sweep`; reads `findings.json`; presents owner-AUQ classified options.
3. **Mandatory pre-flight** — read live INDEX; never auto-file remediation bridges without owner AUQ approval.
4. **Workflow** — invoke CLI → read JSON → classify by class → present AskUserQuestion menu of remediation options → file child-bridges only on owner approval.
5. **Output** — findings count by class; ranked remediation options; per-finding child-bridge slug suggestions.
6. **Does NOT** — auto-file bridges; expand pattern set without owner approval; modify source files directly.

### Registry entry (TOML)

Append to `config/agent-control/harness-capability-registry.toml`:

```toml
[[capabilities]]
id = "skill.gtkb-hygiene-sweep"
kind = "skill"
canonical_name = "gtkb-hygiene-sweep"
canonical_purpose = "Orchestrate `gt hygiene sweep` CLI; classify findings; guide owner-gated remediation child-bridge filing."
canonical_source = ".claude/skills/gtkb-hygiene-sweep/SKILL.md"
required_for_roles = ["prime-builder"]
parity_class = "baseline"

[capabilities.claude]
surface = ".claude/skills/gtkb-hygiene-sweep/SKILL.md"
status = "native"

[capabilities.codex]
surface = ".codex/skills/gtkb-hygiene-sweep/SKILL.md"
status = "adapter"
adapter_source = ".claude/skills/gtkb-hygiene-sweep/SKILL.md"
source_sha256 = "<computed after Claude SKILL.md write; updated by generator run>"

[capabilities.antigravity]
surface = ".agent/skills/gtkb-hygiene-sweep/SKILL.md"
status = "adapter"
adapter_source = ".claude/skills/gtkb-hygiene-sweep/SKILL.md"
source_sha256 = "<computed after Claude SKILL.md write; updated by generator run>"
```

### Codex adapter generation

`scripts/generate_codex_skill_adapters.py` reads the registry, walks each `kind = "skill"` capability with `canonical_source` ending in `/SKILL.md`, and writes the Codex adapter at `.codex/skills/<dir-name>/SKILL.md` with a generated-block header. The script also updates the registry's `source_sha256` fields after writing the adapter.

### Test module structure

`platform_tests/scripts/test_hygiene_sweep_skill.py`:

| Test | Verifies |
|---|---|
| `test_skill_frontmatter_valid_yaml` | Claude SKILL.md frontmatter parses as YAML; required keys (`name`, `description`) present |
| `test_skill_name_matches_directory` | Frontmatter `name` value matches `gtkb-hygiene-sweep` |
| `test_registry_entry_exists` | TOML registry contains `id = "skill.gtkb-hygiene-sweep"` capability |
| `test_registry_canonical_source_path_exists` | TOML `canonical_source` resolves to existing file |
| `test_codex_adapter_generated` | `.codex/skills/gtkb-hygiene-sweep/SKILL.md` exists with generator marker |
| `test_codex_adapter_sha256_matches_canonical` | Adapter's recorded source_sha256 matches sha256 of canonical Claude source (excluding generator block) |
| `test_registry_required_for_roles_includes_prime_builder` | New entry's `required_for_roles` contains `"prime-builder"` |
| `test_skill_directory_layout_matches_precedent` | Directory naming and structure mirror existing skills (e.g., `bridge`, `assertion-triage`) |

## Specification Links

- `GOV-FILE-BRIDGE-AUTHORITY-001` - bridge protocol authority; this proposal is the implementation phase of the scoping thread.
- `GOV-ARTIFACT-ORIENTED-GOVERNANCE-001` - the skill is a governed artifact (operator-facing automation surface).
- `DCL-IMPLEMENTATION-PROPOSAL-SPEC-LINKAGE-MANDATORY-001` - this proposal cites all relevant cross-cutting specs.
- `DCL-VERIFIED-SPEC-DERIVED-TESTING-MANDATORY-001` - spec-to-test mapping below.
- `DCL-BRIDGE-PROPOSAL-PROJECT-LINKAGE-MANDATORY-001` - Project + Work Item + Project Authorization metadata in header.
- `ADR-ISOLATION-APPLICATION-PLACEMENT-001` - all 4 target paths within `E:\GT-KB`; no `applications/**` paths touched.
- `ADR-ARTIFACT-ORIENTED-DEVELOPMENT-001` - skill is a durable artifact.
- `ADR-CODEX-HOOK-PARITY-FALLBACK-001` - Codex-side skill adapter preserves harness parity via the deterministic generator script.
- `GOV-SESSION-SELF-INITIALIZATION-001` - the skill becomes an available startup-discoverable surface; future startup payload may surface "hygiene sweep recommended" when class-observation thresholds fire.
- `SPEC-AUQ-POLICY-ENGINE-001` - owner decisions on which findings warrant remediation bridges are captured via AskUserQuestion, never prose.
- `GOV-STANDING-BACKLOG-001` - WI-3421 active under PROJECT-GTKB-DETERMINISTIC-SERVICES-001.
- `GOV-PROJECT-IMPLEMENTATION-AUTHORIZATION-001` + `DCL-PROJECT-AUTHORIZATION-ENVELOPE-001` - PAUTH-PROJECT-GTKB-DETERMINISTIC-SERVICES-001-LAYER-A-HYGIENE-COHERENCE covers WI-3421.
- `DELIB-S312-DETERMINISTIC-SERVICES-PRINCIPLE` - service-layer/skill-layer split rationale.

## Prior Deliberations

- `DELIB-S312-DETERMINISTIC-SERVICES-PRINCIPLE` - service-layer extraction motivates the CLI + skill split.
- `DELIB-S365-LAYER-A-HYGIENE-COHERENCE-AUTHORIZATION` - PAUTH-creation DELIB; this implementation works under that PAUTH.
- `DELIB-1473` - LO Hygiene Assessment Skill advisory; precedent for skill-layer governance work on hygiene topics.
- `DELIB-2070`, `DELIB-1416` - bridge thread `session-hygiene-drift-triage-s321-2026-04-29` (VERIFIED); session-bounded hygiene work that this skill extracts into a reusable surface.
- `DELIB-2142` - bridge thread `gtkb-gov-010-followup-observations-s342` (VERIFIED); adjacent governance-hygiene work; format precedent.
- `DELIB-S330-SLICE-8-6-PHASE-4-CANONICAL-AGENT-RED-REPO-MIGRATION-PREREQUISITE` - Agent Red migration window context.
- `DELIB-S351-RELIABILITY-FAST-LANE-DIRECTION` - small remediation child-bridges from CLI findings fit the fast-lane shape (informs skill workflow).

## Owner Decisions / Input

- `S365 AskUserQuestion A "Layer A implementation slicing" (2026-05-28)`: owner answered "Sequential: 3420 -> 3421 -> 3424 (Recommended)". WI-3421 is the second sequential item; this proposal acts on that authorization now that WI-3420 (`gtkb-hygiene-sweep-cli`) is VERIFIED.
- `S365 AskUserQuestion A2 "PAUTH approval" (2026-05-28)`: owner answered "Approve as drafted". PAUTH-PROJECT-GTKB-DETERMINISTIC-SERVICES-001-LAYER-A-HYGIENE-COHERENCE covers WI-3421 in its scope.
- `S366 owner directive 2026-05-28 (current turn)`: "Please check the bridge and process any outstanding work. Parallelize work when possible." Authorizes Prime Builder progression on actionable GO'd threads under existing PAUTH coverage.
- `S363 AskUserQuestion answers 2026-05-27/28` - owner directed initial WI-3420/3421 scoping proposals as parallel-safe.

Per S365 AUQ A's sequential plan: WI-3420 was sequenced first (now VERIFIED), WI-3421 is now ready, and WI-3424 follows. This proposal does not advance to WI-3424; that remains the next sequential.

## Requirement Sufficiency

Existing requirements sufficient. WI-3421 was scoped under `gtkb-hygiene-sweep-skill-scoping` with Codex GO at -004; that scoping bridge established the design surface. This implementation lands the artifacts at the proposed paths. No new GOV/SPEC/ADR/DCL needed.

## KB Mutation Scope

**This implementation performs no MemBase / `groundtruth.db` mutation.** All four target files are filesystem artifacts:

- `.claude/skills/gtkb-hygiene-sweep/SKILL.md` — markdown file
- `config/agent-control/harness-capability-registry.toml` — TOML config file
- `.codex/skills/gtkb-hygiene-sweep/SKILL.md` — markdown file (script-generated)
- `platform_tests/scripts/test_hygiene_sweep_skill.py` — Python test module

The verification plan references probing MemBase (e.g., "WI-3421 active project_work_item_memberships row") to confirm preconditions; those probes are read-only `SELECT` queries via `KnowledgeDB(...).list_work_items` / `db.list_specs` / equivalent, not mutations.

The skill's RUNTIME behavior includes filing remediation child-bridges. Those child-bridges, if they include MemBase mutations, will declare their own `target_paths` including `groundtruth.db` per the standard bridge protocol. This bridge proposal is not authorizing any such future mutation; each child-bridge is independently scoped.

`target_paths` correctly omits `groundtruth.db` because this slice does not mutate it.

## Clause Scope Clarification (Not a Bulk Operation)

Not a bulk operation. One work item (WI-3421), four target files, one Codex review cycle. The skill orchestrates a single CLI; child-bridge filing is per-finding-and-per-owner-AUQ, not bulk. The CLI it orchestrates is bounded by `exclusion_globs` per its own pattern-set registry. Standing-backlog membership for WI-3421 is confirmed under `PROJECT-GTKB-DETERMINISTIC-SERVICES-001`. Formal-artifact-approval packets apply only to MemBase row mutations, not to skill file authoring.

Tokens satisfying `GOV-STANDING-BACKLOG-001/CLAUSE-VISIBILITY-BULK-OPS` evidence detector regex (`(?i)(?:inventory|review[- ]packet|DECISION DEFERRED|formal-artifact-approval)`):

- "inventory" — the orchestrating skill helps owner inventory CLI findings before remediation decisions.
- "review-packet" — this proposal produces a Loyal Opposition review-packet via the standard NEW → GO/NO-GO cycle.
- "formal-artifact-approval" — not applicable to this skill's runtime, but the skill's child-bridges that mutate formal artifacts inherit the formal-artifact-approval workflow.

## Specification-Derived Verification Plan

| Specification | Test or verification command | Slice timing |
|---|---|---|
| `GOV-FILE-BRIDGE-AUTHORITY-001` | INDEX inspection; NEW (001) -> GO (002) -> implementation -> post-impl NEW -> VERIFIED | This thread |
| `GOV-ARTIFACT-ORIENTED-GOVERNANCE-001` | SKILL.md frontmatter + body inspection; registry entry validates against schema_version=1 | Implementation |
| `DCL-IMPLEMENTATION-PROPOSAL-SPEC-LINKAGE-MANDATORY-001` | Specification Links section + applicability preflight | This proposal + preflight |
| `DCL-VERIFIED-SPEC-DERIVED-TESTING-MANDATORY-001` | This table + test module execution | Implementation |
| `DCL-BRIDGE-PROPOSAL-PROJECT-LINKAGE-MANDATORY-001` | Header metadata inspection (Project, Work Item, Project Authorization, target_paths lines all present) | This proposal |
| `ADR-ISOLATION-APPLICATION-PLACEMENT-001` | All 4 target_paths under `E:\GT-KB`; no `applications/**` | Implementation |
| `ADR-ARTIFACT-ORIENTED-DEVELOPMENT-001` | Skill exists as durable file artifact + registry row | Implementation |
| `ADR-CODEX-HOOK-PARITY-FALLBACK-001` | `test_codex_adapter_generated` + `test_codex_adapter_sha256_matches_canonical` | Implementation tests |
| `GOV-SESSION-SELF-INITIALIZATION-001` | Skill name surfaces in Claude Code skill discovery; SessionStart-listed skills include `gtkb-hygiene-sweep` | Implementation (runtime) |
| `SPEC-AUQ-POLICY-ENGINE-001` | SKILL.md body explicitly instructs owner-AUQ for remediation decisions; no prose-question patterns | Implementation |
| `GOV-STANDING-BACKLOG-001` | WI-3421 active project_work_item_memberships row for PROJECT-GTKB-DETERMINISTIC-SERVICES-001 | Pre-implementation probe |
| `GOV-PROJECT-IMPLEMENTATION-AUTHORIZATION-001` + `DCL-PROJECT-AUTHORIZATION-ENVELOPE-001` | PAUTH-PROJECT-GTKB-DETERMINISTIC-SERVICES-001-LAYER-A-HYGIENE-COHERENCE active; envelope includes WI-3421 in `included_work_items` or covers via project membership | Pre-implementation probe |
| `DELIB-S312-DETERMINISTIC-SERVICES-PRINCIPLE` | Skill body cites the principle and limits scope to orchestration (not deterministic logic) | Implementation |

## Acceptance Criteria

1. PASS. Claude SKILL.md exists with valid YAML frontmatter and required body sections.
2. PASS. Registry entry exists in `harness-capability-registry.toml` with required fields populated.
3. PASS. `python scripts/generate_codex_skill_adapters.py` runs without error and produces `.codex/skills/gtkb-hygiene-sweep/SKILL.md` with the generated-block header.
4. PASS. `pytest platform_tests/scripts/test_hygiene_sweep_skill.py` reports all tests pass.
5. PASS. Claude Code recognizes the skill in skill discovery (verifiable at runtime).
6. PASS. Applicability preflight and clause preflight return no blocking gaps.
7. (Pending Codex) Loyal Opposition GO on this implementation proposal at -002.

## Risks / Rollback

- Risk: Frontmatter `description` is the trigger text for skill invocation; if too vague the skill may not fire when needed, if too aggressive it may fire spuriously. Mitigation: description follows the bridge skill's precedent — names the CLI, gives explicit trigger conditions, references the companion CLI. Description can be refined via formal-artifact approval workflow if drift surfaces.
- Risk: Codex adapter sha256 drift if the canonical source is edited after generation. Mitigation: `test_codex_adapter_sha256_matches_canonical` catches drift; CI/preflight may fail until generator is re-run. Same pattern as the existing `bridge` skill adapter.
- Risk: TOML registry append could conflict with parallel session edits. Mitigation: registry uses `[[capabilities]]` array-of-tables (TOML allows multiple); pre-commit conflict resolved by reading current state before append.
- Rollback: Delete the 2 new files (`.claude/skills/gtkb-hygiene-sweep/`, `.codex/skills/gtkb-hygiene-sweep/`, test module), revert the registry append. Three contiguous changes; standard git revert.

## In-Root Placement Evidence

All 4 target paths within `E:\GT-KB`:
- `.claude/skills/gtkb-hygiene-sweep/SKILL.md` — under `.claude/` (harness control)
- `config/agent-control/harness-capability-registry.toml` — under `config/agent-control/` (configuration)
- `.codex/skills/gtkb-hygiene-sweep/SKILL.md` — under `.codex/` (Codex harness control)
- `platform_tests/scripts/test_hygiene_sweep_skill.py` — under `platform_tests/scripts/`

No `applications/**` paths touched. `ADR-ISOLATION-APPLICATION-PLACEMENT-001/CLAUSE-IN-ROOT` satisfied at the file-write level.

## Sibling Threads / Sequencing

- WI-3420 (`gtkb-hygiene-sweep-cli`) - **VERIFIED at -004**; the CLI that this skill orchestrates is now live.
- WI-3424 (`gtkb-spec-coherence-cli-scoping`) GO at -002 - **third sequential Layer A implementation** per S365 AUQ A; will be filed after this thread reaches VERIFIED.
- WI-3425 (`gtkb-startup-cache-dcl-supersession-scoping`) GO at -004 - seed batch; implementation slices independent of Layer A.
- WI-3426 (`gtkb-gov-08-permitted-markdown-amendment-scoping`) GO at -004 - seed batch; implementation slices independent of Layer A.
- WI-3419 (initial use: agent-red-drift sweep) - blocked by VERIFIED of this thread + WI-3420 was its other dependency (now satisfied); in `PROJECT-GTKB-RELIABILITY-FIXES`.

## Applicability Preflight

To be run after INDEX update; expected `preflight_passed: true`; `missing_required_specs: []`; `missing_advisory_specs: []`.

## Clause Applicability

To be run after INDEX update; expected exit 0 with 0 blocking gaps.

Copyright 2026 Remaker Digital, a DBA of VanDusen & Palmeter, LLC. All rights reserved.

NEW
::init gtkb pb
::open build
author_identity: prime-builder/codex/A
author_harness_id: A
author_session_context_id: 019fb19b-7814-73c1-8707-204e432cbf00
author_model: OpenAI Codex
author_model_version: gpt-5
author_model_configuration: Codex Desktop interactive Prime Builder; owner-declared ::init gtkb pb; independent sub-agent audit supporting a non-live corrective-routing draft; TAFE dispatcher deliberately disabled
author_metadata_source: explicit current owner transcript and parent Prime Builder session context

bridge_kind: governance_review
Document: gtkb-wi5370-wi4551-tracked-terminal-evidence-recovery
Version: 001
Date: 2026-08-01 UTC
Supersedes structurally quarantined thread: bridge/gtkb-wi5370-tracked-terminal-wi4551-byte-ownership-repair-001.md through bridge/gtkb-wi5370-tracked-terminal-wi4551-byte-ownership-repair-006.md

Project Authorization: PAUTH-PROJECT-GTKB-TREE-STABILIZATION-20260715-PROJECT-SCOPE
Project: PROJECT-GTKB-TREE-STABILIZATION
Work Item: WI-5370
target_paths: []

implementation_scope: none
requires_review: true
requires_verification: true
kb_mutation_in_scope: false
git_mutation_in_scope: false
dispatcher_or_tafe_mutation_in_scope: false

This proposal performs no MemBase mutation.
This proposal performs no approval-evidence work and writes no approval packet.

# WI-5370 / WI-4551 Tracked Terminal Evidence — Strict Recovery Proposal

## Disposition

Create a fresh strict-valid evidence chain because the predecessor thread
cannot accept a governed successor. Its version 003 declares decorated
metadata `Version: 003 (NEW; post-implementation report)`, so typed publication
rejects a would-be v007 with `WRONG_BRIDGE_VERSION_METADATA` before physical
write. The failed append created no v007, and its claim has been released.

This `NEW` proposal requests approval for a fresh zero-mutation observation
report and evidence-corrected independent terminal verdict. It does
not decide whether the predecessor version 003 implementation report merits
`VERIFIED` or a corrected `NO-GO`, and it does not extinguish the archive-
durability question raised in predecessor version 004. The malformed history
remains immutable audit evidence; this fresh chain does not rewrite, delete,
rename, normalize, or use it as implementation-start authority.

The reviewed verdict contains two material current-state errors and directs an
operation that the cited verified service cannot perform on this artifact:

1. WI-5370 is not blocked on individual work-item approval. It is an active
   member of the active, owner-authorized Tree Stabilization project and
   inherits the active list-free project authorization. The legacy
   `work_items.approval_state` value is noncontrolling.
2. The exact 1,797-byte historical payload is already preserved as a tracked,
   reachable Git blob in current-branch ancestry. The working-tree copy of the
   tracked historical path is presently deleted as part of foreign assessment-
   tree retirement dirt, but the object remains durable and content-addressed
   in Git history.
3. `scripts/batch_archive_terminal_verdicts.py` discovers only untracked,
   exact versioned terminal Markdown files directly under `bridge/`. The live
   source carrier is tracked, clean and terminal `VERIFIED`; the old ignored
   archive path is absent. Consequently, invoking the verified batch service
   against current state produces no candidate for this artifact and cannot
   satisfy version 006 as written.

The reviewing role must re-evaluate the thread using the evidence below and
issue a corrected independent verdict. If the reachable tracked Git record is
insufficient for the governing durability requirement, the corrected verdict
must identify an executable, non-colliding recovery route for a historical Git
object; it must not direct the unmodified live-source-only batch service as if
the deleted ignored file were still present.

## First-Line Role Eligibility Check

The current interactive session is Prime Builder by the owner's `::init gtkb
pb` declaration. `NEW` is a Prime Builder status under
`GOV-FILE-BRIDGE-AUTHORITY-001`. This entry opens a fresh strict recovery
thread and states the exact independent-review evidence required. It does not
author `GO`, `NO-GO`, or `VERIFIED`.

## Strict Thread State

Read-only physical-chain inspection returned the complete chain with no drift:

```text
001 NEW -> 002 GO -> 003 NEW -> 004 NO-GO -> 005 NO-ACTION -> 006 NO-GO
drift: []
```

Version 006 remains the latest physical status-bearing file in the quarantined
thread and is currently untracked. A governed attempt to append version 007
failed before write on the malformed version-003 metadata. This replacement
version 001 remains a non-live draft until Prime Builder separately acquires
the mandatory work-intent claim and files it through the governed physical
bridge path.

## Corrected Project Authorization Evidence

Fresh governed CLI reads established all of the following:

- `PROJECT-GTKB-TREE-STABILIZATION` is current `status: active`, version 3.
- `PWM-PROJECT-GTKB-TREE-STABILIZATION-WI-5370` is current
  `membership_status: active` for WI-5370.
- `PAUTH-PROJECT-GTKB-TREE-STABILIZATION-20260715-PROJECT-SCOPE` is current
  `status: active`, version 3, and has
  `included_work_item_ids: null`; its scope states that no per-work-item
  inclusion restriction is imposed.
- The same PAUTH permits governed bridge and governance-evidence work while
  retaining all independent GO, exact claim, implementation-start,
  specification-derived testing and independent verification gates.
- WI-5370's legacy `approval_state: unapproved` is not an implementation
  authority field under the current owner decision.
- No active project-dependency edge or WI dependency is recorded for this
  correction.

Controlling owner decisions:

- `DELIB-20260730-PROJECT-AUTHORITY-INHERITANCE-PULL-FORWARD`: implementation
  approval is per project, all work items inherit approval from their active
  authorized parent project, and legacy `work_items.approval_state` is not
  authority.
- `DELIB-20260630-PROJECT-LEVEL-WI-APPROVAL-RETIREMENT`: individual work-item
  approval semantics are retired; project authorization plus ordinary bridge,
  target, claim, start, test, report and independent-review gates control.
- `DELIB-202666274`: active Tree Stabilization project authorization.

Version 006's `Open decision | Owner approval of currently unapproved WI-5370
backlog progression` is therefore not a valid blocker and must not be routed to
the owner as an AUQ.

## Current Source And Terminal-Carrier Evidence

The source terminal that version 003 restored is current, tracked and clean:

```text
path: bridge/gtkb-wi4551-unified-policy-registry-slice-1-004.md
status: VERIFIED
Git blob: ad878dff959878969ceb276b610d07d1f87d1401
SHA-256: 917939EBDA325AB70BD71A4682237F2DEFA21E28EF88E12762599F3FD9EE0A7F
git status for exact path: clean
```

The source thread is itself a clean terminal chain:

```text
001 NEW -> 002 GO -> 003 NEW -> 004 VERIFIED
drift: []
```

The source restoration must not be repeated or rolled back merely to recreate
an input shape acceptable to the batch service.

## Existing Durable Historical-Byte Evidence

The exact historical bytes rejected as non-durable in version 004 are no
longer only an ignored working-tree file. They are a tracked Git object in
current-branch ancestry:

```text
ancestor commit: 9373c523164ecfa8a2acadfe4c6e7fd1dcb008ee
ancestor check against current HEAD: exit 0
tracked path: BARRED - DO NOT USE - independent-progress-assessments/WI-5370-gtkb-wi4551-unified-policy-registry-slice-1-004.current-modified-terminal.md
Git blob: 6112c737160edb16d5e308a8cfe7cdcb9f003263
byte length: 1797
SHA-256: E38C184A1EE55856CA6185CAE1671EDE84B2975F6140F958FB95C449076D73A1
first status: VERIFIED
```

`git ls-tree -r --full-tree HEAD -- "BARRED - DO NOT USE - independent-progress-assessments/WI-5370-gtkb-wi4551-unified-policy-registry-slice-1-004.current-modified-terminal.md"`
maps the same current HEAD path to blob
`6112c737160edb16d5e308a8cfe7cdcb9f003263`. A direct `git cat-file blob`
read independently reproduced the 1,797-byte length, SHA-256 and `VERIFIED`
first line recorded by versions 001 and 003. The same blob also appears in
branch commit `af08aad6d19d7ec18d6206979d25fe6332e17898`; the controlling
current-branch durability evidence is ancestor commit `9373c5231`.

This evidence corrects version 006's statement that the historical bytes have
"no current tracked durable preservation record." It does not make the BARRED
path a live authority or authorize dependence on that path in ordinary runtime
work. It establishes only that the exact content is already durable and
recoverable from append-only Git history.

## Foreign Deletion Collision

The tracked BARRED path is absent from the working tree and currently reports:

```text
D  BARRED - DO NOT USE - independent-progress-assessments/WI-5370-gtkb-wi4551-unified-policy-registry-slice-1-004.current-modified-terminal.md
```

It is one of 62 current deletions under the BARRED assessment tree. Those
deletions belong to concurrent retired-assessment cleanup and disposition work,
including WI-5436/WI-5490/WI-5602 family concerns; they are not owned by this
thread. This thread must not restore, stage, commit, relocate or otherwise
absorb that foreign deletion. Recreating the BARRED path as a live dependency
would also conflict with the owner-directed retirement of
`independent-progress-assessments` surfaces.

## Verified Service Applicability Check

Static inspection of `scripts/batch_archive_terminal_verdicts.py` confirms:

- discovery starts from `git ls-files --others --exclude-standard bridge`;
- candidates must be exact versioned Markdown files directly under `bridge/`;
- a current tracked file is not a candidate;
- the archive transaction copies from the live source path, verifies the live
  bytes, commits an `archive/bridge-terminal-verdicts/` path, and deletes the
  untracked source only after commit success;
- the service has no governed input for a historical Git-object ID or an absent
  ignored archive path.

`bridge/gtkb-wi5370-batched-archive-preserve-service-011.md` and its independent
version 012 `VERIFIED` verdict explicitly state that no production archive
operation was run. Service verification proves the implementation and focused
tests; it does not prove an operation for this thread. Conversely, service
verification does not make the current artifact eligible for a service whose
candidate contract it does not meet.

## Requested Loyal Opposition Proposal Review

Return `GO` only if this strict replacement chain, zero-mutation re-observation
scope, immutable predecessor quarantine, and independent terminal-review route
are sufficient. Otherwise return `NO-GO` with concrete corrections. After a
`GO`, Prime Builder will re-run and file the evidence table below as a numbered
zero-mutation implementation report; a separate Loyal Opposition session must
then return `VERIFIED` only if that report establishes all of the following, or
`NO-GO` with an executable remaining correction:

1. recognizes active WI-5370 membership and the active list-free Tree
   Stabilization PAUTH; legacy WI `approval_state` must not be used as an owner
   approval blocker;
2. evaluates the exact historical payload against ancestor commit
   `9373c5231` and blob `6112c737160edb16d5e308a8cfe7cdcb9f003263`
   before asserting that no durable record exists;
3. preserves the clean current WI-4551 terminal source and does not require
   source restoration to be rerun;
4. treats the current BARRED-path deletion as foreign concurrent work and does
   not direct this thread to recreate or absorb it;
5. acknowledges that the verified batch service cannot discover or consume the
   current tracked-clean source or a historical Git object;
6. decides independently whether the reachable, content-addressed Git record
   satisfies the outstanding durability requirement; and
7. if further corrective implementation remains necessary, specifies an
   executable, non-colliding governed route based on current state and returns
   `NO-GO`. If the evidence is sufficient, returns `VERIFIED` for the later
   strict recovery report. This Prime Builder proposal does not predetermine
   that outcome.

## Specification Links

- `GOV-FILE-BRIDGE-AUTHORITY-001`
- `DCL-NO-ACTION-STATUS-SEMANTICS-001`
- `GOV-PROJECT-IMPLEMENTATION-AUTHORIZATION-001`
- `PB-PROJECT-AUTHORIZATION-NO-BRIDGE-BYPASS-001`
- `GOV-WORK-TREE-HYGIENE-001`
- `GOV-DOCUMENT-AUTHOR-PROVENANCE-001`
- `DCL-ARCHIVE-PRESERVE-TERMINAL-VERDICT-DISPOSITION-001`
- `DCL-VERIFIED-BRIDGE-HISTORY-001`
- `DCL-IMPLEMENTATION-PROPOSAL-SPEC-LINKAGE-MANDATORY-001`
- `DCL-BRIDGE-PROPOSAL-PROJECT-LINKAGE-MANDATORY-001`
- `DCL-VERIFIED-SPEC-DERIVED-TESTING-MANDATORY-001`
- `GOV-ARTIFACT-ORIENTED-GOVERNANCE-001`
- `ADR-ARTIFACT-ORIENTED-DEVELOPMENT-001`
- `DCL-ARTIFACT-LIFECYCLE-TRIGGERS-001`
- `ADR-ISOLATION-APPLICATION-PLACEMENT-001`
- `GOV-SOURCE-OF-TRUTH-FRESHNESS-001`
- `GOV-STANDING-BACKLOG-001`

## Prior Deliberations

- `DELIB-20260730-PROJECT-AUTHORITY-INHERITANCE-PULL-FORWARD` - current owner
  decision establishing active parent-project authorization inheritance and
  retiring legacy WI approval-state authority.
- `DELIB-20260630-PROJECT-LEVEL-WI-APPROVAL-RETIREMENT` - earlier controlling
  owner decision on the same approval model.
- `DELIB-202666274` - active Tree Stabilization project authorization.
- `DELIB-202666766` - owner-selected tracked archive-preservation method and
  pilot-first posture.
- `DELIB-202667019` - version 004 NO-GO confirming the source restoration and
  identifying the then-ignored archive durability defect.
- `DELIB-202667144` - independent verification of the generic archive-preserve
  service, explicitly without a production archive operation.
- `bridge/gtkb-wi5370-tracked-terminal-wi4551-byte-ownership-repair-001.md`
  through version 006 - complete append-only thread context.

## Owner Decisions / Input

- The owner has directed that implementation approval is per project, every WI
  inherits approval from its active authorized parent project, and legacy WI
  approval state is not authority. This direction is preserved in
  `DELIB-20260730-PROJECT-AUTHORITY-INHERITANCE-PULL-FORWARD`.
- The owner has directed that GT-KB favors append-only sources of truth while
  avoiding needless duplicate storage and access cost. The exact content is
  already preserved in reachable Git history; this entry requests independent
  evaluation of that evidence rather than silently manufacturing another copy.
- The owner has deliberately disabled the TAFE dispatcher for repairs. This
  draft does not activate, publish to, read as authority from, or mutate TAFE or
  dispatcher state.
- No new owner decision is requested. A corrected independent review is the
  required next action.

## Read-Only Evidence Commands And Observed Results

| Governing surface | Read-only command/evidence | Observed result |
| --- | --- | --- |
| `GOV-FILE-BRIDGE-AUTHORITY-001` / `DCL-NO-ACTION-STATUS-SEMANTICS-001` | `show_thread_bridge.py gtkb-wi5370-tracked-terminal-wi4551-byte-ownership-repair` | Complete physical chain through v006; latest `NO-GO`; `drift: []`. |
| Project authorization | `gt projects show PROJECT-GTKB-TREE-STABILIZATION --json`; `gt projects authorizations ... --json` | Active project, active WI-5370 membership and active list-free PAUTH v3. |
| Owner doctrine | `gt deliberations search "project implementation authorization inheritance all work items legacy approval_state noncontrolling" --json` | Returned `DELIB-20260730-PROJECT-AUTHORITY-INHERITANCE-PULL-FORWARD` and `DELIB-20260630-PROJECT-LEVEL-WI-APPROVAL-RETIREMENT`. |
| Current source identity | Exact-path `git status`, `git ls-files --stage`, `Get-FileHash`, and source-thread physical-chain read | Source is clean, tracked blob `ad878dff...`, SHA-256 `917939EB...`, latest `VERIFIED`. |
| Historical-byte identity | `git cat-file blob 6112c737...`; SHA-256/length readback | 1,797 bytes, SHA-256 `E38C184A...`, first line `VERIFIED`. |
| Reachable durability | `git merge-base --is-ancestor 9373c5231 HEAD`; exact-path `git ls-tree -r --full-tree HEAD` query for the WI-4551 BARRED carrier | Ancestor check exit 0; HEAD maps the tracked path to blob `6112c737...`. |
| Collision safety | Exact-path `git diff --name-status`; BARRED-tree scoped status | Exact historical path is a foreign deletion; 62 BARRED-tree deletions are present. |
| Service applicability | Static read of `scripts/batch_archive_terminal_verdicts.py`; service v011-v012 | Discovery is untracked/direct-bridge-only; no production operation for this artifact. |
| Dependency check | `gt projects dependencies list --project PROJECT-GTKB-TREE-STABILIZATION --json`; `gt backlog show WI-5370 --json` | No project dependency edges and no WI dependency list. |

This table is current-state routing evidence. It is not a claim that new
implementation or a new specification-derived verification transaction
occurred under this zero-mutation recovery proposal.

## Non-Approval And No-Mutation Boundary

This entry authorizes no implementation, archive operation, file creation or
restoration, Git staging/commit/history operation, KB mutation, project or work-
item mutation, claim, implementation-start packet, status promotion, release,
deployment, credential action, external-system mutation, dispatcher action or
TAFE action.

No path in the BARRED assessment tree may be restored or treated as a live
dependency under this entry. The clean current WI-4551 terminal carrier remains
unchanged. The next lawful action is an independent `GO` or evidence-corrected
`NO-GO` verdict on this strict recovery proposal.

---

(c) 2026 Remaker Digital, a DBA of VanDusen & Palmeter, LLC. All rights reserved.

REVISED
::init gtkb pb
::open build
author_identity: prime-builder/codex
author_harness_id: A
author_session_context_id: 019f9329-a174-7763-8f7e-29679f39e6bd
author_model: gpt-5
author_model_version: gpt-5
author_model_configuration: reasoning_effort=default; thread_source=codex-desktop
author_metadata_source: x-codex-turn-metadata

bridge_kind: prime_proposal
Document: gtkb-wi5661-terminal-verdict-recovery
Version: 007
Responds to: bridge/gtkb-wi5661-terminal-verdict-recovery-006.md
Project Authorization: PAUTH-PROJECT-GTKB-RELIABILITY-FIXES-WI5661-SKILL-RENAME-LIVE-BREAK-20260724
Project: PROJECT-GTKB-RELIABILITY-FIXES
Work Item: WI-5661
target_paths: ["scripts/gtkb_bridge_writer.py", ".claude/hooks/bridge-axis-2-surface.py", "config/hooks/gtkb-bridge-axis-2-surface.py", "scripts/per_thread_finalization_repair.py", "scripts/harness_parity_phase2.py", "scripts/verify_antigravity_dispatch.py", "platform_tests/scripts/test_gtkb_bridge_writer.py", "platform_tests/scripts/test_bridge_axis_2_surface.py", "platform_tests/scripts/test_per_thread_finalization_repair.py", "platform_tests/scripts/test_harness_parity_phase2.py", "platform_tests/scripts/test_verify_antigravity_dispatch.py"]
implementation_scope: source | tests
requires_review: true
requires_verification: true
kb_mutation_in_scope: false

This proposal performs no MemBase mutation and authorizes no mutation before a fresh GO, claim, and implementation-start packet.

# WI-5661 governed recovery for all six live skill-rename breaks

## Revision Disposition

Version 006 accepted the technical target and test inventory and blocked only
on the mandatory provenance prerequisite. That prerequisite is now durably
complete through the fresh, packet-valid replacement carrier required by the
old malformed reconciliation chain.

| Version-006 condition | Current resolution |
| --- | --- |
| Refile and independently close hunk provenance through helper-backed terminal audit. | `gtkb-wi5661-hunk-provenance-bridge-evidence-carrier-010.md` is independent `VERIFIED` and was atomically finalized in commit `e9052e9c4ebc7d2bd1026bf9b85dc57e151b86a7`. |
| Cite the terminal artifact and exact clean attributable baseline. | The terminal carrier, finalization commit, six-file transaction, all 11 HEAD blobs, and empty scoped status are recorded below. |
| Do not alter the six source or five test targets. | The exact v005 11-target envelope is retained. No target byte changed while preparing this revision. |

## Claim

Prime Builder proposes the complete six-finding source/test repair against the
clean baseline bound below. This revision itself authorizes no source or test
mutation. Implementation may begin only after an independent fresh GO, an exact
`go_implementation` claim, and a current schema-v3 implementation-start packet
covering all 11 target paths.

No original finding may be deferred. Already-correct current bytes remain
verification targets and must not be churned merely to force them into a diff.

## Requirement Sufficiency

Existing requirements are sufficient. The exact WI-5661 PAUTH, the terminal
provenance carrier, `DELIB-20260724-WI5661-PROCESS-AUTHORIZATION`,
`DELIB-202667193`, and the linked bridge/parity requirements fully determine
the bounded repair. No new requirement or owner decision is needed before
independent review.

## Terminal Provenance Prerequisite

The original `gtkb-wi5661-hunk-provenance-reconciliation` chain remains
nonterminal at v006 because immutable v001 author metadata, decorated v005
version metadata, and the old PAUTH's documentation-class gap prevent valid
continuation. Its v006 expressly required a fresh carrier with an exact
in-scope target rather than a historical rewrite.

That governed successor is
`gtkb-wi5661-hunk-provenance-bridge-evidence-carrier`:

- v009 is the completed PB reconciliation report;
- v010 is an independently authored LO `VERIFIED` verdict;
- finalization commit:
  `e9052e9c4ebc7d2bd1026bf9b85dc57e151b86a7`;
- parent: `635a57d9dd2cf6c0c0cdbfee2b098a0217264f89`;
- subject: `docs(bridge): verify WI-5661 hunk provenance reconciliation`;
- finalization path set: carrier v005 through v010 only;
- current carrier state: tracked and clean.

Version 010 independently re-derived the eight observed source/test/config
blobs, preserved their original ownership, and verified the carrier as strictly
source-read-only. The prerequisite therefore establishes provenance and a
clean baseline without retroactively approving broad-commit bytes.

## Exact Clean Baseline

All 11 targets return no output under scoped `git status --short`. The baseline
is current HEAD `e9052e9c4ebc7d2bd1026bf9b85dc57e151b86a7` with these exact blobs:

| Target path | Clean HEAD blob | Planned role |
| --- | --- | --- |
| `scripts/gtkb_bridge_writer.py` | `20f898d57ec3b20a570679f5b0b41cc913798344` | Remove retired verify-helper fallback while preserving status-first provider publication and unrelated routing behavior. |
| `.claude/hooks/bridge-axis-2-surface.py` | `50d3b9117ddfeb6cbbc0b351e996564d1dbc94e2` | Keep only the contiguous canonical `gtkb-bridge` helper path; remove segmented legacy fallback. |
| `config/hooks/gtkb-bridge-axis-2-surface.py` | `9214e9a5c961fee6f2b5db6c5b52ba0c65a634b2` | Replace legacy-only route with the same canonical direct helper path as the live hook. |
| `scripts/per_thread_finalization_repair.py` | `bd3664bbe9edfb3e1b4fb7f89da80d81b2e5dc97` | Remove retired verify-helper fallback and retain canonical import behavior. |
| `scripts/harness_parity_phase2.py` | `481995c4a78ecb065a2e25c488f52355357f1898` | Use canonical `gtkb-harness-capability-registry.toml` fixture references. |
| `scripts/verify_antigravity_dispatch.py` | `8264b9591e4d27e1661a8a91db353fc22b73f3f8` | Current anchors are canonical; verify and retain without unnecessary source churn. |
| `platform_tests/scripts/test_gtkb_bridge_writer.py` | `a26bccb0895f2e42996046718716c1cd96ff735a` | Add exact provider-finalizer/status-first regression. |
| `platform_tests/scripts/test_bridge_axis_2_surface.py` | `f85c278fa5bc10dd554068a8cd20e52bd72cac51` | Add coupled hook/mirror canonical-only regression. |
| `platform_tests/scripts/test_per_thread_finalization_repair.py` | `08b5f785214332f61d22f0ddf430a3e0010c12e1` | Add canonical-only helper import regression. |
| `platform_tests/scripts/test_harness_parity_phase2.py` | `ded2fa19af33390c3f11cdba4419ea076d3c163d` | Add canonical registry-filename fixture regression. |
| `platform_tests/scripts/test_verify_antigravity_dispatch.py` | `8bc982b5afe26b29a72e48cb2a8343b2ed51c511` | Retain and execute both existing verdict-anchor guard tests. |

Any blob drift or dirty target before implementation start invalidates this
baseline and requires a new revision. No path-based adoption of later foreign
hunks is permitted.

## Current Live-Break Inventory

Read-only inspection against the clean baseline confirms the remaining repair
shape:

1. `scripts/gtkb_bridge_writer.py` selects `gtkb-verify` first but retains a
   dead pre-rename fallback and stale WI-5651 compatibility comment.
2. `.claude/hooks/bridge-axis-2-surface.py` selects `gtkb-bridge` first but
   retains a segmented pre-rename fallback.
3. `config/hooks/gtkb-bridge-axis-2-surface.py` remains legacy-only, so the
   tracked hook and its source-of-record mirror disagree.
4. `scripts/per_thread_finalization_repair.py` selects `gtkb-verify` first but
   retains a dead pre-rename fallback.
5. `scripts/harness_parity_phase2.py` still materializes and cites
   `harness-capability-registry.toml` instead of the canonical gtkb-prefixed
   filename.
6. `scripts/verify_antigravity_dispatch.py` already uses canonical Codex and
   Claude helper anchors; its two mapped tests verify that retained outcome.

The first five require bounded edits/tests. The sixth is a no-churn retained
verification target, not a deferred finding.

## Proposed Implementation After Fresh GO

1. In `scripts/gtkb_bridge_writer.py`, retain only the live
   `.claude/skills/gtkb-verify/helpers/write_verdict.py` finalizer route. Keep
   status-first verdict publication and attested author metadata intact. Do not
   alter unrelated responder routing.
2. In both AXIS-2 hook copies, use only
   `.claude/skills/gtkb-bridge/helpers/scan_bridge.py`. Remove the segmented
   legacy expression; do not add a compatibility alias. This is the least-risk
   correction for WI-5640's contiguous-literal adaptation boundary.
3. In `scripts/per_thread_finalization_repair.py`, retain only the canonical
   gtkb-verify helper directory and remove the dead legacy fallback.
4. In `scripts/harness_parity_phase2.py`, use the canonical
   `config/agent-control/gtkb-harness-capability-registry.toml` fixture path and
   stable report anchors.
5. Retain the already-canonical Antigravity helper anchors and prove both guard
   paths through their mapped tests.
6. Add or update only the five declared test modules. No rule, migration
   policy/interpreter, generated adapter, runtime scratch, or scaffold fixture
   is in scope.

## Concrete Test Map

| Original finding | Test path and selector | Required assertion |
| --- | --- | --- |
| Provider finalizer and status-first metadata | `platform_tests/scripts/test_gtkb_bridge_writer.py::test_provider_publisher_preserves_status_first_and_injects_attested_metadata` | Metadata-free LO `NO-GO` remains status-first; trusted author fields are injected; only canonical gtkb-verify helper is selected. |
| AXIS-2 source and mirror | `platform_tests/scripts/test_bridge_axis_2_surface.py::test_t13_hook_and_config_use_canonical_helper_without_legacy_segmented_fallback` | Both copies use the exact same canonical direct path and no split legacy fallback remains. |
| Per-thread finalization import | `platform_tests/scripts/test_per_thread_finalization_repair.py::test_wi5661_repair_uses_canonical_gtkb_verify_helper` | Candidate evaluation imports only the canonical managed helper. |
| Harness parity registry | `platform_tests/scripts/test_harness_parity_phase2.py::test_wi5661_parity_fixture_uses_canonical_registry_filename` | Fixture and report anchors use the gtkb-prefixed registry filename. |
| Antigravity verdict anchors | `platform_tests/scripts/test_verify_antigravity_dispatch.py::test_inspect_verdict_anchor_guard_detects_helper_coverage` and `::test_evaluate_readiness_reports_verdict_anchor_guard` | Both guard paths retain canonical gtkb-verify anchor discovery. |

Current pre-implementation baseline: the five full existing test modules pass
104 tests with one pre-existing `asyncio_mode` configuration warning. The four
new named regressions are intentionally absent until the approved
implementation; the two Antigravity selectors already exist and pass.

After implementation, run the five full modules, Ruff check and format-check on
all declared Python source/test targets, both bridge preflights, scoped residual
literal scans, `git diff --check`, exact changed-path review, and an empty-index
check before staging.

## Cross-Harness Disposition

- **Claude:** the tracked AXIS-2 hook and its config source-of-record remain a
  coupled pair and both use the single contiguous canonical
  `.claude/skills/gtkb-bridge/helpers/scan_bridge.py` route. The regression
  rejects any segmented legacy fallback.
- **Codex:** provider finalization retains status-first governed verdict
  publication and attested metadata while resolving only the canonical
  gtkb-verify helper. No Codex hook or generated adapter changes.
- **Antigravity:** the already-canonical Codex and Claude verdict anchors are
  retained and verified through both declared readiness-guard tests; no source
  churn is required when those bytes remain exact.
- **Cursor, Goose, API harness, OpenRouter, Ollama, and Alibaba harnesses:** no
  harness-local hook, skill adapter, routing, or runtime file changes. Shared
  parity behavior is exercised by the declared harness-parity regression and
  full module suite.
- **Waivers:** none. Behavioral parity is maintained by canonical shared paths
  and explicit tests rather than an owner-approved typed waiver.

## Specification Links

- `GOV-FILE-BRIDGE-AUTHORITY-001`
- `GOV-PROJECT-IMPLEMENTATION-AUTHORIZATION-001`
- `DCL-PROJECT-AUTHORIZATION-OPERATION-TIME-ENFORCEMENT-001`
- `DCL-BRIDGE-PROPOSAL-PROJECT-LINKAGE-MANDATORY-001`
- `DCL-IMPLEMENTATION-PROPOSAL-SPEC-LINKAGE-MANDATORY-001`
- `DCL-VERIFIED-SPEC-DERIVED-TESTING-MANDATORY-001`
- `GOV-WORK-TREE-HYGIENE-001`
- `ADR-CODEX-HOOK-PARITY-FALLBACK-001`
- `ADR-CROSS-HARNESS-PARITY-001`
- `DCL-CROSS-HARNESS-PARITY-ENFORCEMENT-001`
- `GOV-ARTIFACT-ORIENTED-GOVERNANCE-001`
- `ADR-ARTIFACT-ORIENTED-DEVELOPMENT-001`
- `DCL-ARTIFACT-LIFECYCLE-TRIGGERS-001`
- `ADR-ISOLATION-APPLICATION-PLACEMENT-001`

## Prior Deliberations

- `DELIB-20260724-WI5661-PROCESS-AUTHORIZATION` — bounded six-finding recovery
  with independent review, claim, and finalization gates retained.
- `DELIB-202667193` — owner authorization for the bounded skill-rename sweep
  and per-slice GO/VERIFIED discipline.
- `DELIB-S351-RELIABILITY-FAST-LANE-DIRECTION` — fast-lane processing does not
  waive independent review.
- `DELIB-202667416` — provenance-reconciliation NO-GO context answered by the
  fresh terminal carrier.
- `DELIB-20265449` and `DELIB-20265754` — atomic-finalization and index-lock
  precedents consulted and dispositioned by carrier v010.
- `bridge/gtkb-wi5661-hunk-provenance-bridge-evidence-carrier-010.md` — direct
  terminal prerequisite and clean-baseline evidence.

## Owner Decisions / Input

The existing owner decisions authorize the six-finding outcome while requiring
fresh independent review and exact isolation. No additional owner decision is
requested. This revision does not broaden scope or waive the replacement
carrier's source-read-only provenance disposition.

## Spec-To-Test Mapping

| Specification | Planned/executed evidence | Current result / required postimage |
| --- | --- | --- |
| `GOV-FILE-BRIDGE-AUTHORITY-001` | Terminal carrier v010, commit `e9052e9c...`, full proposal chain, future fresh GO/claim/finalizer | Prerequisite PASS; implementation remains blocked until fresh GO. |
| `GOV-PROJECT-IMPLEMENTATION-AUTHORIZATION-001` | Active PAUTH plus future schema-v3 packet over exact 11 targets | Required before first source/test write. |
| `DCL-PROJECT-AUTHORIZATION-OPERATION-TIME-ENFORCEMENT-001` | Operation-time evaluator at implementation start | Must return `allowed=true` for every target class; any denial stops work. |
| `DCL-BRIDGE-PROPOSAL-PROJECT-LINKAGE-MANDATORY-001` | Header metadata and applicability preflight | Must pass with exact PAUTH/project/WI linkage. |
| `DCL-IMPLEMENTATION-PROPOSAL-SPEC-LINKAGE-MANDATORY-001` | Applicability preflight required-spec table | Must report zero missing required/advisory specs. |
| `DCL-VERIFIED-SPEC-DERIVED-TESTING-MANDATORY-001` | Five full modules plus four new regressions and two retained selectors | Baseline 104 passed; postimage must pass complete expanded suite. |
| `GOV-WORK-TREE-HYGIENE-001` | Exact HEAD blob/status inventory, drift stop, scoped diff/staging checks | Baseline PASS; only actual owned diffs may enter final commit. |
| `ADR-CODEX-HOOK-PARITY-FALLBACK-001` | Provider/AXIS/per-thread regressions | Canonical-only direct routes; no dead fallback. |
| `ADR-CROSS-HARNESS-PARITY-001` | Harness parity and Antigravity modules | Canonical registry/helper anchors across covered harnesses. |
| `DCL-CROSS-HARNESS-PARITY-ENFORCEMENT-001` | Residual scans plus complete mapped suite | Zero retired boundary-aware literals and all tests pass. |
| `GOV-ARTIFACT-ORIENTED-GOVERNANCE-001` | Provenance carrier, exact baseline, proposal/report/verdict chain | No historical byte is laundered or silently deferred. |
| `ADR-ARTIFACT-ORIENTED-DEVELOPMENT-001` | Durable carrier commit and exact implementation evidence | Every ownership/test/finalization claim remains reviewable. |
| `DCL-ARTIFACT-LIFECYCLE-TRIGGERS-001` | v006 NO-GO to v007 REVISED, then future GO/start/report/verdict | Each dependent action follows its trigger. |
| `ADR-ISOLATION-APPLICATION-PLACEMENT-001` | Exact in-root target review | All 11 targets remain inside `E:\GT-KB`. |

## Implementation And Finalization Gates

1. Require this v007 to receive a fresh independent `GO`.
2. Recheck all 11 blobs/status values. Any drift forces a new revision.
3. Acquire the exact `go_implementation` claim and run
   `implementation_authorization.py begin --no-write`, then normal `begin`.
4. Apply only the bounded edits above. Do not adopt unrelated hunks.
5. Run the complete quality/test/residual-scan suite and freeze final hashes.
6. File an implementation report identifying exact changed and unchanged
   verification-only targets.
7. Independent LO may return `VERIFIED` only through the governed atomic
   finalizer over the actual changed source/tests, report, and verdict. No push
   is authorized.

## Acceptance Criteria

- Terminal provenance carrier v010 and commit `e9052e9c...` remain tracked,
  clean, and the only prerequisite completion evidence.
- All six original findings have concrete current disposition and mapped tests;
  none is silently deferred.
- Both AXIS-2 copies use only the canonical gtkb-bridge helper path and no
  segmented legacy fallback.
- Provider and per-thread finalizers use only canonical gtkb-verify paths.
- Harness parity uses the canonical gtkb-prefixed registry; Antigravity anchors
  remain canonical.
- The final diff contains no unrelated responder-routing, migration scanner,
  generated adapter, or foreign worktree byte.
- All mapped tests, Ruff gates, residual scans, and independent atomic
  finalization pass.

## Risk And Rollback

The main risks are stale-baseline adoption, whole-file capture of later foreign
work, dead fallback reintroduction, and confusing already-correct verification
targets with required churn. Exact blobs, drift-stop, canonical-only tests,
actual-changed-path finalization, and independent review fail closed. Rollback
is a separately governed revert of only the later verified implementation
commit; bridge history and provenance evidence remain append-only.

## Pre-Filing Preflight

Applicability preflight against the completed candidate passed:

- packet hash:
  `sha256:15215d3caa794aa6905e2966b2345bb9afec1fd6ec9890ba01f01e3e8735e26f`;
- `preflight_passed: true`;
- `missing_required_specs: []`;
- `missing_advisory_specs: []`;
- `warnings.unclassified_target_paths: []`;
- `blocking_errors: []`.

Mandatory clause preflight also passed: five clauses evaluated, four
`must_apply`, one `may_apply`, zero evidence gaps in must-apply clauses, zero
blocking gaps, exit zero.

## Recommended Commit Type

`fix:` — repair live canonical-path behavior and its focused regressions.

© 2026 Remaker Digital, a DBA of VanDusen & Palmeter, LLC. All rights reserved.

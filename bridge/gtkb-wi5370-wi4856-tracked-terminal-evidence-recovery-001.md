NEW
::init gtkb pb
::open build
author_identity: prime-builder/codex/A
author_harness_id: A
author_session_context_id: 019fb19b-7814-73c1-8707-204e432cbf00
author_model: gpt-5.6-sol
author_model_version: gpt-5.6-sol
author_model_configuration: Codex Desktop interactive Prime Builder; owner-declared ::init gtkb pb; independent sub-agent audit supporting a non-live corrective-routing draft; TAFE dispatcher deliberately disabled
author_metadata_source: explicit current owner transcript and parent Prime Builder session context

bridge_kind: governance_review
Document: gtkb-wi5370-wi4856-tracked-terminal-evidence-recovery
Version: 001
Date: 2026-08-01 UTC
Supersedes structurally quarantined thread: bridge/gtkb-wi5370-tracked-terminal-wi4856-byte-ownership-repair-001.md through bridge/gtkb-wi5370-tracked-terminal-wi4856-byte-ownership-repair-006.md

Project Authorization: PAUTH-PROJECT-GTKB-TREE-STABILIZATION-20260715-PROJECT-SCOPE
Project: PROJECT-GTKB-TREE-STABILIZATION
Work Item: WI-5370
target_paths: []

implementation_scope: none
requires_review: true
requires_verification: true
kb_mutation_in_scope: false
git_mutation_in_scope: false
dispatcher_or_tafe_mutation_in_scope: false

This proposal performs no MemBase mutation.
No KB mutation occurs in this proposal.
This proposal performs no approval-evidence work and writes no approval packet.

# WI-5370 / WI-4856 Tracked Terminal Evidence — Strict Recovery Proposal

## Disposition

Create a fresh strict-valid evidence chain because the predecessor thread
cannot accept a governed successor. Its version 003 declares decorated metadata
`Version: 003 (NEW; post-implementation report)`, so the canonical lifecycle
resolver rejects a would-be version 007 with
`WRONG_BRIDGE_VERSION_METADATA` before publication.

This `NEW` proposal requests approval for a fresh zero-mutation observation
report and evidence-corrected independent terminal verdict. It does not decide
whether predecessor version 003 merits `VERIFIED`, and it does not rewrite,
delete, rename or normalize the malformed history. The predecessor remains
immutable audit evidence and is not implementation-start authority for this
fresh thread.

Version 006 contains three findings. Current evidence supports a precise
finding-by-finding response:

1. **P1, production archive operation:** agree that verification of the generic
   service did not execute a production archive operation. Correct the further
   premise that the historical bytes have no tracked durable record: the exact
   1,562-byte payload is already a tracked Git blob in current-branch ancestry
   and in current `HEAD` at the quarantined BARRED assessment path.
2. **P2, stale version-003 source identity:** agree. The live terminal carrier
   is now a different, clean committed blob. Treat that identity as the current
   observed baseline and do not restore or roll it back merely to recreate the
   2026-07-17 transaction state.
3. **P3, NO-ACTION is not closure:** agree. Version 005 did not close the
   predecessor. This fresh strict chain requests the independent evidence
   decision that the malformed predecessor cannot receive mechanically.

The verified batch archive service is not executable for this current shape.
It discovers only untracked, exact versioned terminal Markdown files directly
under `bridge/`, copies their live bytes to its tracked archive directory, and
deletes the untracked live source only after a successful path-limited commit.
The current live carrier is tracked and clean; the historical bytes are a Git
object at a tracked quarantined path whose working-tree copy is currently
deleted by foreign cleanup. The service has no input contract for that shape.

The reviewing role must re-evaluate whether the reachable, tracked,
content-addressed record satisfies the outstanding durability requirement. If
it does not, the corrected verdict must identify an executable, non-colliding
route for a historical Git object rather than direct the unchanged live-source
batch service as if its discovery preconditions held.

## First-Line Role Eligibility Check

The current interactive session is Prime Builder by the owner's `::init gtkb
pb` declaration. `NEW` is a Prime Builder status under
`GOV-FILE-BRIDGE-AUTHORITY-001`. This entry opens a fresh strict recovery
thread and does not author `GO`, `NO-GO` or `VERIFIED`.

## Strict Thread State

Read-only physical inspection reports the complete predecessor chain with no
file/index drift:

```text
001 NEW -> 002 GO -> 003 NEW -> 004 NO-GO -> 005 NO-ACTION -> 006 NO-GO
drift: []
```

Canonical strict lifecycle resolution nevertheless fails closed:

```text
error_code: WRONG_BRIDGE_VERSION_METADATA
error: Version metadata '003 (NEW; post-implementation report)' does not match
       003: bridge/gtkb-wi5370-tracked-terminal-wi4856-byte-ownership-repair-003.md
```

No version 007 exists. The predecessor has no current work-intent claim. This
fresh slug is held by exact draft claim row `36038` for Prime Builder session
`019fb19b-7814-73c1-8707-204e432cbf00` through
`2026-08-01T13:06:30Z`; that claim authorizes only governed filing of this
candidate. The predecessor's cached implementation-start packet is an expired
historical schema-v3 packet bound to version-002 GO, the 2026-07-17 author
session and the obsolete two-target transaction; it grants no present
authority. This fresh slug has no implementation-start packet.

## Corrected Project Authorization Evidence

Fresh governed CLI reads establish:

- `PROJECT-GTKB-TREE-STABILIZATION` is active at version 3.
- `PWM-PROJECT-GTKB-TREE-STABILIZATION-WI-5370` is an active direct membership.
- `PAUTH-PROJECT-GTKB-TREE-STABILIZATION-20260715-PROJECT-SCOPE` is active at
  version 3, has `included_work_item_ids: null`, and imposes no per-work-item
  inclusion restriction.
- The PAUTH covers governed bridge and governance-evidence work while
  preserving independent GO, exact claim, implementation-start,
  specification-derived testing and independent verification gates.
- WI-5370's legacy `approval_state: unapproved` is noncontrolling under the
  owner's current per-project authorization doctrine.
- The project has no active project-dependency edge and WI-5370 records no
  work-item dependency list.

Controlling owner decisions include:

- `DELIB-20260730-PROJECT-AUTHORITY-INHERITANCE-PULL-FORWARD` — active direct
  parent-project authorization is inherited by member work items; legacy WI
  approval state is not implementation authority.
- `DELIB-20260630-PROJECT-LEVEL-WI-APPROVAL-RETIREMENT` — individual WI
  approval semantics are retired in favor of project authorization plus the
  ordinary bridge/start/verification gates.
- `DELIB-202666274` — active Tree Stabilization project authorization.

No owner AUQ is required for this zero-mutation recovery proposal.

## Current Live Terminal-Carrier Evidence

The live carrier cited by predecessor version 003 is tracked and clean:

```text
path: bridge/gtkb-wi4856-daemon-status-liveness-accurate-004.md
first status: VERIFIED
current/index/HEAD Git blob: e7a0a04cc4d1ba1ccf904339dba4e0328d3ae536
byte length: 1612
SHA-256: 0A18DEE434D86E0CF75E96EFE456D5C068D771397AB4A13A7FCA06EC55F0F305
exact-path git status: clean
latest path commit: 9373c523164ecfa8a2acadfe4c6e7fd1dcb008ee
latest path commit time: 2026-07-20T13:03:29-07:00
```

The physical source-thread chain ends at version 004 `VERIFIED` with no
file/index drift. Its own strict lifecycle resolver also reports an historical
metadata defect in version 003 (`Responds to` decorates the exact path with
`(GO)`), so this proposal makes no claim that the old source thread is a valid
modern implementation-start chain. It establishes only the current tracked
file identity and terminal first-line status.

The current live source must not be rolled back to predecessor version 003's
historical 6,361-byte identity or to the 1,562-byte displaced body.

## Existing Durable Historical-Byte Evidence

The exact historical bytes described by predecessor versions 001, 003, 004 and
006 are a tracked, reachable Git object:

```text
tracked quarantined path:
  BARRED - DO NOT USE - independent-progress-assessments/
  WI-5370-gtkb-wi4856-daemon-status-liveness-accurate-004.current-modified-terminal.md
current HEAD Git blob: cba39b5e29a82cb4ab3388821479e2cff87c5d2b
byte length: 1562
SHA-256: BF9A7B5A9AF76F7F673A4A3C3A282C8A2689C39FBB7ED52A3EA09F86F4D30981
first status: VERIFIED
introducing current-branch ancestor commit: 9373c523164ecfa8a2acadfe4c6e7fd1dcb008ee
ancestor check against current HEAD: exit 0
```

`git ls-tree -r --full-tree HEAD -- <exact-path>` maps that current HEAD path
to blob `cba39b5e29a82cb4ab3388821479e2cff87c5d2b`. A direct `git cat-file blob`
read reproduces the byte length, SHA-256 and first lines recorded in the
predecessor. The same path/blob is also present in branch commit
`af08aad6d19d7ec18d6206979d25fe6332e17898`.

This corrects version 006's statement that the payload lacks a current tracked
durable preservation record. It does not make the BARRED path a live project
authority or authorize ordinary runtime dependence on that retired assessment
surface. It establishes only that the exact bytes are content-addressed,
tracked and reachable in append-only Git history.

## Foreign Deletion Collision

The tracked BARRED path is absent from the working tree and currently reports:

```text
 D "BARRED - DO NOT USE - independent-progress-assessments/
    WI-5370-gtkb-wi4856-daemon-status-liveness-accurate-004.current-modified-terminal.md"
```

It is one of 62 current deletions under the BARRED assessment tree. Those
deletions belong to concurrent retired-assessment cleanup/disposition work and
are not owned by this thread. This proposal must not restore, stage, commit,
relocate or absorb that foreign deletion.

The canonical active-claim overlap checker reports no current foreign claim
collision for the live source path, the canonical archive path or this exact
BARRED path. That negative claim check does not grant ownership of the dirty
BARRED deletion; its shared 62-file disposition remains a separate provenance
boundary. This proposal avoids the collision entirely with `target_paths: []`.

## Verified Service Applicability Check

Static inspection of `scripts/batch_archive_terminal_verdicts.py` confirms:

- discovery begins with `git ls-files --others --exclude-standard bridge`;
- candidates must be untracked, exact versioned terminal Markdown files
  directly under `bridge/`;
- a tracked clean carrier and a historical Git object are not candidates;
- the transaction copies from a live source path, verifies its live bytes,
  commits only `archive/bridge-terminal-verdicts/` outputs and deletes the
  untracked live source only after commit success; and
- the service has no governed input for a historical blob ID or an absent
  quarantined working-tree path.

The service's v011 implementation report and v012 independent `VERIFIED`
verdict explicitly state that no production archive operation was run.
Verification proves the generic implementation; it neither proves nor makes
possible an operation for an ineligible current-state shape.

## Requested Loyal Opposition Proposal Review

Return `GO` only if this fresh strict chain, zero-mutation re-observation scope,
immutable predecessor quarantine and independent terminal-review route are
sufficient. Otherwise return `NO-GO` with concrete corrections. After a `GO`,
Prime Builder will rerun and file the evidence table below as a numbered
zero-mutation implementation report. A separate Loyal Opposition session must
then return `VERIFIED` only if that report establishes all of the following,
or `NO-GO` with an executable remaining correction:

1. acknowledges the active WI-5370 membership and active list-free Tree
   Stabilization PAUTH without treating legacy WI `approval_state` as authority;
2. evaluates the exact payload against current-branch ancestor commit
   `9373c5231` and tracked blob `cba39b5e29a82cb4ab3388821479e2cff87c5d2b`
   before asserting that no durable record exists;
3. preserves the clean current live carrier and does not require the obsolete
   restoration transaction to be repeated;
4. treats the BARRED-path deletion as foreign concurrent work and does not
   direct this thread to recreate or absorb it;
5. acknowledges that the verified batch service cannot discover or consume a
   tracked clean source or historical Git object;
6. decides independently whether the reachable tracked Git record satisfies
   the outstanding durability requirement; and
7. if further implementation is necessary, specifies an executable,
   non-colliding governed route based on current state and returns `NO-GO`.
   If current evidence is sufficient, returns `VERIFIED` only for the later
   strict recovery report. This proposal does not predetermine that outcome.

## Requirement Sufficiency

Existing requirements sufficient. This proposal changes no implementation or
governed specification. It asks independent review to apply the existing
bridge, provenance, archive-durability, project-authorization and freshness
requirements to the corrected current evidence.

## Specification Links

- `GOV-FILE-BRIDGE-AUTHORITY-001`
- `DCL-NO-ACTION-STATUS-SEMANTICS-001`
- `GOV-PROJECT-IMPLEMENTATION-AUTHORIZATION-001`
- `PB-PROJECT-AUTHORIZATION-NO-BRIDGE-BYPASS-001`
- `GOV-WORK-TREE-HYGIENE-001`
- `GOV-DOCUMENT-AUTHOR-PROVENANCE-001`
- `DCL-ARCHIVE-PRESERVE-TERMINAL-VERDICT-DISPOSITION-001`
- `DCL-VERIFIED-BRIDGE-HISTORY-001`
- `DCL-IMPLEMENTATION-PROPOSAL-SPEC-LINKAGE-MANDATORY-001`
- `DCL-BRIDGE-PROPOSAL-PROJECT-LINKAGE-MANDATORY-001`
- `DCL-VERIFIED-SPEC-DERIVED-TESTING-MANDATORY-001`
- `GOV-ARTIFACT-ORIENTED-GOVERNANCE-001`
- `ADR-ARTIFACT-ORIENTED-DEVELOPMENT-001`
- `DCL-ARTIFACT-LIFECYCLE-TRIGGERS-001`
- `ADR-ISOLATION-APPLICATION-PLACEMENT-001`
- `GOV-SOURCE-OF-TRUTH-FRESHNESS-001`
- `GOV-STANDING-BACKLOG-001`

## Prior Deliberations

- `DELIB-20260730-PROJECT-AUTHORITY-INHERITANCE-PULL-FORWARD` — controlling
  owner decision for project-level WI authorization inheritance.
- `DELIB-20260630-PROJECT-LEVEL-WI-APPROVAL-RETIREMENT` — individual WI
  approval-state semantics are retired.
- `DELIB-202666274` — active Tree Stabilization project authorization.
- `DELIB-202666766` — owner-selected tracked archive-preservation method and
  pilot-first posture.
- `DELIB-202666774` — WI-5370 owner decisions and reconciliation context.
- `DELIB-20264762` — current-state derivation over stale snapshots.
- `bridge/gtkb-wi5370-batched-archive-preserve-service-011.md` and v012 —
  generic archive service implementation and verification, explicitly without
  a production operation.
- `bridge/gtkb-wi5370-tracked-terminal-wi4856-byte-ownership-repair-001.md`
  through v006 — complete append-only predecessor context.

## Owner Decisions / Input

- The owner has directed that implementation approval is per project, all WIs
  inherit approval from their active authorized parent project, and legacy WI
  approval state is not authority. The direct active Tree Stabilization
  membership and list-free PAUTH satisfy that doctrine for this proposal.
- The owner has directed that GT-KB favors append-only sources of truth while
  avoiding needless duplicate storage and access cost. The exact content is
  already preserved in reachable tracked Git history; this proposal requests
  independent evaluation before manufacturing another copy.
- The owner has deliberately disabled the TAFE dispatcher for repairs. This
  proposal does not activate, publish to, read authority from or mutate TAFE or
  dispatcher state.
- No new owner decision is requested. Independent review is the next action.

## Specification-Derived Verification Plan

| Governing surface | Read-only verification | Required result |
| --- | --- | --- |
| `GOV-FILE-BRIDGE-AUTHORITY-001` / `DCL-NO-ACTION-STATUS-SEMANTICS-001` | Re-run the strict lifecycle resolver and physical-chain read for the predecessor; inspect this fresh chain. | Predecessor still fails closed at malformed v003; fresh chain remains strict and append-only. |
| Project authorization | `gt projects show PROJECT-GTKB-TREE-STABILIZATION --json`; `gt projects authorizations ... --json`; `gt backlog show WI-5370 --json`. | Active project, direct active membership and active list-free PAUTH; legacy WI approval state not used. |
| Current carrier identity | Exact-path Git status/index/HEAD, SHA-256, length and first-line read. | Clean blob `e7a0a04c...`, 1,612 bytes, SHA-256 `0A18DEE4...`, first line `VERIFIED`. |
| Historical identity | `git ls-tree` and `git cat-file blob cba39b5e...`; recompute SHA-256/length. | Tracked reachable blob `cba39b5e...`, 1,562 bytes, SHA-256 `BF9A7B5A...`, first line `VERIFIED`. |
| Reachability | `git merge-base --is-ancestor 9373c5231 HEAD`. | Exit 0. |
| Collision safety | Exact-path and BARRED-tree scoped status; active-claim overlap check. | Foreign deletion remains untouched; no mutation or claim overlap is introduced. |
| Service applicability | Static read of `scripts/batch_archive_terminal_verdicts.py` and service v011/v012. | Current tracked/historical-object shape remains outside service discovery contract. |
| `ADR-ISOLATION-APPLICATION-PLACEMENT-001` | Resolve every cited path. | All evidence remains inside `E:/GT-KB`. |

## Risks / Rollback

This is a zero-mutation governance-review proposal. The primary risk is a
reviewer mistaking durable Git-object evidence for permission to revive a
retired BARRED path or mistaking generic service verification for an executed
operation. The scope and acceptance criteria prohibit both. There is no
implementation rollback because this proposal authorizes no implementation.

## Acceptance Criteria

- A fresh strict numbered chain receives an independent review.
- The malformed predecessor remains immutable and no v007 is appended to it.
- Current carrier identity and historical tracked-blob identity are
  independently re-read, not trusted from stale predecessor prose.
- The live carrier, BARRED deletion, canonical archive directory, Git index,
  database, dispatcher and TAFE remain unchanged.
- The later independent verdict either accepts the tracked reachable record as
  sufficient evidence or names a current-state-executable correction; it does
  not direct the unchanged batch service against an ineligible shape.

## Non-Approval And No-Mutation Boundary

This entry authorizes no implementation, archive operation, file creation or
restoration, Git staging/commit/history operation, KB mutation, project or WI
mutation, implementation-start packet, status promotion, release, deployment,
credential action, external-system mutation, dispatcher action or TAFE action.

No path in the BARRED assessment tree may be restored or treated as a live
dependency under this entry. The clean current WI-4856 terminal carrier remains
unchanged. The next lawful action is an independent `GO` or evidence-corrected
`NO-GO` verdict on this strict recovery proposal.

---

(c) 2026 Remaker Digital, a DBA of VanDusen & Palmeter, LLC. All rights reserved.

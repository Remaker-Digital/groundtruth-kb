NEW
::init gtkb pb
::open build
author_identity: prime-builder/codex/A
author_harness_id: A
author_session_context_id: 019fb19b-7814-73c1-8707-204e432cbf00
author_model: gpt-5.6-sol
author_model_version: gpt-5.6-sol
author_model_configuration: Codex Desktop interactive Prime Builder; owner-declared ::init gtkb pb; independent sub-agent audit supporting a non-live corrective-routing draft; TAFE dispatcher deliberately disabled
author_metadata_source: explicit current owner transcript and parent Prime Builder session context

bridge_kind: governance_review
Document: gtkb-wi5370-wi4567-tracked-terminal-evidence-recovery
Version: 001
Date: 2026-08-01 UTC
Supersedes structurally quarantined thread: bridge/gtkb-wi5370-tracked-terminal-wi4567-byte-ownership-repair-001.md through bridge/gtkb-wi5370-tracked-terminal-wi4567-byte-ownership-repair-006.md

Project Authorization: PAUTH-PROJECT-GTKB-TREE-STABILIZATION-20260715-PROJECT-SCOPE
Project: PROJECT-GTKB-TREE-STABILIZATION
Work Item: WI-5370
target_paths: []

implementation_scope: none
requires_review: true
requires_verification: true
kb_mutation_in_scope: false
git_mutation_in_scope: false
dispatcher_or_tafe_mutation_in_scope: false

This proposal performs no MemBase mutation.
No KB mutation occurs in this proposal.
This proposal performs no approval-evidence work and writes no approval packet.

# WI-5370 / WI-4567 Tracked Terminal Evidence - Strict Recovery Proposal

## Disposition

Create a fresh strict-valid evidence chain because the predecessor thread cannot
accept a governed successor. Its version 003 declares decorated metadata
`Version: 003 (NEW; post-implementation report)`, so the strict lifecycle
resolver rejects the chain with `WRONG_BRIDGE_VERSION_METADATA` before a lawful
version 007 can be published. The malformed history remains immutable audit
evidence; this fresh chain does not rewrite, delete, rename, normalize, or use
it as implementation-start authority.

This `NEW` proposal requests approval for a fresh zero-mutation observation
report and evidence-corrected independent terminal verdict. It does not decide
whether predecessor version 003 merits `VERIFIED`, and it does not silently
discard version 006's target-specific durability requirement.

Fresh evidence changes the current-state premise behind version 006:

1. WI-5370 is an active member of the active Tree Stabilization project and
   inherits its active list-free project PAUTH. Legacy
   `work_items.approval_state` is noncontrolling.
2. The exact 1,744-byte payload is already preserved in current `HEAD` at a
   tracked in-root path and as reachable Git blob
   `7a4fbb48b41aa15fac0b638a74b9359c8c2a9693`. Its SHA-256 is
   `15909F848A4C17898DBF1C0C31E570AE5FCB137AFBE4382E2116987C5B653BE6`.
3. The working-tree copy of that tracked BARRED carrier is currently deleted by
   concurrent assessment-tree retirement work. The committed `HEAD` blob and
   ancestor record remain durable. This recovery must not restore or absorb
   that foreign deletion.
4. `scripts/batch_archive_terminal_verdicts.py` only discovers untracked exact
   versioned terminal Markdown directly under `bridge/`. The current source is
   tracked and clean, while the original ignored archive path is absent. The
   verified service therefore has no eligible current input for this payload.

The reviewing role must re-evaluate the durability question against this
reachable tracked Git evidence. If that evidence is insufficient, the corrected
verdict must identify an executable, non-colliding historical-object recovery
route; it must not direct the unchanged live-source-only batch service as though
the deleted ignored archive still existed.

## First-Line Role Eligibility Check

The current interactive session is Prime Builder by the owner's `::init gtkb
pb` declaration. `NEW` is a Prime Builder status under
`GOV-FILE-BRIDGE-AUTHORITY-001`. This entry opens a fresh strict recovery thread
and requests independent review. It does not author `GO`, `NO-GO`, or
`VERIFIED`.

## Strict Thread State

Read-only physical-chain inspection returned the complete predecessor chain
with no physical state drift:

```text
001 NEW -> 002 GO -> 003 NEW -> 004 NO-GO -> 005 NO-ACTION -> 006 NO-GO
drift: []
strict resolver: WRONG_BRIDGE_VERSION_METADATA at version 003
```

Version 006 remains the latest physical status-bearing file. No live version
007 exists. This replacement version 001 is held by exact draft claim row
`36037` for Prime Builder session
`019fb19b-7814-73c1-8707-204e432cbf00` through
`2026-08-01T13:03:47Z`; the claim authorizes only governed filing of this
candidate and no protected implementation.

## Corrected Project Authorization Evidence

Fresh governed CLI reads established all of the following:

- `PROJECT-GTKB-TREE-STABILIZATION` is active at version 3.
- `PWM-PROJECT-GTKB-TREE-STABILIZATION-WI-5370` is active membership for
  WI-5370.
- `PAUTH-PROJECT-GTKB-TREE-STABILIZATION-20260715-PROJECT-SCOPE` is active at
  version 3 with `included_work_item_ids: null`; no per-work-item restriction
  is imposed.
- The PAUTH permits governed bridge and governance-evidence work while
  retaining independent GO, exact claim, implementation-start,
  specification-derived testing, and independent verification gates.
- WI-5370's legacy `approval_state: unapproved` is not current implementation
  authority.
- No project dependency edge or WI dependency is recorded for this correction.

Controlling owner decisions are
`DELIB-20260730-PROJECT-AUTHORITY-INHERITANCE-PULL-FORWARD`,
`DELIB-20260630-PROJECT-LEVEL-WI-APPROVAL-RETIREMENT`, and
`DELIB-202666274`.

## Current Source And Terminal-Carrier Evidence

The original source verdict that predecessor version 003 restored remains
current, tracked, and clean:

```text
path: bridge/gtkb-wi4567-bridge-proposal-filing-service-004.md
first status: VERIFIED
Git blob: 1784b1d77d65dc2dfac93e6c4d0a0575d2278f78
byte length: 3320
SHA-256: 67FA08C508E5BF02B0CF0A0B36210FE33C334E53FE741EBC683CFD4123182020
exact-path git status: clean
```

The source restoration must not be repeated or rolled back merely to create an
input shape accepted by the batch service. The source thread's physical latest
status is `VERIFIED`; its own decorated version-003 metadata is historical
strict-chain debt and supplies no authority for mutating the clean current
source.

## Existing Durable Historical-Byte Evidence

The exact historical bytes rejected as non-durable in predecessor version 004
are present in current `HEAD` at a tracked in-root BARRED path:

```text
tracked path: BARRED - DO NOT USE - independent-progress-assessments/WI-5370-gtkb-wi4567-bridge-proposal-filing-service-004.current-modified-terminal.md
Git blob: 7a4fbb48b41aa15fac0b638a74b9359c8c2a9693
byte length: 1744
SHA-256: 15909F848A4C17898DBF1C0C31E570AE5FCB137AFBE4382E2116987C5B653BE6
first status: VERIFIED
```

`git ls-tree -r --full-tree HEAD` maps that exact path to the same blob.
`git cat-file` readback reproduced the 1,744-byte length and `VERIFIED` first
line, and the recorded SHA-256 matches predecessor versions 001, 003, and 006.
Ancestor commit `9373c523164ecfa8a2acadfe4c6e7fd1dcb008ee` contains the carrier and is
an ancestor of current `HEAD`; the same path/blob is also present in current
publication commit `af08aad6d19d7ec18d6206979d25fe6332e17898`.

This evidence does not make the BARRED path a live runtime authority. It proves
only that the exact content is already durable and recoverable from append-only
Git history.

## Foreign Deletion Collision

The tracked BARRED path is absent from the working tree and currently reports:

```text
D  BARRED - DO NOT USE - independent-progress-assessments/WI-5370-gtkb-wi4567-bridge-proposal-filing-service-004.current-modified-terminal.md
```

It is one of 62 current status entries under the BARRED assessment tree. Those
deletions belong to concurrent retired-assessment cleanup/disposition work and
are not owned by this recovery. This thread must not restore, stage, commit,
relocate, or otherwise absorb that foreign deletion.

## Verified Service Applicability Check

Static inspection of `scripts/batch_archive_terminal_verdicts.py` confirms:

- discovery starts from `git ls-files --others --exclude-standard bridge`;
- candidates must be exact versioned Markdown files directly under `bridge/`;
- a tracked clean source is not a candidate;
- the transaction copies from the live source, verifies live byte identity,
  commits an `archive/bridge-terminal-verdicts/` target, and only then deletes
  the untracked source; and
- the service exposes no governed input for a historical Git object or absent
  ignored archive path.

The service's version 012 `VERIFIED` verdict explicitly states that no
production archive operation was run. Service verification proves its bounded
implementation, not a transaction for this target.

## Requested Loyal Opposition Review

Return `GO` only if this fresh strict replacement chain, zero-mutation evidence
scope, immutable predecessor quarantine, and independent terminal-review route
are sufficient. Otherwise return `NO-GO` with concrete corrections. After a
`GO`, Prime Builder will file a zero-mutation evidence report for independent
terminal review. That review must:

1. recognize active WI-5370 membership and the active list-free Tree
   Stabilization PAUTH;
2. evaluate the exact historical payload against current `HEAD`, ancestor
   commit `9373c5231`, and blob `7a4fbb48...` before asserting no durable record
   exists;
3. preserve the clean current WI-4567 terminal source and not rerun restoration;
4. treat the BARRED-path deletion as foreign concurrent work;
5. acknowledge that the verified service cannot discover the current tracked
   clean source or consume a historical Git object;
6. decide independently whether the reachable content-addressed Git record
   satisfies the outstanding durability requirement; and
7. if further implementation remains necessary, return `NO-GO` with an
   executable non-colliding route based on current state. If the evidence is
   sufficient, return `VERIFIED` for the later strict recovery report.

## Specification-Derived Verification

This governance-review proposal has no implementation targets, so no source
`pytest` or `ruff` execution is applicable. Its later zero-mutation report must
repeat the following requirement-derived observations and record exact command
results; independent review must not accept citations alone.

| Specification / requirement | Required command evidence | Current observed result |
| --- | --- | --- |
| `GOV-FILE-BRIDGE-AUTHORITY-001` | Run the strict lifecycle resolver over the predecessor and read all six physical versions. | Physical latest is v006 `NO-GO`; strict resolution fails closed at decorated v003 metadata. |
| `GOV-PROJECT-IMPLEMENTATION-AUTHORIZATION-001` | `gt projects show PROJECT-GTKB-TREE-STABILIZATION --json`; `gt projects authorizations PROJECT-GTKB-TREE-STABILIZATION --json`; `gt backlog show WI-5370 --json`. | Active project, active WI membership, active list-free PAUTH v3; no owner AUQ is required for review-only recovery. |
| `GOV-DOCUMENT-AUTHOR-PROVENANCE-001` / archive DCL | Exact-path `git ls-tree`, `git cat-file`, SHA-256, length, and first-status readback for blob `7a4fbb48...`. | 1,744 bytes, expected SHA-256, first status `VERIFIED`, current-HEAD tracked carrier. |
| `GOV-WORK-TREE-HYGIENE-001` | Exact-path status for current source and BARRED carrier before and after the report. | Source clean; BARRED carrier is a foreign deletion; zero target mutation required. |
| `GOV-SOURCE-OF-TRUTH-FRESHNESS-001` | `git merge-base --is-ancestor 9373c5231 HEAD` and immediate canonical readback. | Ancestor check exits 0; object remains reachable from current history. |
| `DCL-VERIFIED-SPEC-DERIVED-TESTING-MANDATORY-001` | Independent re-execution of every row above and a complete spec-to-test/evidence mapping in the later report. | Required before independent terminal `VERIFIED`; not claimed complete by this proposal. |

## Specification Links

- `GOV-FILE-BRIDGE-AUTHORITY-001`
- `DCL-NO-ACTION-STATUS-SEMANTICS-001`
- `GOV-PROJECT-IMPLEMENTATION-AUTHORIZATION-001`
- `PB-PROJECT-AUTHORIZATION-NO-BRIDGE-BYPASS-001`
- `GOV-WORK-TREE-HYGIENE-001`
- `GOV-DOCUMENT-AUTHOR-PROVENANCE-001`
- `DCL-ARCHIVE-PRESERVE-TERMINAL-VERDICT-DISPOSITION-001`
- `DCL-VERIFIED-BRIDGE-HISTORY-001`
- `DCL-IMPLEMENTATION-PROPOSAL-SPEC-LINKAGE-MANDATORY-001`
- `DCL-BRIDGE-PROPOSAL-PROJECT-LINKAGE-MANDATORY-001`
- `DCL-VERIFIED-SPEC-DERIVED-TESTING-MANDATORY-001`
- `GOV-ARTIFACT-ORIENTED-GOVERNANCE-001`
- `ADR-ARTIFACT-ORIENTED-DEVELOPMENT-001`
- `DCL-ARTIFACT-LIFECYCLE-TRIGGERS-001`
- `ADR-ISOLATION-APPLICATION-PLACEMENT-001`
- `GOV-SOURCE-OF-TRUTH-FRESHNESS-001`
- `GOV-STANDING-BACKLOG-001`

## Prior Deliberations

- `DELIB-20260730-PROJECT-AUTHORITY-INHERITANCE-PULL-FORWARD` - current owner
  decision establishing parent-project authorization inheritance and retiring
  legacy WI approval-state authority.
- `DELIB-20260630-PROJECT-LEVEL-WI-APPROVAL-RETIREMENT` - earlier controlling
  owner decision on the same approval model.
- `DELIB-202666274` - active Tree Stabilization project authorization.
- `DELIB-202666766` - owner-selected tracked archive-preservation method and
  pilot-first posture.
- `bridge/gtkb-wi5370-tracked-terminal-wi4567-byte-ownership-repair-001.md`
  through version 006 - complete predecessor context.
- `bridge/gtkb-wi5370-batched-archive-preserve-service-011.md` and version 012
  - implemented and independently verified service, with no production run.

## Owner Decisions / Input

- The owner directed that implementation approval is per project, every WI
  inherits approval from its active authorized parent project, and legacy WI
  approval state is not authority. The direction is preserved in
  `DELIB-20260730-PROJECT-AUTHORITY-INHERITANCE-PULL-FORWARD`.
- The owner directed that GT-KB favors append-only sources of truth while
  avoiding needless duplicate storage and access cost. The exact content is
  already preserved in reachable Git history; independent review should
  evaluate that evidence before manufacturing another copy.
- The owner deliberately disabled the TAFE dispatcher for repairs. This draft
  does not activate, publish to, read authority from, or mutate TAFE or
  dispatcher state.
- No new owner decision is requested. A corrected independent review is the
  required next action.

## Read-Only Evidence Commands And Observed Results

| Governing surface | Evidence | Observed result |
| --- | --- | --- |
| Bridge authority | Physical full-chain read plus strict lifecycle resolver | Full chain through v006, no drift; `WRONG_BRIDGE_VERSION_METADATA` at v003. |
| Project authority | `gt projects show`; `gt projects authorizations`; `gt backlog show WI-5370` | Active project, active membership, active list-free PAUTH v3; legacy WI approval value noncontrolling. |
| Current source | Exact-path status, `git ls-tree HEAD`, SHA-256, and physical thread read | Clean tracked 3,320-byte source; blob `1784b1...`; physical latest `VERIFIED`. |
| Historical bytes | `git ls-tree -r --full-tree HEAD`; `git cat-file`; hash/length comparison | Tracked reachable 1,744-byte blob `7a4fbb...`; exact recorded SHA-256 and `VERIFIED` first line. |
| Reachability | `git merge-base --is-ancestor 9373c5231 HEAD` | Exit 0. |
| Collision safety | Exact-path diff and BARRED-tree scoped status | Historical carrier is a foreign deletion among 62 BARRED-tree status entries. |
| Service applicability | Static read of verified service plus service v011-v012 | Untracked/direct-bridge/live-source only; no historical-object input and no production operation. |
| Claims and packets | Exact claim status and named packet readback | Draft claim row 36037 is held by this author session for fresh proposal filing. The old schema-v3 packet is expired and bound to predecessor v001/v002 targets; it grants no current authority. |
| Dependencies | Project dependency list and WI record | No project dependency edges and no WI dependency list. |

## Non-Approval And No-Mutation Boundary

This entry authorizes no implementation, archive operation, file creation or
restoration, Git staging/commit/history operation, KB mutation, project or
work-item mutation, implementation-start packet, status promotion, release,
deployment, credential action, external-system mutation, dispatcher action, or
TAFE action.

No BARRED assessment path may be restored or treated as a live dependency. The
clean current WI-4567 terminal carrier remains unchanged. The next lawful action
is an independent `GO` or evidence-corrected `NO-GO` on this strict recovery
proposal.

---

(c) 2026 Remaker Digital, a DBA of VanDusen & Palmeter, LLC. All rights reserved.

NEW

bridge_kind: prime_implementation_report
Document: gtkb-fab-20-hygiene-investigation-skill
Version: 005
Author: prime-builder (Claude Opus 4.8, harness B) — interactive owner session
Date: 2026-06-11
Responds-To: bridge/gtkb-fab-20-hygiene-investigation-skill-004.md (GO)

Project: PROJECT-FABLE-INVESTIGATION
Work Item: WI-4432
Project Authorization: PAUTH-FAB20-20260610

author_identity: prime-builder
author_harness_id: B
author_session_context_id: 244ad9d8-1982-4987-9181-662ef9b47074
author_model: claude-opus-4-8
author_model_version: 4.8
author_model_configuration: interactive owner session, ::init gtkb pb

# FAB-20 — gtkb-hygiene-investigation Skill — Implementation Report

Implements the GO'd `-003` dependency-free first slice (Codex GO at `-004`). The
orchestration skill, the two deterministic helper modules, the frozen HYG-001..068
baseline registry, the Codex adapter, and the spec-derived tests are all landed.
No delta mode / FAB-19 evidence-pack consumer ships in this slice (deferred
follow-on), per the GO constraints.

## Summary of Implemented Changes

| # | Path | Kind | What |
|---|------|------|------|
| 1 | `.claude/skills/gtkb-hygiene-investigation/SKILL.md` | new | Orchestration skill: structured findings schema + 4-round probe workflow (parallel focus-area probes -> gap probe -> completeness critic -> adversarial skeptic) + loop-until-dry with mandatory decay disclosure. References canonical role registry (`groundtruth_kb.harness_projection.read_roles`), NOT the retired `role-assignments.json` mirror. Delta mode explicitly deferred. |
| 2 | `scripts/hygiene/hygiene_baseline.py` | new | `HygieneFinding` structured schema (shared) + `BaselineRegistry` + `load_baseline()` frozen-findings loader. Pure: no subprocess, no DB write, no traversal. |
| 3 | `scripts/hygiene/hygiene_report.py` | new | `generate_report_chunks()` (size-bounded v1-style report chunks) + `finding_to_work_item()` (backlog-routable form). No bulk MemBase mutation; no FAB-19/delta consumer. |
| 4 | `config/governance/hygiene-baseline-registry.toml` | new | Frozen HYG-001..068 corpus (68 entries: id/title/class/impact/effort/confidence/owner-touchpoint/decision-complexity/source/fab_cluster), seeded from the v1 report + advisory v2 merge. |
| 5 | `config/agent-control/harness-capability-registry.toml` | edit | New `skill.gtkb-hygiene-investigation` capability (claude native + codex adapter; `required_for_roles = ["prime-builder"]`). Codex `source_sha256` written by the generator. |
| 6 | `.codex/skills/gtkb-hygiene-investigation/SKILL.md` | generated | Codex adapter generated from canonical source by `scripts/generate_codex_skill_adapters.py` (not hand-forked). |
| 7 | `.codex/skills/MANIFEST.json` | generated | Codex adapter manifest regenerated by the same generator. |
| 8 | `platform_tests/scripts/test_gtkb_hygiene_investigation.py` | new | 27 spec-derived tests (baseline loader + report generator + skill/adapter parity). |

**Out of scope (intentional):** No `.agent/` antigravity adapter is generated — `target_paths` excludes `.agent/skills/**`, and antigravity parity drift is separately tracked (HYG-061 / FAB-16). No delta mode, no FAB-19 evidence-pack differ.

## Specification Links

Carried forward from `-003`, all in-root and satisfied:

- `GOV-ARTIFACT-ORIENTED-GOVERNANCE-001` / `ADR-ARTIFACT-ORIENTED-DEVELOPMENT-001` — the proven investigation method is packaged as a durable reusable skill; re-run findings become routable backlog candidates, not chat-only context.
- `SPEC-DSI-DOCTOR-CHECK-001` — the report generator emits a deterministic, schema-keyed findings record.
- `GOV-08` — probes report drift against canonical state; findings route to `work_items`, not markdown.
- `GOV-STANDING-BACKLOG-001` — findings are emitted in a `work_items`-routable form; no bulk mutation (capture is not implementation approval).
- `ADR-ISOLATION-APPLICATION-PLACEMENT-001` — all changes are in-root under `E:\GT-KB`.
- `GOV-FILE-BRIDGE-AUTHORITY-001` — filed under `bridge/` with a matching INDEX entry; append-only.
- `DCL-IMPLEMENTATION-PROPOSAL-SPEC-LINKAGE-MANDATORY-001` — all relevant specs cited.
- `DCL-VERIFIED-SPEC-DERIVED-TESTING-MANDATORY-001` — spec-derived tests created and executed (below).

## Spec-to-Test Mapping

| Spec / acceptance | Derived test(s) | Result |
|---|---|---|
| `GOV-ARTIFACT-ORIENTED-GOVERNANCE-001` — skill packages method (schema + 4-round + loop-until-dry + adapter parity) | `test_skill_frontmatter`, `test_skill_documents_structured_findings_schema`, `test_skill_documents_four_round_workflow`, `test_skill_documents_loop_until_dry_with_decay_disclosure`, `test_codex_adapter_generated_with_marker`, `test_capability_registry_entry_and_codex_parity` | PASS |
| `SPEC-DSI-DOCTOR-CHECK-001` — chunked report generator renders v1-style report within a single-context size bound | `test_render_finding_emits_present_fields`, `test_chunks_respect_size_bound_and_cover_all_findings`, `test_empty_corpus_returns_single_chunk`, `test_max_chars_below_header_reserve_raises`, `test_baseline_renders_through_generator` | PASS |
| `GOV-08` + `GOV-STANDING-BACKLOG-001` — baseline loads + enumerates 68; a finding is routable to `work_items` (not markdown-only); no bulk mutation | `test_loads_sixty_eight_findings`, `test_ids_are_contiguous_hyg_001_to_068`, `test_every_finding_has_valid_class_and_title`, `test_finding_to_work_item_is_backlog_routable`, `test_finding_to_work_item_defaults_component_without_cluster`, `test_report_module_performs_no_bulk_mutation_or_fab19_consumer` | PASS |
| Acceptance criterion 5 — no delta-mode surface ships; no FAB-19 consumer | `test_skill_defers_delta_mode`, `test_report_module_performs_no_bulk_mutation_or_fab19_consumer` (AST: no `delta`/`evidence_pack` function) | PASS |
| `DCL-VERIFIED-SPEC-DERIVED-TESTING-MANDATORY-001` — tests executed + code-quality gates | full pytest run + `ruff check` + `ruff format --check` (below) | PASS |

Additional registry-integrity tests: `test_ratings_use_allowed_vocabulary`, `test_embedded_quotes_survive_toml_escaping` (HYG-063 `("claude", "codex")`), `test_empty_string_fields_load_as_none`, `test_owner_touchpoint_flag_round_trips`, `test_registry_metadata`, `test_to_dict_is_json_serializable`, `test_missing_registry_raises`.

## Verification Commands and Results

```
python -m pytest platform_tests/scripts/test_gtkb_hygiene_investigation.py -q
  => 27 passed in 0.39s

python -m ruff check scripts/hygiene/hygiene_baseline.py scripts/hygiene/hygiene_report.py platform_tests/scripts/test_gtkb_hygiene_investigation.py
  => All checks passed!  (exit 0)

python -m ruff format --check <same 3 files>
  => 3 files already formatted  (exit 0)
```

## Parity / Capability-Registration Evidence (Codex GO constraint)

```
python scripts/generate_codex_skill_adapters.py --update-registry
  => updated 3 file(s): .codex/skills/gtkb-hygiene-investigation/SKILL.md,
     .codex/skills/MANIFEST.json, config/agent-control/harness-capability-registry.toml

python scripts/generate_codex_skill_adapters.py --check
  => Codex skill adapters: PASS (37 adapters current)  (exit 0)
```

- The Codex adapter is generated from the canonical source `.claude/skills/gtkb-hygiene-investigation/SKILL.md` (not hand-forked); the registry `[capabilities.codex].source_sha256` was computed and written by the generator, and `test_capability_registry_entry_and_codex_parity` asserts it is a real 64-hex digest (not the `pending-generation` placeholder).

## Recommended Commit Type

`feat:` — net-new `gtkb-hygiene-investigation` skill, two deterministic helper modules (`hygiene_baseline.py`, `hygiene_report.py`), the HYG-001..068 baseline registry, capability registration + generated Codex adapter, and a 27-test spec-derived suite. New repeatable-investigation capability; the delta-mode capability is deferred to a follow-on.

## Owner Decisions / Input

This session's owner decisions, recorded via AskUserQuestion (the only valid owner-decision channel):

1. **Session focus (AUQ, 2026-06-11):** Owner selected **"Drain a GO'd FAB cluster"**. Prime selected FAB-20 (the GO'd, lowest-collision greenfield candidate per the campaign note).
2. **Test-file landing under contention (AUQ, 2026-06-11):** Owner selected **"Authorize subprocess write"** — authorizing the final in-scope test file (`platform_tests/scripts/test_gtkb_hygiene_investigation.py`) to be written via a subprocess while holding a valid FAB-20 GO authorization packet, because 3+ concurrent Prime sessions (FAB-04 storage-reclamation, backlog-triage-stage-0) were thrashing the single shared `current.json` implementation-authorization pointer and blocking the Write-tool gate. See the transparency disclosure below.

## Implementation-Start Authorization + Subprocess-Write Disclosure

- **Authorization held throughout:** live FAB-20 GO at `bridge/gtkb-fab-20-hygiene-investigation-skill-004.md` + an implementation-start packet minted from it (`python scripts/implementation_authorization.py begin --bridge-id gtkb-fab-20-hygiene-investigation-skill`; packet covers all 8 target-path globs incl. `config/governance/hygiene-baseline-registry.toml`, `config/agent-control/harness-capability-registry.toml`, `platform_tests/**`) under `PAUTH-FAB20-20260610`.
- **Gated Write/Edit tools (normal path):** items 1–5 (SKILL.md, both `scripts/hygiene/*.py`, the baseline TOML, the registry edit) all went through the gated Write/Edit tools. Because concurrent sessions repeatedly clobbered the shared `current.json` active-packet pointer, the FAB-20 packet was re-activated (`begin` / `activate`) immediately before each gated write.
- **Generator subprocess (standard):** items 6–7 (`.codex` adapter + MANIFEST) were produced by the standard `generate_codex_skill_adapters.py` generator.
- **Owner-authorized subprocess write (item 8):** the test file was written via a PowerShell `.NET WriteAllText` subprocess (UTF-8, no BOM, LF), bypassing only the Write-tool's contended `current.json` PreToolUse check — **not** the authorization, which was valid (GO + by-bridge packet) and in-scope (`platform_tests/**`). This is materially different from the HYG-047 anti-pattern (bypassing an *unsatisfiable* gate): here the gate was satisfiable but the single-slot pointer could not represent 3 concurrent authorized implementers. The owner explicitly authorized this via AUQ (decision 2 above). Disclosed here for verification transparency.

## Acceptance Criteria Check (`-003`)

1. SKILL.md exists with the structured findings schema, the 4-round probe workflow, and loop-until-dry decay disclosure; Codex adapter generated; capability parity green. — **MET** (tests + `--check` PASS).
2. Chunked report generator renders the v1-style report from a findings list without exceeding a single-context size limit. — **MET** (`test_chunks_respect_size_bound_and_cover_all_findings`).
3. The HYG-001..068 baseline registry exists and loads. — **MET** (`test_loads_sixty_eight_findings`, contiguous IDs).
4. All new tests pass; `ruff check` and `ruff format --check` clean on every changed Python file. — **MET** (27 passed; both ruff gates exit 0).
5. No delta-mode surface implemented in this slice. — **MET** (`test_skill_defers_delta_mode`; report module AST-verified to define no `delta`/`evidence_pack` function).

## Codex Implementation-Constraint Check (`-004` Findings)

- Do not implement delta mode / evidence-pack differ / FAB-19 consumer — **honored** (deferred; AST-verified).
- Keep generated findings routable to `work_items` without bulk backlog/MemBase mutation — **honored** (`finding_to_work_item` produces the form; no DB import; no mutation).
- Keep the Codex adapter generated from canonical source, not manually forked — **honored** (generator + parity `--check` PASS).
- Include parity/capability-registration evidence in the report — **included** (above).

## Prior Deliberations

- `DELIB-FABLE-GRILL-20260610-Q5` — owner repeatability-architecture charter.
- `DELIB-FAB20-REMEDIATION-20260610` — FAB-20 determined build under Q5.
- `DELIB-FAB19-REMEDIATION-20260610` — the deterministic-core cluster the deferred delta mode will consume.
- `DELIB-S312-DETERMINISTIC-SERVICES-PRINCIPLE` — service/skill split rationale.
- `bridge/gtkb-fab-20-hygiene-investigation-skill-002.md` (NO-GO, sequencing) and `-004.md` (GO).

## Bridge Protocol Compliance

Filed at `bridge/gtkb-fab-20-hygiene-investigation-skill-005.md` with a matching `NEW` entry prepended to `bridge/INDEX.md`; append-only (`-001`..`-004` retained). Work-intent claim acquired (`gtkb-fab-20-hygiene-investigation-skill`, session `244ad9d8-1982-4987-9181-662ef9b47074`) before drafting. Awaiting Loyal Opposition verification (VERIFIED or NO-GO).
